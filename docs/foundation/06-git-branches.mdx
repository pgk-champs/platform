---
title: 'Git: ветки и merge'
audience: 'все'
level: 'база'
order: 6
---

import ChapterCover from '@site/src/components/ChapterCover';

<ChapterCover chapterId="git-branches" />

import SyntaxBreakdown from '@site/src/components/SyntaxBreakdown';
import CodeTyping from '@site/src/components/CodeTyping';
import Flashcards from '@site/src/components/Flashcards';
import ToolCard from '@site/src/components/ToolCard';
import SelfCheck from '@site/src/components/SelfCheck';
import ChapterExam from '@site/src/components/ChapterExam';
import Block from '@site/src/components/Block';
import Hint from '@site/src/components/Hint';
import CollapsibleToc from '@site/src/components/CollapsibleToc';
import ChapterProgress from '@site/src/components/ChapterProgress';
import SectionAnchor from '@site/src/components/SectionAnchor';
import GitSim from '@site/src/components/GitSim';

# Git: ветки и merge

<ChapterProgress chapterId="git-branches" totalSections={14} totalQuizzes={6} totalTrainers={5} />

## Зачем это нужно

Ты уже умеешь сохранять состояние проекта коммитами. Но вот ситуация: проект работает, и тебе хочется попробовать рискованную идею — переделать оформление, переписать кусок кода. А вдруг не получится? Ломать работающую версию страшно, а копировать папку проекта «на всякий случай» — снова путь к хаосу из `project_final_2_new`, с которого начиналась прошлая глава. Для этого в Git есть ветка (branch) — параллельная линия истории внутри одного и того же репозитория. Ты сворачиваешь с основной дороги, экспериментируешь сколько хочешь, и основная версия при этом остаётся нетронутой: не вышло — просто вернулся обратно, как будто ничего и не было. А получилось — результат вливается в основную ветку одной командой: это называется слияние (merge).

Так устроена работа любой команды разработчиков, и на чемпионате «Профессионалы» в том числе: каждый участник — а иногда и один участник, но по разным задачам — пилит свою часть в своей ветке, а потом всё сливается вместе в одну историю. Иногда при слиянии Git встречает противоречие — конфликт (conflict): обе ветки поменяли одну и ту же строку по-разному, и машина не готова угадывать, кто прав. Звучит грозно, но на деле это обычная рабочая ситуация с понятным алгоритмом решения — научиться его проходить спокойно и быстро важнее, чем никогда не встречать конфликтов вообще. В этой главе, как и в прошлой, весь путь разобран по молекулам: что происходит внутри `.git` после каждой команды и что означает каждое слово в ответе терминала.

<CollapsibleToc toc={toc} chapterId="git-branches" />

<SectionAnchor chapterId="git-branches" sectionId="why" />

## Сквозной пример: ветки в том же my-app

Продолжаем ровно тот проект `my-app`, на котором остановилась прошлая глава: три коммита, файл `main.py` с двумя строками приветствия, файл `.gitignore` с одной записью `draft.txt`. Если у тебя этой папки уже нет — не страшно, весь путь читается и без повторного запуска: главное, что каждая команда и весь вывод терминала ниже — не выдумка. Это дословный вывод настоящего Git 2.43.0 на чистой Ubuntu 24.04, полученный специально для этой главы на следующий день после главы про первый коммит — те же самые пять команд `git init` → `git add` → `git commit`, разобранные там, привели ровно к этому состоянию.

Убедимся, что мы на одной странице — состояние `my-app`, с которого стартуем:

```bash
cd my-app
git log --oneline
```

```text
fe59fd2 add gitignore for drafts
2f16b54 add welcome message
334596a add main screen
```

### git branch: какие ветки уже есть

Спросим Git, сколько веток вообще существует в этом репозитории прямо сейчас:

```bash
git branch
```

```text
* master
```

Одна-единственная строка — ветка `master`, та самая, что создалась сама при первом `git init` в прошлой главе. Звёздочка перед именем — не украшение, а рабочий индикатор: она отмечает текущую ветку, ту, на которой стоит `HEAD` и куда лягут следующие коммиты.

<Block kind="breakdown" title="Разбор вывода: git branch" chapterId="git-branches" blockId="breakdown-branch-list">
<SyntaxBreakdown parts={[
  { text: '* ', label: 'звёздочка', note: 'отмечает текущую ветку — ту, на которой ты сейчас стоишь. У остальных веток в списке звёздочки не будет' },
  { text: 'master', label: 'имя ветки', note: 'единственная ветка в свежем репозитории; создаётся автоматически при первом коммите, отдельной команды на её создание не было' },
]} />
</Block>

<SectionAnchor chapterId="git-branches" sectionId="branch-list" />

### git switch -c: создать ветку и сразу на неё перейти

Хотим попробовать доработку — добавить в `main.py` счёт и число игроков — но не портить пока рабочую версию в `master`. Заводим отдельную ветку:

<Block kind="breakdown" title="Разбор по словам: git switch -c" chapterId="git-branches" blockId="breakdown-switch-create">
<SyntaxBreakdown parts={[
  { text: 'git ', label: 'программа', note: 'сама система контроля версий' },
  { text: 'switch ', label: 'подкоманда', note: 'переключает рабочую папку и HEAD на другую ветку — именно её файлы окажутся на диске после команды' },
  { text: '-c ', label: 'флаг create', note: 'сначала создать новую ветку с указанным именем, затем сразу переключиться на неё — без флага switch умеет переходить только на уже существующую ветку' },
  { text: 'feature-scoreboard', label: 'имя ветки', note: 'придумываешь сам; принято коротко и по-английски описывать задачу, которую ветка решает' },
]} />
</Block>

```bash
git switch -c feature-scoreboard
```

```text
Switched to a new branch 'feature-scoreboard'
```

«Переключился на новую ветку feature-scoreboard» — коротко и по делу. Проверим список веток снова:

```bash
git branch
```

```text
* feature-scoreboard
  master
```

Звёздочка переехала: теперь текущая ветка — `feature-scoreboard`, а `master` просто стоит в списке рядом, никуда не делась и не изменилась.

<Hint id="git-branches-h1" type="fact">
**Ветка — не копия проекта.** Внутри `.git` ветка — это один короткий текстовый файл в `refs/heads/`, который хранит всего один hash: на какой коммит она указывает. `git branch -c` не копирует ни один файл проекта — вот почему создание ветки происходит мгновенно, даже если в проекте гигабайты кода.
</Hint>

Убедиться легко:

```bash
cat .git/refs/heads/feature-scoreboard
cat .git/refs/heads/master
```

```text
fe59fd2533f5042944c1211e96e7b96c92f5aa59
fe59fd2533f5042944c1211e96e7b96c92f5aa59
```

Прямо сейчас обе ветки — это две подписанные бумажки-закладки, указывающие ровно на один и тот же коммит `fe59fd2`. Разница появится, как только сделаем коммит: закладка `feature-scoreboard` сдвинется вперёд вместе с `HEAD`, а `master` останется лежать на месте, пока мы сами её туда не позовём.

В старых статьях и видео вместо `git switch -c` встретится `git checkout -b feature-scoreboard` — это ровно та же операция создания и переключения, просто старой универсальной командой `checkout`, которая умеет ещё много всего другого (например, отменять правки в файле) и поэтому исторически была источником путаницы. `git switch` — новее, появилась в Git 2.23 специально для того, чтобы у переключения веток была своя, ничем не перегруженная команда. Обе работают и сейчас, но новые материалы и эта глава используют `switch`.

<Block kind="quiz" title="Ветки и переключение" chapterId="git-branches" blockId="quiz-switch-create">
<SelfCheck chapterId="git-branches" quizId="q1" questions={[
  { q: 'Что физически происходит с файлами проекта на диске в момент git switch -c feature-scoreboard?', options: ['Вся папка проекта копируется в новое место', 'Ничего не копируется — создаётся лёгкая закладка (ref), указывающая на текущий коммит, и HEAD переключается на неё', 'Файлы шифруются', 'Создаётся архив .zip со всем проектом'], correct: 1, why: 'Ветка в Git — это файл-указатель с одним hash внутри, а не копия файлов. Поэтому создание ветки мгновенно, даже в огромных проектах.' },
  { q: 'После git switch -c feature-scoreboard команда git branch показывает feature-scoreboard со звёздочкой и master без неё. Что это значит?', options: ['master удалена', 'Звёздочка отмечает текущую ветку — ту, на которой стоит HEAD и куда попадут следующие коммиты', 'feature-scoreboard временная и исчезнет после перезагрузки', 'Обе ветки сейчас активны одновременно'], correct: 1, why: 'Активной может быть только одна ветка одновременно — именно она помечена звёздочкой в git branch.' },
  { q: 'Флаг -c у команды git switch — для чего он?', options: ['Копирует файлы в новую папку', 'Создаёт новую ветку с указанным именем и сразу переключается на неё', 'Удаляет текущую ветку', 'Показывает список коммитов'], correct: 1, why: '-c — от create: без него git switch умеет переключаться только на уже существующие ветки, а с флагом ещё и создаёт новую.' },
  { q: 'В старой команде git checkout -b feature-scoreboard и новой git switch -c feature-scoreboard — в чём разница по результату?', options: ['Результат разный: checkout создаёт ветку без переключения', 'Результат одинаковый — это одна и та же операция «создать и переключиться», просто через разные команды', 'checkout работает только с удалёнными ветками', 'switch появилась раньше checkout'], correct: 1, why: 'git switch появилась в Git 2.23 как более узкая замена части возможностей checkout — конкретно для переключения и создания веток, результат тот же.' },
]} />
</Block>

<SectionAnchor chapterId="git-branches" sectionId="switch-create" />

### Коммиты внутри ветки: master не шевелится

Теперь работаем на `feature-scoreboard` как обычно — правим файл, коммитим:

```bash
printf 'print("Привет, чемпионат!")\nprint("Удачи!")\nprint("Счёт: 0")\n' > main.py
git add main.py
git commit -m "add score line"
```

```text
[feature-scoreboard dd98912] add score line
 1 file changed, 1 insertion(+)
```

Заметь: квадратные скобки теперь показывают `feature-scoreboard`, а не `master` — коммит лёг именно в текущую ветку. Добавим вторую строку и закоммитим ещё раз:

```bash
printf 'print("Привет, чемпионат!")\nprint("Удачи!")\nprint("Счёт: 0")\nprint("Игроков: 2")\n' > main.py
git add main.py
git commit -m "add player count line"
```

```text
[feature-scoreboard 074d80c] add player count line
 1 file changed, 1 insertion(+)
```

Посмотрим на историю целиком, с новым флагом:

<Block kind="breakdown" title="Разбор по словам: git log --graph --all" chapterId="git-branches" blockId="breakdown-log-graph-flags">
<SyntaxBreakdown parts={[
  { text: 'git log ', label: 'команда', note: 'показать историю коммитов' },
  { text: '--oneline ', label: 'флаг', note: 'уже знаком: сжимает каждый коммит до одной строки' },
  { text: '--graph ', label: 'флаг graph', note: 'рисует ASCII-дерево слева от хешей: столбики и линии показывают, как ветки расходятся и сходятся' },
  { text: '--all', label: 'флаг all', note: 'без него git log показывает историю только текущей ветки; с --all — сразу всех веток в репозитории' },
]} />
</Block>

```bash
git log --oneline --graph --all
```

```text
* 074d80c add player count line
* dd98912 add score line
* fe59fd2 add gitignore for drafts
* 2f16b54 add welcome message
* 334596a add main screen
```

Прямая линия без единой развилки — и это не ошибка вывода. Веток в репозитории уже две, но `master` до сих пор указывает на `fe59fd2` — тот самый коммит, что лежит прямо посреди этой цепочки. Разветвление на графике появляется только там, где у веток разные *дальнейшие* коммиты; здесь `master` попросту ещё не ушёл никуда от точки, где родилась `feature-scoreboard`, поэтому взгляду не на чем расходиться. Сверим это командой `git branch`, но теперь с `-v` — «verbose», показать ещё и коммит, на котором стоит каждая ветка:

```bash
git branch -v
```

```text
* feature-scoreboard 074d80c add player count line
  master             fe59fd2 add gitignore for drafts
```

Вот она, разница, спрятанная за одинаковой картинкой графа: `feature-scoreboard` ушла на два коммита вперёд, а `master` стоит там же, где стояла до `git switch -c`.

<SectionAnchor chapterId="git-branches" sectionId="branch-commits" />

### git merge: перемотка (fast-forward)

Эксперимент удался — пора вернуть счёт и число игроков в основную ветку. Встаём на ветку-приёмник и зовём к себе изменения:

```bash
git switch master
```

```text
Switched to branch 'master'
```

```bash
cat main.py
```

```text
print("Привет, чемпионат!")
print("Удачи!")
```

Именно та старая версия — ведь на `master` мы ничего не коммитили, пока сидели в `feature-scoreboard`. Сливаем:

<Block kind="breakdown" title="Разбор по словам: git merge" chapterId="git-branches" blockId="breakdown-merge-command">
<SyntaxBreakdown parts={[
  { text: 'git ', label: 'программа', note: 'сама система контроля версий' },
  { text: 'merge ', label: 'подкоманда', note: 'merge — «слить»: перенести коммиты другой ветки в текущую' },
  { text: 'feature-scoreboard', label: 'откуда сливаем', note: 'имя ветки-источника; вливаем именно в ту ветку, на которой стоим сейчас, то есть в master' },
]} />
</Block>

```bash
git merge feature-scoreboard
```

```text
Updating fe59fd2..074d80c
Fast-forward
 main.py | 2 ++
 1 file changed, 2 insertions(+)
```

<Block kind="breakdown" title="Разбор вывода: Fast-forward" chapterId="git-branches" blockId="breakdown-fast-forward-output">
<SyntaxBreakdown parts={[
  { text: 'Updating fe59fd2..074d80c', label: 'откуда куда', note: 'закладка master стояла на fe59fd2, а после операции окажется на 074d80c — двойная точка обозначает диапазон между двумя коммитами' },
  { text: '\nFast-forward', label: 'тип слияния', note: 'перемотка: раз master не менялась всё это время, Git не создаёт отдельный коммит слияния — он просто передвигает закладку master вперёд, на последний коммит feature-scoreboard. Никакой «магии»: коммиты dd98912 и 074d80c уже существовали, менялась только метка' },
  { text: '\n main.py | 2 ++', label: 'диффстат: файл', note: 'main.py изменился; два плюсика — сокращённая шкала того, сколько строк добавилось (уже видел такую же строку в прошлой главе для одной строки)' },
  { text: '\n 1 file changed, 2 insertions(+)', label: 'диффстат: итог', note: 'суммарно по всем слитым коммитам разом: один файл, две вставленные строки — те самые «Счёт: 0» и «Игроков: 2»' },
]} />
</Block>

```bash
cat main.py
```

```text
print("Привет, чемпионат!")
print("Удачи!")
print("Счёт: 0")
print("Игроков: 2")
```

Обе строки на месте. А теперь ключевой момент, который часто ускользает: посмотрим на историю снова.

```bash
git log --oneline --graph --all
```

```text
* 074d80c add player count line
* dd98912 add score line
* fe59fd2 add gitignore for drafts
* 2f16b54 add welcome message
* 334596a add main screen
```

Ни одной новой строки по сравнению с выводом до слияния — те же пять коммитов, те же хеши. Fast-forward в принципе не создаёт нового коммита: он буквально «перематывает» закладку `master` вперёд по уже существующей цепочке, как будто с самого начала коммитил именно в `master`. Это возможно только потому, что `master` за время работы над веткой не получила ни одного собственного коммита — ей было некуда «расходиться» с `feature-scoreboard`, и слияние превращается в простое перемещение указателя.

Ветка своё дело сделала — коммиты уже в `master`, закладка `feature-scoreboard` больше не нужна:

```bash
git branch -d feature-scoreboard
```

```text
Deleted branch feature-scoreboard (was 074d80c).
```

`-d` — «удалить, но безопасно»: Git заранее проверяет, что все коммиты ветки уже присутствуют где-то ещё (в нашем случае — в `master`, после fast-forward), и только тогда соглашается удалить закладку. Сами коммиты `dd98912` и `074d80c` при этом никуда не делись — они остаются в истории `master`, просто больше не подписаны отдельным именем ветки.

<Block kind="quiz" title="Fast-forward" chapterId="git-branches" blockId="quiz-fast-forward">
<SelfCheck chapterId="git-branches" quizId="q2" questions={[
  { q: 'Git ответил Updating fe59fd2..074d80c / Fast-forward. Создался ли новый коммит слияния?', options: ['Да, всегда создаётся merge-коммит', 'Нет — при fast-forward Git просто передвигает закладку ветки на существующий коммит, нового коммита не появляется', 'Да, но только если файлы совпадают', 'Нет, но старые коммиты при этом удаляются'], correct: 1, why: 'Fast-forward — это перемотка указателя, а не создание истории: коммиты уже существовали в feature-ветке, merge просто дал на них указывать ветке master.' },
  { q: 'При каком условии git merge вообще способен сделать fast-forward?', options: ['Только если в сливаемой ветке один коммит', 'Если ветка-приёмник (например, master) не получила ни одного нового коммита с момента, когда от неё отделилась сливаемая ветка', 'Всегда, независимо от истории', 'Только на GitHub, локально не работает'], correct: 1, why: 'Fast-forward возможен, только когда истории не разошлись: ветка-приёмник стоит там же, где стояла в момент создания сливаемой ветки — тогда некуда объединять, можно просто перемотать вперёд.' },
  { q: 'После fast-forward слияния feature-scoreboard в master — что покажет git log --oneline --all: пять коммитов или семь (пять старых плюс два новых)?', options: ['Семь — коммиты дублируются для каждой ветки', 'Пять — это те же самые коммиты dd98912 и 074d80c, они не копируются, просто на них теперь смотрит и master тоже', 'Три — старые коммиты удаляются', 'Зависит от версии Git'], correct: 1, why: 'Коммит — это не файл, привязанный к одной ветке, а узел в общем графе истории. Несколько закладок (веток) могут указывать на один и тот же коммит или на цепочку, ведущую к нему.' },
  { q: 'Что реально делает git branch -d feature-scoreboard после успешного fast-forward слияния?', options: ['Удаляет из истории коммиты dd98912 и 074d80c', 'Удаляет только закладку (указатель) feature-scoreboard — сами коммиты остаются в истории master', 'Откатывает main.py к прошлой версии', 'Требует подтверждения по email'], correct: 1, why: '-d — безопасное удаление: Git проверяет, что коммиты ветки уже есть в другой ветке, и удаляет только сам указатель, а не содержимое истории.' },
]} />
</Block>

<SectionAnchor chapterId="git-branches" sectionId="merge-fast-forward" />

### git merge: настоящее слияние с коммитом (three-way merge)

Fast-forward — самый простой случай, но он работает только пока `master` стоит на месте. Устроим ситуацию, где обе ветки успели уйти вперёд каждая по-своему — и посмотрим, как Git справляется с этим уже без перемотки.

```bash
git switch -c feature-timer
```

```text
Switched to a new branch 'feature-timer'
```

```bash
printf 'print("Привет, чемпионат!")\nprint("Удачи!")\nprint("Счёт: 0")\nprint("Игроков: 2")\nprint("Таймер: 5:00")\n' > main.py
git add main.py
git commit -m "add timer line"
```

```text
[feature-timer 1c96c1a] add timer line
 1 file changed, 1 insertion(+)
```

Пока ветка `feature-timer` дописывала `main.py`, вернёмся на `master` и сделаем там *свой* коммит — но не в том же файле, а в `.gitignore`, чтобы точно не пересечься:

```bash
git switch master
```

```text
Switched to branch 'master'
```

```bash
printf 'draft.txt\n*.log\n' > .gitignore
git add .gitignore
git commit -m "ignore log files"
```

```text
[master d895572] ignore log files
 1 file changed, 1 insertion(+)
```

Теперь у обеих веток есть собственный, ничем не связанный друг с другом коммит. Наконец-то граф покажет настоящую развилку:

```bash
git log --oneline --graph --all
```

```text
* 1c96c1a add timer line
| * d895572 ignore log files
|/  
* 074d80c add player count line
* dd98912 add score line
* fe59fd2 add gitignore for drafts
* 2f16b54 add welcome message
* 334596a add main screen
```

Вот теперь линия разошлась на два столбика: верхний `*` — коммит `feature-timer`, соседний `*` чуть правее — коммит `master`, а `|/` внизу показывает место, где они разделились, — коммит `074d80c`, их общий предок. Сольём:

```bash
git merge feature-timer
```

```text
Merge made by the 'ort' strategy.
 main.py | 1 +
 1 file changed, 1 insertion(+)
```

На этот раз ни слова про fast-forward — вместо этого «слияние сделано стратегией ort» (это имя алгоритма слияния по умолчанию с Git 2.33, сменившего более старый `recursive`; расшифровывать название не нужно, достаточно знать, что это обычный, стандартный способ). Раз обе ветки успели разойтись, Git не может просто передвинуть закладку — он создаёт новый коммит, у которого не один родитель, как обычно, а сразу два: последний коммит `master` и последний коммит `feature-timer`. Посмотрим на этот коммит напрямую:

```bash
git log -1
```

```text
commit f958c3f7168aa399da131b53a68694e8b8c18989
Merge: d895572 1c96c1a
Author: Ivan Petrov <ivan@example.com>
Date:   Tue Sep 1 02:51:16 2026 +0000

    Merge branch 'feature-timer'
```

<Block kind="breakdown" title="Разбор вывода: merge-коммит" chapterId="git-branches" blockId="breakdown-merge-commit">
<SyntaxBreakdown parts={[
  { text: 'commit f958c3f7168aa399da131b53a68694e8b8c18989', label: 'hash', note: 'у merge-коммита такой же 40-символьный SHA-1, как у любого другого — сам факт, что родителей два, ничего в формате hash не меняет' },
  { text: '\nMerge: d895572 1c96c1a', label: 'строка Merge', note: 'бывает только у коммитов слияния — перечисляет хеши обоих родителей: сначала тот, на котором стоял (master), затем влитый (feature-timer)' },
  { text: '\nAuthor: Ivan Petrov <ivan@example.com>', label: 'автор', note: 'тот, кто выполнил команду git merge — не обязательно тот, кто писал код в обеих ветках' },
  { text: '\n\n    Merge branch \'feature-timer\'', label: 'сообщение по умолчанию', note: 'Git сам подставляет этот текст. В настоящем терминале на этом шаге откроется текстовый редактор с уже вписанной строкой — просто сохрани и закрой (в nano — Ctrl+O, Enter, Ctrl+X), и коммит создастся с этим сообщением; можно и переписать текст на своё, если хочется объяснить причину слияния подробнее' },
]} />
</Block>

Взглянем на итоговую картину:

```bash
git log --oneline --graph --all
```

```text
*   f958c3f Merge branch 'feature-timer'
|\  
| * 1c96c1a add timer line
* | d895572 ignore log files
|/  
* 074d80c add player count line
* dd98912 add score line
* fe59fd2 add gitignore for drafts
* 2f16b54 add welcome message
* 334596a add main screen
```

Ромб из символов `*`, `|\`, `* |`, `|/` — это и есть слияние на письме: `f958c3f` наверху — новый merge-коммит, от него вниз расходятся две линии к обоим родителям (`d895572` и `1c96c1a`), а ниже — `|/` — они снова сходятся к общему предку `074d80c`, откуда когда-то разошлись. Именно так рисуют дерево коммитов в тренажёре из следующего раздела — теперь за каждой линией стоит команда, которую ты только что запускал сам. Дело сделано — уборка:

```bash
git branch -d feature-timer
```

```text
Deleted branch feature-timer (was 1c96c1a).
```

<Block kind="quiz" title="Merge-коммит" chapterId="git-branches" blockId="quiz-three-way">
<SelfCheck chapterId="git-branches" quizId="q3" questions={[
  { q: 'Чем ситуация с feature-timer отличалась от предыдущей с feature-scoreboard и привела не к fast-forward, а к merge-коммиту?', options: ['Файл main.py стал слишком большим', 'Пока существовала feature-timer, в master тоже появился собственный коммит («ignore log files») — истории разошлись, и перематывать стало некуда', 'feature-timer создали неправильной командой', 'Разница в названиях веток'], correct: 1, why: 'Fast-forward работает только пока ветка-приёмник стоит на месте. Как только у master появился свой коммит, единственный способ объединить линии — создать коммит с двумя родителями.' },
  { q: 'В выводе git log -1 у merge-коммита есть строка Merge: d895572 1c96c1a. Что она означает?', options: ['Это два разных сообщения коммита', 'Это хеши двух родителей merge-коммита — у обычного коммита родитель один, у merge-коммита их два', 'Это ошибка слияния', 'Это версии Git на двух компьютерах'], correct: 1, why: 'Строка Merge появляется только у коммитов слияния и перечисляет хеши обоих родителей — то, что делает merge-коммит особенным в графе истории.' },
  { q: 'В графе git log --graph коммит merge-коммита отмечен *  , а строкой ниже — |\\, ещё ниже — |/. Что показывает такая картинка?', options: ['Ошибку в истории', 'Развилку и последующее схождение: |\\ — расхождение от merge-коммита к двум родителям, |/ — место ниже, где эти две линии когда-то произошли из одного общего предка', 'Что репозиторий повреждён', 'Порядок файлов в проекте'], correct: 1, why: 'ASCII-граф рисует именно форму дерева коммитов: вертикальные линии — ветки, \\  и /  — точки расхождения и схождения истории.' },
  { q: 'Сообщение merge-коммита Merge branch \'feature-timer\' появилось само. Что произошло бы в обычном терминале (не в скрипте) на этом шаге?', options: ['Git создал бы коммит без единого слова о том, что произошло', 'Git открыл бы текстовый редактор по умолчанию с уже вписанным этим текстом — достаточно сохранить и закрыть, либо переписать сообщение своими словами', 'Потребовался бы флаг -m обязательно, иначе ошибка', 'Ничего — merge-коммиты не имеют сообщений'], correct: 1, why: 'Как и у git commit без -m, Git открывает редактор — только теперь уже с заранее подготовленным сообщением слияния, которое можно просто принять или отредактировать.' },
]} />
</Block>

<Block kind="trainer" title="Git-симулятор: слей ветку сам" chapterId="git-branches" blockId="trainer-gitsim-merge">
<GitSim
  scenario="branches"
  quest={{ title: 'Создай ветку, закоммить в ней и слей её в main', goal: 'merged', optimalCommands: 6 }}
  chapterId="git-branches"
  trainerId="trainer-gitsim-merge"
/>
</Block>

А теперь закрепи разницу между перемоткой и настоящим слиянием руками — получи в одном репозитории оба ответа Git: сначала `Fast-forward`, затем `Merge made by the 'ort' strategy`.

<Block kind="trainer" title="Git-симулятор: fast-forward против merge-коммита" chapterId="git-branches" blockId="trainer-gitsim-ff-vs-merge">
<GitSim
  scenario="branches"
  quest={{ title: 'Fast-forward против merge-коммита', goal: 'ff-and-merge', optimalCommands: 15 }}
  chapterId="git-branches"
  trainerId="trainer-gitsim-ff-vs-merge"
/>
</Block>

<SectionAnchor chapterId="git-branches" sectionId="merge-three-way" />

### Конфликт: когда Git не может выбрать сам

До сих пор обе ветки трогали разные строки или разные файлы — Git прекрасно справлялся сам. А что если обе ветки поменяют одну и ту же строку по-разному? Проверим прямо на первой строке `main.py` — «Привет, чемпионат!» давно пора обновить под финал.

```bash
git switch -c feature-name-fix
```

```text
Switched to a new branch 'feature-name-fix'
```

```bash
printf 'print("Привет, финалисты!")\nprint("Удачи!")\nprint("Счёт: 0")\nprint("Игроков: 2")\nprint("Таймер: 5:00")\n' > main.py
git commit -am "update greeting for finals"
```

```text
[feature-name-fix f1d457a] update greeting for finals
 1 file changed, 1 insertion(+), 1 deletion(-)
```

<Hint id="git-branches-h2" type="tip">
Флаг `-a` у `commit` — «all»: сразу добавляет в коммит изменения уже отслеживаемых файлов, без отдельного `git add`. Работает только для файлов, которые Git уже знает, — на untracked-файлы, как в прошлой главе, не действует.
</Hint>

Вернёмся на `master` и поправим ту же самую первую строку — но по-другому:

```bash
git switch master
```

```text
Switched to branch 'master'
```

```bash
printf 'print("Привет, чемпионы!")\nprint("Удачи!")\nprint("Счёт: 0")\nprint("Игроков: 2")\nprint("Таймер: 5:00")\n' > main.py
git commit -am "update greeting to champions"
```

```text
[master 4929e77] update greeting to champions
 1 file changed, 1 insertion(+), 1 deletion(-)
```

Оба коммита изменили ровно одну и ту же строку — по-разному. Сливаем и смотрим, что скажет Git:

```bash
git merge feature-name-fix
```

```text
Auto-merging main.py
CONFLICT (content): Merge conflict in main.py
Automatic merge failed; fix conflicts and then commit the result.
```

<Block kind="breakdown" title="Разбор вывода: CONFLICT" chapterId="git-branches" blockId="breakdown-conflict-output">
<SyntaxBreakdown parts={[
  { text: 'Auto-merging main.py', label: 'Git честно попробовал сам', note: 'сначала Git всегда пытается слить автоматически — это сообщение появляется до того, как найдётся проблема' },
  { text: '\nCONFLICT (content): Merge conflict in main.py', label: 'конфликт содержимого', note: 'найдена строка, изменённая по-разному в обеих ветках, — Git не может выбрать сам и честно об этом сообщает, указывая конкретный файл' },
  { text: '\nAutomatic merge failed; fix conflicts and then commit the result.', label: 'что делать', note: 'дословно: «автоматическое слияние не удалось; исправь конфликты и затем закоммить результат». Уже сейчас Git подсказывает весь дальнейший план' },
]} />
</Block>

Ничего не сломалось. Команда завершилась с ненулевым кодом возврата — это ожидаемо, Git таким образом сигналит скриптам и IDE «слияние остановлено», а не «произошёл сбой». Спросим `git status`, что происходит:

```bash
git status
```

```text
On branch master
You have unmerged paths.
  (fix conflicts and run "git commit")
  (use "git merge --abort" to abort the merge)

Unmerged paths:
  (use "git add <file>..." to mark resolution)
	both modified:   main.py

no changes added to commit (use "git add" and/or "git commit -a")
```

<Block kind="breakdown" title="Разбор вывода: git status при конфликте" chapterId="git-branches" blockId="breakdown-conflict-status">
<SyntaxBreakdown parts={[
  { text: 'You have unmerged paths.', label: 'главный факт', note: '«у тебя есть неслитые пути» — репозиторий сейчас в промежуточном состоянии «посреди слияния», и так будет, пока конфликт не решён' },
  { text: '\n  (use "git merge --abort" to abort the merge)', label: 'аварийный выход', note: 'если решил, что сливать вообще не стоило, эта команда откатывает всё к состоянию до git merge — как будто слияния не было' },
  { text: '\nUnmerged paths:', label: 'список конфликтных файлов', note: 'здесь окажется каждый файл, где обе стороны изменили одно и то же место — в этот раз файл ровно один' },
  { text: '\n\tboth modified:   main.py', label: 'both modified', note: '«изменён с обеих сторон» — специальный статус конфликта, отличный от обычного modified из прошлой главы' },
]} />
</Block>

Откроем сам файл — Git уже вставил в него разметку конфликта прямо на месте спорной строки:

```bash
cat main.py
```

```text
<<<<<<< HEAD
print("Привет, чемпионы!")
=======
print("Привет, финалисты!")
>>>>>>> feature-name-fix
print("Удачи!")
print("Счёт: 0")
print("Игроков: 2")
print("Таймер: 5:00")
```

Читается так: между `<<<<<<< HEAD` и `=======` — версия ветки, на которой ты стоишь сейчас (`HEAD` — указатель Git на текущую ветку и её последний коммит, тот же `master`); между `=======` и `>>>>>>> feature-name-fix` — версия вливаемой ветки, её имя подписано прямо в последнем маркере. Остальные четыре строки файла не тронуты — конфликт локальный, ровно там, где обе стороны реально разошлись.

Алгоритм решения всегда один и тот же:

1. Найди маркеры `<<<<<<<` — их всегда ровно три, как в этом файле.
2. Реши, какой текст должен остаться: одна из версий, обе, или вообще новая формулировка.
3. Удали все три строки-маркера — `<<<<<<<`, `=======` и `>>>>>>>` — целиком, вместе с именами веток.
4. Скажи Git, что разобрался: `git add`, затем `git commit`.

Объединим обе идеи в одну строку:

```bash
printf 'print("Привет, чемпионы-финалисты!")\nprint("Удачи!")\nprint("Счёт: 0")\nprint("Игроков: 2")\nprint("Таймер: 5:00")\n' > main.py
cat main.py
```

```text
print("Привет, чемпионы-финалисты!")
print("Удачи!")
print("Счёт: 0")
print("Игроков: 2")
print("Таймер: 5:00")
```

Ни единого символа `<`, `=` или `>` не осталось — только чистый код. Отмечаем, что конфликт решён, и смотрим на статус ещё раз:

```bash
git add main.py
git status
```

```text
On branch master
All conflicts fixed but you are still merging.
  (use "git commit" to conclude merge)

Changes to be committed:
	modified:   main.py
```

«Все конфликты исправлены, но слияние ещё не завершено» — Git заметил, что спорных мест больше нет, и ждёт последнего шага:

```bash
git commit
```

```text
[master 556acb5] Merge branch 'feature-name-fix'
```

Обрати внимание: в этот раз после `git commit` не понадобился `-m` — раз мы посреди слияния, Git уже знает, что писать (то же поведение, что и у бесконфликтного merge-коммита из прошлого раздела), и в интерактивном терминале просто открыл бы редактор с готовым текстом. Проверим итог:

```bash
git log -1
```

```text
commit 556acb5b08dd147144c07bae1a9ae545bdc08a5a
Merge: 4929e77 f1d457a
Author: Ivan Petrov <ivan@example.com>
Date:   Tue Sep 1 02:51:16 2026 +0000

    Merge branch 'feature-name-fix'
```

Merge-коммит с двумя родителями — ровно как и в разделе про `feature-timer`: с точки зрения истории конфликтный merge и бесконфликтный устроены одинаково, разница была только в промежуточном шаге, где пришлось руками решить, какая строка правильная. Вся картина целиком:

```bash
git log --oneline --graph --all
```

```text
*   556acb5 Merge branch 'feature-name-fix'
|\  
| * f1d457a update greeting for finals
* | 4929e77 update greeting to champions
|/  
*   f958c3f Merge branch 'feature-timer'
|\  
| * 1c96c1a add timer line
* | d895572 ignore log files
|/  
* 074d80c add player count line
* dd98912 add score line
* fe59fd2 add gitignore for drafts
* 2f16b54 add welcome message
* 334596a add main screen
```

Два ромба один под другим — оба слияния, которые сделали в этой главе, целиком видны в одном графе. И последняя проверка: раз `feature-name-fix` теперь полностью влита (её коммит `f1d457a` стал предком `master` через merge-коммит), безопасное удаление сработает без вопросов:

```bash
git branch -d feature-name-fix
```

```text
Deleted branch feature-name-fix (was f1d457a).
```

<Block kind="trainer" title="Git-симулятор: устрой конфликт и разреши его" chapterId="git-branches" blockId="trainer-gitsim-conflict">
<GitSim
  scenario="branches"
  quest={{ title: 'Измени одну и ту же строку в двух ветках, получи конфликт при merge и разреши его', goal: 'conflict-resolved', optimalCommands: 12 }}
  chapterId="git-branches"
  trainerId="trainer-gitsim-conflict"
/>
</Block>

<Block kind="quiz" title="Конфликт" chapterId="git-branches" blockId="quiz-conflict">
<SelfCheck chapterId="git-branches" quizId="q4" questions={[
  { q: 'Что означают три маркера <<<<<<< HEAD, ======= и >>>>>>> feature-name-fix внутри файла с конфликтом?', options: ['Файл повреждён и не читается', 'Между первым и вторым — версия текущей ветки (HEAD), между вторым и третьим — версия вливаемой ветки (её имя подписано в третьем маркере)', 'Это три разных файла, склеенных в один', 'Маркеры нужно закомментировать, а не удалять'], correct: 1, why: 'Git показывает обе конфликтующие версии прямо в файле, подписывая, где чья, чтобы ты выбрал или объединил их сам.' },
  { q: 'После git merge с конфликтом команда завершилась ненулевым кодом возврата. Значит ли это, что что-то сломалось?', options: ['Да, репозиторий повреждён и требует переустановки Git', 'Нет — это ожидаемый сигнал «слияние остановлено, жду решения», а не сбой; сам репозиторий цел', 'Да, файл main.py теперь потерян', 'Нет, но нужно сделать git init заново'], correct: 1, why: 'Ненулевой код — это то, как Git сообщает скриптам и IDE о незавершённом слиянии. Ничего не потеряно: и обе версии строки, и остальной файл на месте.' },
  { q: 'Каким должен быть файл после решения конфликта, прежде чем делать git add?', options: ['В нём могут остаться маркеры для истории', 'Все три вида маркеров (<<<<<<<, ======= и >>>>>>>) должны быть удалены целиком, а сама строка — переписана в финальном виде', 'Нужно оставить только строку из HEAD, вторую версию удалить целиком', 'Файл нужно переименовать'], correct: 1, why: 'Если маркеры остались в файле, они станут частью кода — Git не проверяет это за тебя, слежение за чистотой файла целиком на программисте.' },
  { q: 'Почему после конфликтного слияния git commit сработал без флага -m?', options: ['Флаг -m в принципе необязателен', 'Посреди незавершённого слияния Git уже подготовил сообщение по умолчанию (Merge branch \'...\') — как и при обычном merge-коммите без конфликтов', 'Потому что main.py маленький файл', 'Это баг именно этой версии Git'], correct: 1, why: 'Поведение то же самое, что у бесконфликтного merge-коммита из прошлого раздела: Git сам знает, что писать в сообщение о слиянии, коммит без -m лишь принимает подготовленный текст.' },
]} />
</Block>

<SectionAnchor chapterId="git-branches" sectionId="merge-conflict" />

## Типичные ответы Git на четыре частые ситуации

Не все из них — ошибки в строгом смысле; одна вообще просто информационная строка. Но каждая встречается почти на каждом проекте, и тексты ниже — снова дословный, живьём проверенный вывод, а не пересказ по памяти.

### 1. Already up to date — уже актуально

Попробуем влить `feature-name-fix` ещё раз — она ведь уже влита:

```bash
git merge feature-name-fix
```

```text
Already up to date.
```

**Перевод:** «уже актуально». **Причина:** это не ошибка и не предупреждение — Git проверил, что последний коммит `feature-name-fix` уже является предком `master` (он и правда стал им после прошлого слияния), и попросту нечего добавлять. **Что делать:** ничего — если видишь эту строку, значит, всё нужное уже здесь.

### 2. invalid reference — такой ветки не существует

```bash
git switch does-not-exist
```

```text
fatal: invalid reference: does-not-exist
```

**Перевод:** «фатальная ошибка: недействительная ссылка». **Причина:** Git ищет ветку, тег или коммит с именем `does-not-exist` — и не находит ничего похожего. Все фатальные ошибки Git возвращают код `128`, если это важно для скрипта или CI. **Как починить:** свериться со списком реальных имён через `git branch`, поправить опечатку — либо, если ветки правда ещё нет, добавить `-c`, чтобы её создать.

### 3. изменения помешали переключиться

Заведём вторую ветку с изменением в `main.py` и вернёмся на `master`:

```bash
git switch -c feature-blocker
printf 'print("Привет, чемпионы-финалисты!")\nprint("Удачи!")\nprint("Счёт: 0")\nprint("Игроков: 2")\nprint("Таймер: 5:00")\nprint("blocker branch line")\n' > main.py
git commit -am "add blocker branch line"
git switch master
```

Теперь, не коммитя, поправим `main.py` прямо на `master` и попробуем переключиться на `feature-blocker`, которая тоже трогает этот файл:

```bash
printf 'print("Привет, чемпионы-финалисты!")\nprint("Удачи!")\nprint("Счёт: 0")\nprint("Игроков: 2")\nprint("Таймер: 5:00")\nprint("незакоммиченная правка")\n' > main.py
git switch feature-blocker
```

```text
error: Your local changes to the following files would be overwritten by checkout:
	main.py
Please commit your changes or stash them before you switch branches.
Aborting
```

**Перевод:** «твои локальные изменения в следующих файлах будут перезаписаны переключением (checkout)... закоммить изменения или спрятать их (stash) перед переключением веток». Обрати внимание на слово `checkout` внутри сообщения — команда, которую мы запускали, называлась `switch`, но внутри Git это ошибка того же самого старого механизма, и сообщение об этом до сих пор не переименовали. Путаться не стоит: это именно про твой `git switch`. **Причина:** у незакоммиченной правки и у ветки, на которую переключаешься, разные версии одной и той же строки — Git отказывается тихо стереть работу, которую ты ещё не сохранил ни в одном коммите. **Как починить:** один из трёх путей — закоммитить правку (`git add` + `git commit`), спрятать её (`git stash`, тема для отдельного разбора), либо просто отменить, если она не нужна:

```bash
git restore main.py
git status
```

```text
On branch master
nothing to commit, working tree clean
```

### 4. git branch -d отказывается удалить невлитую ветку

```bash
git switch -c feature-unmerged
echo "unmerged draft idea" > idea.txt
git add idea.txt
git commit -m "wip: unmerged idea"
git switch master
git branch -d feature-unmerged
```

```text
error: the branch 'feature-unmerged' is not fully merged.
If you are sure you want to delete it, run 'git branch -D feature-unmerged'
```

**Перевод:** «ветка не полностью влита. Если уверен, что хочешь удалить её, выполни git branch -D». **Причина:** `-d` — безопасное удаление: коммит `wip: unmerged idea` не входит ни в одну другую ветку, и удаление закладки означало бы, что до него больше не добраться обычным путём (сам объект в `.git/objects` какое-то время ещё проживёт, но найти его будет заметно сложнее). **Как починить:** либо сначала слить ветку (как во всех примерах этой главы), либо, если она правда больше не нужна, — заглавная `-D`, которая делает то же самое, но без проверки:

```bash
git branch -D feature-unmerged
```

```text
Deleted branch feature-unmerged (was b488e60).
```

<Hint id="git-branches-h3" type="important">
`-D` — это буквально `-d --force`: разница ровно в одной проверке, но именно она отделяет «удалить хвост уже слитой работы» от «выбросить единственную копию незавершённой». Пользоваться заглавной версией стоит осознанно, а не по привычке.
</Hint>

<Block kind="quiz" title="Ответы Git" chapterId="git-branches" blockId="quiz-git-answers">
<SelfCheck chapterId="git-branches" quizId="q5" questions={[
  { q: 'git merge feature-name-fix во второй раз ответил Already up to date. Что произошло с историей?', options: ['Git создал ещё один пустой merge-коммит', 'Ничего — Git проверил, что коммит уже является предком текущей ветки, и добавлять нечего; история не изменилась', 'Ветка feature-name-fix была удалена', 'Это ошибка, слияние не удалось'], correct: 1, why: 'Already up to date — не ошибка, а честный ответ: Git заранее проверяет, есть ли вообще что сливать, и если нет — просто сообщает об этом.' },
  { q: 'git switch does-not-exist отвечает fatal: invalid reference: does-not-exist. Какой код возврата у любой fatal-ошибки Git?', options: ['0', '1', '128', 'Зависит от текста ошибки'], correct: 2, why: 'Fatal-ошибки Git всегда возвращают код 128 — это полезно знать при написании скриптов и в CI, где код возврата определяет, продолжать ли дальше.' },
  { q: 'Сообщение об ошибке при заблокированном переключении говорит "overwritten by checkout", хотя запускали git switch. Почему?', options: ['Это баг именно в твоей версии Git', 'Под капотом switch использует ту же старую защиту от потери несохранённых изменений, что и checkout, и сообщение об этом ещё не переименовали — но относится оно именно к твоему switch', 'Нужно было использовать команду checkout вместо switch', 'Сообщение вообще не связано с текущей командой'], correct: 1, why: 'Исторически вся эта логика жила в checkout; switch — более новый и узкий интерфейс поверх того же механизма, поэтому часть внутренних сообщений сохранила старое имя.' },
  { q: 'Чем git branch -D отличается от git branch -d?', options: ['Ничем, это два способа написать одну и ту же команду', '-D удаляет ветку без проверки, что все её коммиты уже есть в другой ветке; -d делает такую проверку и отказывается удалять, если рискует потерять единственную копию коммитов', '-D работает только с удалёнными ветками на GitHub', '-d удаляет ветку вместе с файлами проекта'], correct: 1, why: '-D — это -d --force: та же операция, но без предохранителя. Использовать заглавную стоит осознанно, когда точно понимаешь, что теряешь.' },
]} />
</Block>

<SectionAnchor chapterId="git-branches" sectionId="typical-errors" />

## Символы и команды главы

<Block kind="cheatsheet" title="Символы и команды главы" chapterId="git-branches" blockId="cheatsheet-commands" favPayload={{
  kind: 'table',
  head: ['Команда / символ', 'Что делает', 'Пример'],
  rows: [
    ['git branch', 'показывает список веток; текущая отмечена звёздочкой', 'git branch'],
    ['git branch -v', 'список веток вместе с последним коммитом каждой', 'git branch -v'],
    ['git switch <name>', 'переключает рабочую папку и HEAD на существующую ветку', 'git switch master'],
    ['git switch -c <name>', 'создаёт новую ветку и сразу переключается на неё', 'git switch -c fix-title'],
    ['git checkout -b <name>', 'старый эквивалент git switch -c', 'git checkout -b fix-title'],
    ['git merge <name>', 'вливает коммиты указанной ветки в текущую', 'git merge feature-timer'],
    ['Fast-forward', 'тип слияния без нового коммита: закладка ветки просто сдвигается вперёд', 'Fast-forward в выводе merge'],
    ["Merge made by the 'ort' strategy.", 'настоящее слияние: создан merge-коммит с двумя родителями', 'вывод git merge при разошедшихся ветках'],
    ['<<<<<<< ======= >>>>>>>', 'разметка конфликта: своя версия / чужая версия', 'внутри файла во время конфликта'],
    ['git status во время конфликта', 'показывает both modified и подсказки для решения', 'git status'],
    ['git merge --abort', 'отменяет незавершённое слияние, откатывает как до merge', 'git merge --abort'],
    ['git log --graph --all', 'рисует ASCII-дерево всех веток разом', 'git log --oneline --graph --all'],
    ['git branch -d <name>', 'безопасно удаляет уже влитую ветку', 'git branch -d fix-title'],
    ['git branch -D <name>', 'удаляет ветку без проверки, что она влита', 'git branch -D fix-title'],
    ['Already up to date.', 'сливать нечего — коммит уже есть в текущей ветке', 'вывод git merge'],
    ['HEAD', 'указатель на текущую ветку и её последний коммит', 'видно в <<<<<<< HEAD'],
  ],
}}>

| Команда / символ | Что делает | Пример |
|---|---|---|
| `git branch` | показывает список веток; текущая отмечена звёздочкой | `git branch` |
| `git branch -v` | список веток вместе с последним коммитом каждой | `git branch -v` |
| `git switch <name>` | переключает рабочую папку и HEAD на существующую ветку | `git switch master` |
| `git switch -c <name>` | создаёт новую ветку и сразу переключается на неё | `git switch -c fix-title` |
| `git checkout -b <name>` | старый эквивалент `git switch -c` | `git checkout -b fix-title` |
| `git merge <name>` | вливает коммиты указанной ветки в текущую | `git merge feature-timer` |
| Fast-forward | тип слияния без нового коммита: закладка ветки просто сдвигается вперёд | `Fast-forward` в выводе `merge` |
| `Merge made by the 'ort' strategy.` | настоящее слияние: создан merge-коммит с двумя родителями | вывод `git merge` при разошедшихся ветках |
| `<<<<<<<` `=======` `>>>>>>>` | разметка конфликта: своя версия / чужая версия | внутри файла во время конфликта |
| `git status` во время конфликта | показывает `both modified` и подсказки для решения | `git status` |
| `git merge --abort` | отменяет незавершённое слияние, откатывает как до `merge` | `git merge --abort` |
| `git log --graph --all` | рисует ASCII-дерево всех веток разом | `git log --oneline --graph --all` |
| `git branch -d <name>` | безопасно удаляет уже влитую ветку | `git branch -d fix-title` |
| `git branch -D <name>` | удаляет ветку без проверки, что она влита | `git branch -D fix-title` |
| `Already up to date.` | сливать нечего — коммит уже есть в текущей ветке | вывод `git merge` |
| HEAD | указатель на текущую ветку и её последний коммит | видно в `<<<<<<< HEAD` |

</Block>

<SectionAnchor chapterId="git-branches" sectionId="cheatsheet-symbols" />

## Слова главы

<Block kind="vocab" title="Словарь главы" chapterId="git-branches" blockId="vocab-glossary">
<Flashcards chapterId="git-branches" cards={[
  { term: 'branch', translation: 'ветка', note: 'параллельная линия истории; create a branch — создать ветку' },
  { term: 'merge', translation: 'слияние', note: 'влить изменения одной ветки в другую: git merge feature-timer' },
  { term: 'conflict', translation: 'конфликт', note: 'обе ветки изменили одни и те же строки — Git ждёт твоего решения' },
  { term: 'switch', translation: 'переключиться', note: 'git switch master — перейти на ветку master' },
  { term: 'fast-forward', translation: 'перемотка', note: 'слияние без нового коммита: закладка ветки просто сдвигается вперёд' },
  { term: 'merge commit', translation: 'коммит слияния', note: 'коммит с двумя родителями, который объединяет две линии истории' },
  { term: 'HEAD', translation: 'указатель на текущее место', note: 'на какой ветке и коммите ты сейчас стоишь; сдвигается при каждом switch' },
  { term: 'diverge', translation: 'разойтись', note: 'ветки разошлись — у обеих появились свои коммиты после общей точки, поэтому fast-forward уже невозможен' },
]} />
</Block>

<SectionAnchor chapterId="git-branches" sectionId="vocab" />

## Потренируйся печатать

<Block kind="trainer" title="Команды главы" chapterId="git-branches" blockId="trainer-git-commands">
<CodeTyping keyboard chapterId="git-branches" trainerId="trainer-git-commands" targetCpm={140} targetAccuracy={90} pools={[
  { label: 'Команды главы', snippets: [
    'git switch -c feature-timer',
    'git branch -v',
    'git log --oneline --graph --all',
    'git merge feature-timer',
    'git branch -d feature-timer',
    'git merge --abort',
  ] },
]} />
</Block>

Полный рабочий цикл слияния и уборки — набери его целиком одной строкой, как делают в реальной работе:

<Block kind="trainer" title="Полный цикл слияния" chapterId="git-branches" blockId="trainer-merge-cycle">
<CodeTyping snippet={'git switch master && git merge feature-timer && git branch -d feature-timer'} chapterId="git-branches" trainerId="trainer-merge-cycle" targetCpm={150} targetAccuracy={90} />
</Block>

<Block kind="cheatsheet" title="Тренажёр веток" chapterId="git-branches" blockId="cheatsheet-trainer-lgb">

<ToolCard
  name="Learn Git Branching"
  url="https://learngitbranching.js.org/?locale=ru_RU"
  desc="Визуальный тренажёр Git прямо в браузере: вводишь команды — дерево коммитов перестраивается на глазах, а не только текстом в терминале. Есть русский язык. Пройди уровни раздела «Introduction Sequence» — там как раз branch и merge, ровно то, что было в этой главе."
/>

</Block>

<details>
<summary>Углубиться</summary>

**Три снимка для одного слияния.** Настоящее слияние (не перемотку) Git строит не из двух версий файла, а из трёх: последнего коммита текущей ветки, последнего коммита вливаемой и их общего предка — коммита, где ветки когда-то разошлись (в примере с `feature-timer` это был `074d80c`). Это называется трёхсторонним слиянием (three-way merge). Строки, изменённые только с одной стороны относительно предка, Git берёт автоматически; конфликтом объявляет только те строки, что разошлись с обеих сторон по-разному. Вот почему конфликты на практике случаются заметно реже, чем кажется новичкам, — большая часть правок в реальном проекте просто не пересекается.

**У разметки конфликта можно попросить третий блок.** По умолчанию Git показывает только «твоя версия» и «чужая версия». Команда `git config merge.conflictStyle zdiff3` включает более подробный стиль — добавляет между ними ещё и то, как строка выглядела у общего предка. Проверено вживую на этой же самой строке из главы:

```text
<<<<<<< HEAD
print("Привет, чемпионы!")
||||||| 2124d9d
print("Привет, чемпионат!")
=======
print("Привет, финалисты!")
>>>>>>> feature-name-fix
print("Удачи!")
```

Средний блок между `|||||||` и `=======` — общий предок: то, с чего обе версии стартовали. Часто именно это подсказывает, чья правка «новее по смыслу», а не просто «чья была написана позже по хронологии».

**`git merge --abort` — кнопка «отменить всё».** Если посреди решения конфликта понял, что сливать было рано или не ту ветку, эта команда откатывает рабочую папку ровно к состоянию перед `git merge` — как будто слияния не начиналось вовсе. Работает, только пока конфликт не закоммичен: после `git commit` слияние уже завершено, и отменить его — отдельная тема (`git revert`, не в этой главе).

Дальше по теме:
- [git switch — официальная документация](https://git-scm.com/docs/git-switch) — все флаги команды переключения, включая работу с detached HEAD
- [git merge — официальная документация](https://git-scm.com/docs/git-merge) — раздел «How conflicts are presented» подробно разбирает разметку конфликта и стили `diff3`/`zdiff3`
- [git branch — официальная документация](https://git-scm.com/docs/git-branch) — полный список флагов, включая `-m` (переименовать) и `-a` (показать вместе с удалёнными)

</details>

<SectionAnchor chapterId="git-branches" sectionId="practice" />

## Куда дальше: проверенные ресурсы

<Block kind="cheatsheet" title="Русские материалы" chapterId="git-branches" blockId="cheatsheet-resources-ru">

- [Pro Git: Основы ветвления и слияния](https://git-scm.com/book/ru/v2/Ветвление-в-Git-Основы-ветвления-и-слияния) — официальный перевод главной книги про Git; та же тема, что в этой главе, с дополнительными картинками деревьев коммитов.
- [Хабр: раскладываем Git по полочкам — терминология](https://habr.com/ru/articles/796219/) — чёткие определения branch, merge и fast-forward с диаграммами, если после этой главы хочется увидеть те же понятия ещё раз другими словами.
- [Хабр: как решить конфликт в Git — merge, rebase, cherry-pick](https://habr.com/ru/companies/amvera/articles/1041676/) — конфликты не только при merge: та же логика возникает при `rebase` и `cherry-pick`, с которыми ты столкнёшься позже.
- [Atlassian: Git Merge](https://www.atlassian.com/ru/git/tutorials/using-branches/git-merge) — разбор fast-forward и трёхстороннего слияния с наглядными схемами веток на русском.

</Block>

<Block kind="cheatsheet" title="Официальная документация (на английском)" chapterId="git-branches" blockId="cheatsheet-resources-docs">

- [git-switch](https://git-scm.com/docs/git-switch) — man-страница команды переключения веток.
- [git-merge](https://git-scm.com/docs/git-merge) — man-страница слияния: стратегии, флаги, разбор конфликтов.
- [git-branch](https://git-scm.com/docs/git-branch) — man-страница управления ветками: создание, удаление, переименование.

</Block>

<Block kind="cheatsheet" title="Полезное" chapterId="git-branches" blockId="cheatsheet-resources-extra">

- [Шпаргалка по Git на dev-notes.ru](https://www.dev-notes.ru/articles/git/git-cheatsheet/) — все команды главы (и не только) на одной странице: удобно держать открытой во время практики или на чемпионате.

- [Видео и материалы сообщества — ниже](#community-sources) — ролики по теме и ссылки от студентов и преподавателей смотри в самом низу страницы.

</Block>

<SectionAnchor chapterId="git-branches" sectionId="resources" />

## Челлендж ⭐

Устрой конфликт своими руками в отдельном свежем репозитории — и победи его самостоятельно, не подглядывая в решение раньше времени.

1. Создай папку `team`, зайди в неё и сделай `git init`. Создай файл `team.txt` со строкой `Название команды: пока думаем` и закоммить его.
2. Создай ветку `new-name` и переключись на неё одной командой (`git switch -c`). В файле замени строку на `Название команды: Молния`. Закоммить.
3. Вернись на `master` (или `main` — какое имя досталось твоей ветке при `git init`, то и используй). Замени ту же строку на `Название команды: Огонь`. Закоммить.
4. Влей `new-name` в основную ветку. Git объявит конфликт — так и задумано.
5. Реши конфликт: оставь в файле то название, которое нравится больше, или придумай третье, — убери маркеры, заверши слияние.

Проверка: `git log --oneline --graph` показывает коммит слияния с двумя родителями, а `cat team.txt` — чистый файл без единого символа `<`, `=` или `>`.

**⭐ Ступень со звёздочкой.** Прежде чем выполнять `git branch -d new-name` в конце, ответь себе вслух, не заглядывая в терминал: сработает ли эта команда без ошибки и без флага `-D`, хотя ветка только что участвовала в конфликте? Сравни свой ответ с разделом «Типичные ответы Git», пункт про `not fully merged` — а затем проверь командой.

<details>
<summary>Решение (сначала попробуй сам)</summary>

```bash
mkdir team
cd team
git init
echo "Название команды: пока думаем" > team.txt
git add team.txt
git commit -m "начальное название"

git switch -c new-name
echo "Название команды: Молния" > team.txt
git commit -am "вариант Молния"

git switch master
echo "Название команды: Огонь" > team.txt
git commit -am "вариант Огонь"

git merge new-name
# Auto-merging team.txt
# CONFLICT (content): Merge conflict in team.txt
```

Открой `team.txt` в редакторе (например, `nano team.txt`) — внутри будет знакомая по главе разметка:

```text
<<<<<<< HEAD
Название команды: Огонь
=======
Название команды: Молния
>>>>>>> new-name
```

Оставь одну строку, например `Название команды: Огненная Молния`, удали три строки с маркерами, сохрани файл и заверши слияние:

```bash
git add team.txt
git commit -m "решили конфликт: объединили названия"
git log --oneline --graph
```

Ответ на ⭐: `git branch -d new-name` сработает без единой ошибки и без `-D`. Причина — ровно та же, что и с `feature-name-fix` в главе: после того как конфликт решён и `git commit` завершил слияние, коммит из `new-name` становится предком `master` через новый merge-коммит. Условие для `-d` — «коммиты ветки уже присутствуют где-то ещё» — выполнено, даже несмотря на то, что путь к этому был не прямым fast-forward, а конфликтным слиянием.

</details>

<SectionAnchor chapterId="git-branches" sectionId="challenge" />

<Block kind="quiz" title="Экзамен главы" chapterId="git-branches" blockId="exam">
<ChapterExam chapterId="git-branches" questions={[
  { q: 'Сразу после git switch -c feature-scoreboard в главе выполнили cat .git/refs/heads/feature-scoreboard и cat .git/refs/heads/master — оба файла показали один и тот же hash fe59fd2533f5042944c1211e96e7b96c92f5aa59. Почему они совпадают?', options: ['git switch -c скопировал файлы проекта в новую ветку', 'В этот момент обе ветки — просто две закладки, указывающие на один и тот же коммит; разойдутся они только когда в одной из веток появится новый коммит', 'Это ошибка вывода терминала, на самом деле хеши разные', 'master и feature-scoreboard на самом деле одна и та же ветка под двумя именами'], correct: 1, why: 'Ветка — лёгкий указатель на коммит. Создание ветки не трогает файлы проекта и не отделяет её от родителя сразу — закладки расходятся только с первым новым коммитом одной из них.' },
  { q: 'Команда git branch -v показала: * feature-scoreboard 074d80c add player count line / master fe59fd2 add gitignore for drafts. Что добавляет флаг -v по сравнению с обычным git branch?', options: ['-v ("verbose") добавляет к каждой ветке hash и сообщение её последнего коммита', '-v показывает только удалённые (remote) ветки', '-v — это сокращение от "version", показывает версию Git', '-v удаляет неиспользуемые ветки'], correct: 0, why: 'v — verbose: без флага git branch показывает только имена веток и текущую (со звёздочкой), а с -v ещё и коммит, на котором стоит каждая.' },
  { q: 'После git switch -c feature-scoreboard и двух коммитов в ней команда git log --oneline --graph --all всё равно нарисовала прямую линию без единой развилки, хотя в репозитории уже две ветки. Почему граф не разошёлся?', options: ['Флаг --all не сработал, и вторая ветка не попала в вывод', 'Разветвление на графике появляется только там, где у веток разные дальнейшие коммиты — а master пока стоит там же, где стояла в момент создания feature-scoreboard, взгляду не на чем расходиться', 'Это баг конкретно в Git 2.43.0', 'feature-scoreboard ещё не была создана на самом деле'], correct: 1, why: 'Граф рисует расхождение только когда есть разные коммиты на разных ветках. master не сдвинулась с места, пока шла работа в feature-scoreboard, поэтому визуально расходиться ещё не с чем.' },
  { q: 'В команде git commit -am "add timer line" использован флаг -a. Что он делает и на какие файлы не действует?', options: ['-a добавляет в коммит изменения уже отслеживаемых файлов без отдельного git add, но не действует на untracked-файлы', '-a добавляет вообще все файлы в папке, включая новые untracked', '-a — сокращение от --amend, переписывает предыдущий коммит', '-a автоматически пишет текст сообщения коммита'], correct: 0, why: 'Флаг -a ("all") — добавляет изменения уже известных Git файлов без отдельного git add, но, как и обычный git add, не подхватывает untracked-файлы.' },
  { q: 'В выводе fast-forward слияния main.py | 2 ++ и 1 file changed, 2 insertions(+). Что означают два знака + рядом с именем файла?', options: ['Каждый + — это отдельный слитый коммит', 'Это сокращённая шкала того, сколько строк добавилось в файл — здесь это ровно те же две строки, что указаны как insertions(+)', 'Знаки + означают конфликт, который нужно решить', 'Плюсы показывают, сколько веток участвовало в слиянии'], correct: 1, why: 'Диффстат рисует плюсики как масштабированную шкалу добавленных строк — в этом примере их два, ровно столько же, сколько insertions(+) в итоговой строке.' },
  { q: 'В выводе git log -1 у merge-коммита строка Author: Ivan Petrov <ivan@example.com>. Кого именно она обозначает?', options: ['Автора кода в обеих слитых ветках', 'Того, кто выполнил команду git merge, создавшую этот конкретный коммит слияния — не обязательно того, кто писал код в обеих ветках', 'Владельца репозитория на GitHub', 'Автора самого первого коммита в истории проекта'], correct: 1, why: 'Author у merge-коммита — тот, кто запустил git merge. Код в обеих ветках вполне мог писать кто-то другой — эти роли не обязаны совпадать.' },
  { q: 'Глава показывает: при включённом git config merge.conflictStyle zdiff3 между ||||||| и ======= появляется дополнительный блок с print("Привет, чемпионат!"). Что показывает этот блок, которого нет в обычной разметке конфликта?', options: ['Список всех веток репозитория', 'Как строка выглядела у общего предка обеих веток — до того, как они разошлись, что часто подсказывает, чья правка новее по смыслу', 'Синтаксическую ошибку в файле', 'Содержимое третьей, ещё не влитой ветки'], correct: 1, why: 'Стиль zdiff3 добавляет версию строки из общего предка (common ancestor) между HEAD и вливаемой веткой — контекст, которого в стандартной разметке нет.' },
  { q: 'В решении ⭐-задания про team.txt конфликт между master и new-name разрешили и закоммитили. Сработает ли после этого git branch -d new-name без ошибки и без флага -D?', options: ['Нет — конфликтные ветки Git никогда не считает полностью влитыми, нужен -D', 'Да — после merge-коммита коммит из new-name становится предком master, условие безопасного удаления -d выполнено, точно так же, как было с feature-name-fix ранее в главе', 'Да, но только если использовать git branch -D вместо -d', 'Это невозможно проверить, не запустив команду'], correct: 1, why: 'Итог для -d не зависит от того, был ли путь fast-forward или конфликтным слиянием — важно только, что коммиты ветки уже присутствуют в истории master, а после merge-коммита это так.' },
]} />
</Block>

## Что должен уметь

- Объяснять, что такое ветка (branch): лёгкий указатель на коммит, а не копия файлов проекта — и почему её создание мгновенно.
- Создавать ветку и переключаться между ветками через `git switch` / `git switch -c` — и узнавать ту же операцию в старой записи `git checkout -b`.
- Отличать fast-forward от настоящего merge-коммита по выводу `git merge`, объяснять, почему у первого нет нового коммита, а у второго — два родителя.
- Читать ASCII-граф `git log --graph --all`: находить точки расхождения и слияния веток.
- Проходить конфликт от начала до конца: понимать `CONFLICT` и `git status` во время конфликта, читать разметку `<<<<<<<`/`=======`/`>>>>>>>`, убирать маркеры, завершать через `git add` и `git commit`.
- Узнавать по точному тексту четыре типичные ситуации — `Already up to date`, `invalid reference`, заблокированное незакоммиченными правками переключение, отказ `git branch -d` удалить невлитую ветку — и знать, что делать в каждой, включая разницу между `-d` и `-D`.
- Убирать за собой уже влитые ветки командой `git branch -d`.

<SectionAnchor chapterId="git-branches" sectionId="what-you-should-know" />
