---
title: 'Linux и терминал'
audience: 'все'
level: 'база'
order: 3
---

import ChapterCover from '@site/src/components/ChapterCover';

<ChapterCover chapterId="linux-terminal" />

import SyntaxBreakdown from '@site/src/components/SyntaxBreakdown';
import CodeTyping from '@site/src/components/CodeTyping';
import Flashcards from '@site/src/components/Flashcards';
import ToolCard from '@site/src/components/ToolCard';
import SelfCheck from '@site/src/components/SelfCheck';
import ChapterExam from '@site/src/components/ChapterExam';
import Block from '@site/src/components/Block';
import Hint from '@site/src/components/Hint';
import CollapsibleToc from '@site/src/components/CollapsibleToc';
import ChapterProgress from '@site/src/components/ChapterProgress';
import SectionAnchor from '@site/src/components/SectionAnchor';
import KeysTabs from '@site/src/components/KeysTabs';
import TerminalSim from '@site/src/components/TerminalSim';
import FsTreeViz from '@site/src/components/FsTreeViz';
import Figure from '@site/src/components/Figure';
import MiniFileManager from '@site/src/components/MiniFileManager';

# Linux и терминал

<ChapterProgress chapterId="linux-terminal" totalSections={15} totalQuizzes={6} totalTrainers={5} />

## Зачем это нужно

На чемпионате «Профессионалы» рабочая машина — не Windows, а Ubuntu. Это самый популярный дистрибутив (distribution — сборка) Linux — операционной системы, на которой крутится почти весь интернет: серверы, облака, суперкомпьютеры, и даже Android внутри — это ядро Linux. Организаторы выбирают Linux не из вредности: он бесплатный, одинаковый у всех участников и в нём удобно автоматизировать проверку заданий. Так что чем раньше Ubuntu станет для тебя привычной, тем меньше сюрпризов будет на площадке.

Главный инструмент в Linux — терминал (terminal): окно, в котором ты общаешься с компьютером текстом. Выглядит как чёрный экран из фильмов про хакеров, но на деле это просто диалог: ты пишешь команду (command), нажимаешь Enter — компьютер отвечает. Никакой магии и ничего страшного: худшее, что случится у новичка, — компьютер вежливо скажет `command not found`. Зато многие вещи в терминале делаются в разы быстрее, чем мышкой, а некоторые — только в нём: например, запуск скриптов проверки на чемпионате.

В этой главе мы разберём десять команд, которых хватает на 90% повседневной работы в терминале, буквально по частям: что означает каждый флаг, что команда выведет в ответ и что она скажет, если ты ошибёшься. Терминал нельзя «нажать» мышкой в браузере, поэтому вместо статичных скриншотов здесь — интерактивные разборы, которые можно потрогать: кликай по частям команд, проходи самопроверки, тренируй набор руками.

<CollapsibleToc toc={toc} chapterId="linux-terminal" />

<SectionAnchor chapterId="linux-terminal" sectionId="why" />

import HistoryNote from '@site/src/components/HistoryNote';

<HistoryNote id="linux-1991">25 августа 1991 года 21-летний студент из Хельсинки Линус Торвальдс написал в новостную группу comp.os.minix, что делает свободную операционную систему — «просто хобби, ничего большого и профессионального». Из этого «хобби» вырос Linux: сегодня он работает на всех 500 мощнейших суперкомпьютерах мира, на большинстве серверов интернета и внутри каждого Android-смартфона — включая тот терминал Ubuntu, который ты сейчас изучаешь.</HistoryNote>

## Окна в Ubuntu за 1 минуту

Прежде чем открывать терминал, стоит на минуту освоиться с самим рабочим столом Ubuntu (окружение GNOME) — если раньше видел только Windows или macOS, тут почти всё управление держится на одной клавише:

- **Super** (клавиша с логотипом, между `Ctrl` и `Alt`) — открывает **Activities Overview**: обзор всех открытых окон сразу. Не отпуская мышь, начни просто печатать — заработает мгновенный поиск по приложениям, документам и контактам: нажал `Super`, напечатал «term» — и вот ярлык терминала, осталось нажать `Enter`.
- **Super+Tab** — переключение между открытыми окнами, `Shift` держит обратный порядок. Тот же принцип, что и `Alt+Tab` в Windows или `Cmd+Tab` в macOS (уже встречались в главе про печать) — просто другая клавиша-модификатор.
- **Super+←** / **Super+→** — прижимает текущее окно к левой или правой половине экрана (оконный снаппинг): удобно держать задание в браузере и терминал рядом, не переключаясь. То же самое можно сделать мышью — перетащить окно за заголовок к краю экрана, пока не подсветится половина.

Сам терминал в Ubuntu открывается двумя способами: сочетанием `Ctrl+Alt+T` (самый быстрый) или через уже знакомый Activities Overview — `Super`, напечатать «Terminal», `Enter`.

<Figure scheme="ubuntu-windows" caption="Super открывает обзор окон с мгновенным поиском; Super+← и Super+→ прижимают активное окно к половине экрана." />

<SectionAnchor chapterId="linux-terminal" sectionId="ubuntu-windows" />

## Терминал, оболочка и анатомия команды

Терминал и оболочка (shell) — не одно и то же, и путаница между ними мешает понимать, что вообще происходит. Терминал — это только окно, программа-обёртка, которая показывает текст и принимает нажатия клавиш. Настоящую работу внутри делает оболочка — отдельная программа, которая читает то, что ты напечатал, разбирает на команду и её части и решает, что с этим делать. В Ubuntu стандартная оболочка — bash (Bourne Again SHell). Поэтому «выучить терминал» на самом деле значит «выучить язык оболочки bash» — а сам терминал можно закрыть и открыть заново хоть двадцать раз, ничего не изменится.

Любая команда собирается из одних и тех же деталей: *что делать*, *как именно* и *над чем*. Нажми на каждую часть — под разбором появится подсказка.

<Block kind="breakdown" title="Разбор по частям: ls -la /home/student" chapterId="linux-terminal" blockId="breakdown-ls-anatomy">
<SyntaxBreakdown parts={[
  { text: 'ls ', label: 'команда', note: 'что делать: показать содержимое каталога (от английского list — «перечислить»)' },
  { text: '-la ', label: 'флаги', note: 'как именно: флаг -l — подробный список (права, размер, дата), флаг -a — показать и скрытые файлы; флаги можно склеивать: -la = -l -a' },
  { text: '/home/student', label: 'аргумент', note: 'над чем: путь (path) к каталогу; начинается с / — значит, отсчитывается от самого корня файловой системы' },
]} />
</Block>

Уточнения вроде `-l` и `-a` называются флагами (flag): они начинаются с дефиса и меняют поведение команды, но не обязательны — без них команда работает в базовом режиме. То, над чем команда работает (в примере выше — `/home/student`), называется аргументом (argument).

Путь (path) — это адрес файла или каталога (directory — то же самое, что папка, просто более «системное» слово, принятое в Linux и в текстах ошибок). `/home/student` читается так: в корне файловой системы (root — самая верхняя точка, у которой нет родителя) есть каталог `home`, в нём — `student`. Это домашний каталог (home directory) пользователя student — место, куда система пускает его без вопросов и где хранятся его личные файлы. У домашнего каталога есть короткое имя-псевдоним: `~` (тильда).

<Figure scheme="linux-fs-tree" caption="Дерево файловой системы: от корня / вниз к домашнему каталогу /home/student — у него есть короткое имя ~." />

Если забыл, как работает команда, у большинства из них есть встроенная подсказка — допиши `--help`: например, `ls --help` выведет список всех флагов прямо в терминале. Более полное описание хранится в man-странице (от manual — «руководство»): `man ls` открывает её на весь экран, листать — стрелками, выйти — клавишей `q`. Это первоисточник: любой урок в интернете, включая эту главу, пересказывает именно man-страницы.

<Block kind="quiz" title="Терминал и анатомия команды" chapterId="linux-terminal" blockId="quiz-anatomy">
<SelfCheck chapterId="linux-terminal" quizId="quiz-anatomy" questions={[
  { q: 'Чем терминал отличается от оболочки (shell)?', options: ['Это два названия одной и той же программы', 'Терминал — это окно, которое показывает текст; оболочка (например bash) — программа внутри, которая разбирает и выполняет команды', 'Терминал работает только в Linux, а оболочка — во всех системах', 'Оболочка — это то же самое, что операционная система'], correct: 1, why: 'Терминал — обёртка-окно. Настоящую работу с командами делает оболочка, в Ubuntu по умолчанию это bash.' },
  { q: 'На какие три части раскладывается команда ls -la /home/student?', options: ['Дата, время, имя пользователя', 'Команда (ls), флаги (-la), аргумент (/home/student)', 'Только команда и аргумент, флагов не существует', 'Источник, виновник, суть — как в сообщении об ошибке'], correct: 1, why: 'ls — что делать, -la — как именно (флаги), /home/student — над чем (аргумент).' },
  { q: 'Что означает символ ~ (тильда) в пути?', options: ['Корень файловой системы', 'Короткое имя домашнего каталога текущего пользователя', 'Родительский каталог', 'Ошибку в пути'], correct: 1, why: '~ — псевдоним для домашнего каталога (home directory), например /home/student.' },
  { q: 'Чем отличаются --help и man?', options: ['--help показывает краткую справку прямо в терминале, man открывает полное руководство на весь экран', 'Это одна и та же команда с разным написанием', '--help работает только в Windows', 'man показывает только примеры, а --help — только флаги'], correct: 0, why: '--help — быстрая шпаргалка по флагам конкретной команды. man ls — полное руководство, которое листают стрелками и закрывают клавишей q.' },
]} />
</Block>

<SectionAnchor chapterId="linux-terminal" sectionId="anatomy" />

## Навигация: pwd, ls, cd

Первые три команды нужны, чтобы понимать, где ты находишься, что вокруг и как туда попасть.

### `pwd` — где я нахожусь

`pwd` расшифровывается как print working directory — «напечатать рабочий каталог». Это самая простая команда в главе: без флагов, без обязательных аргументов, только один ответ — полный путь до того места, где сейчас стоит терминал.

<Block kind="breakdown" title="Разбор по частям: pwd" chapterId="linux-terminal" blockId="breakdown-pwd">
<SyntaxBreakdown parts={[
  { text: 'pwd', label: 'команда', note: 'без флагов и аргументов: сама по себе печатает абсолютный путь до текущего каталога' },
]} />
</Block>

Пример вывода в свежеоткрытом терминале:

```text
/home/student
```

**Типичная ловушка:** `pwd` — не про ошибку, а про невнимательность. Команда `pwd project` не ищет каталог `project` и не ругается на лишнее слово — она молча игнорирует всё, что ей передали, и печатает текущий путь как ни в чём не бывало. Многие Unix-утилиты именно так реагируют на аргументы, которые не умеют использовать: не падают, а просто их не замечают. Если результат `pwd` выглядит не так, как ты ожидал, — значит, ты правда не в той папке, где думал, и решает это не `pwd`, а следующая команда, `cd`.

### `ls` — что лежит рядом

`ls` (list) показывает содержимое каталога. Без аргументов — содержимое текущего каталога; с аргументом-путём — содержимое каталога по этому пути.

<Block kind="breakdown" title="Разбор по частям: ls" chapterId="linux-terminal" blockId="breakdown-ls">
<SyntaxBreakdown parts={[
  { text: 'ls', label: 'команда', note: 'без аргументов показывает содержимое текущего каталога' },
]} />
</Block>

Три флага меняют вид списка настолько, что стоит разобрать их по отдельности — вот реальный вывод для каталога `project` с файлом `README.md` и подкаталогами `docs` и `src`.

**Без флагов** — только имена, в несколько столбцов, ничего больше:

```text
README.md  docs  src
```

**`ls -l`** (long — «длинный формат») — по одной записи на строку: права доступа, число жёстких ссылок, владелец, группа, размер в байтах, дата изменения, имя:

```text
total 8
-rw-r--r-- 1 student student    0 Aug 31 18:35 README.md
drwxr-xr-x 2 student student 4096 Aug 31 18:35 docs
drwxr-xr-x 2 student student 4096 Aug 31 18:35 src
```

Первый символ строки — тип: `-` значит обычный файл, `d` — каталог (directory). Дальше идут девять символов прав доступа тремя тройками `rwx` (подробно про биты `rwx` будет отдельная глава про файлы и права) — пока достаточно заметить, что у каталогов почти всегда есть `x` в каждой тройке: без него нельзя *зайти* внутрь, даже если он виден.

**`ls -a`** (all — «все») — добавляет к обычному списку скрытые файлы: те, чьё имя начинается с точки, вроде `.bashrc`. Обычный `ls` их прячет, чтобы не мешались, но они никуда не делись:

```text
.  ..  README.md  docs  src
```

Здесь `.` — это сам текущий каталог, а `..` — его родитель: тоже скрытые «файлы», но особые, есть в любом каталоге всегда.

**`ls -la`** — комбинация: подробный формат плюс скрытые файлы, самый информативный вид:

```text
total 16
drwxr-xr-x 4 student student 4096 Aug 31 18:35 .
drwxr-x--- 3 student student 4096 Aug 31 18:35 ..
-rw-r--r-- 1 student student    0 Aug 31 18:35 README.md
drwxr-xr-x 2 student student 4096 Aug 31 18:35 docs
drwxr-xr-x 2 student student 4096 Aug 31 18:35 src
```

Дата, права и точный размер у тебя на экране будут свои — сравнивай структуру (какие строки и в каком порядке), а не конкретные цифры.

**Типичная ошибка** — попытка посмотреть каталог, которого не существует:

```text
ls: cannot access 'nope': No such file or directory
```

### `cd` — переместиться в другой каталог

`cd` (change directory) переключает текущий каталог. Путь можно указать четырьмя способами, и вперемешку они встречаются постоянно.

<Block kind="breakdown" title="Разбор по частям: cd ../docs" chapterId="linux-terminal" blockId="breakdown-cd">
<SyntaxBreakdown parts={[
  { text: 'cd ', label: 'команда', note: 'перейти в другой каталог (change directory); без аргумента — вернуться в домашний каталог' },
  { text: '..', label: 'родительский каталог', note: 'две точки — специальное обозначение каталога на уровень выше текущего' },
  { text: '/docs', label: 'дальше по относительному пути', note: 'от найденного родителя сразу зайти в его подкаталог docs; путь без ведущего / отсчитывается от текущего места, а не от корня' },
]} />
</Block>

<Figure scheme="lx-abs-rel-path" caption="Абсолютный путь всегда начинается от корня /; относительный — от того места, где ты сейчас (pwd)." />

Четыре формы пути для `cd`:

- **Абсолютный путь** — начинается с `/`, отсчитывается от корня и работает из любого места: `cd /home/student/project`.
- **Относительный путь** — без ведущего `/`, отсчитывается от текущего каталога: если ты в `project`, то `cd docs` заведёт в `project/docs`.
- **`cd ..`** — подняться на один уровень к родителю. `cd ../..` поднимает сразу на два уровня — точки можно продолжать через `/`.
- **`cd ~`** или просто **`cd`** без аргументов — вернуться в свой домашний каталог, откуда бы ты ни находился.

**Типичная ошибка** — опечатка в имени каталога или каталог ещё не создан:

```text
bash: cd: nope: No such file or directory
```

Все четыре формы пути удобно потрогать на живом дереве: кликай по узлам — и смотри, какой `cd` туда ведёт из текущего места.

<Block kind="trainer" title="Дерево файловой системы: куда ведёт cd" chapterId="linux-terminal" blockId="trainer-fstree">
<FsTreeViz chapterId="linux-terminal" trainerId="trainer-fstree" />
</Block>

<Block kind="quiz" title="Навигация: pwd, ls, cd" chapterId="linux-terminal" blockId="quiz-navigation">
<SelfCheck chapterId="linux-terminal" quizId="quiz-navigation" questions={[
  { q: 'Что покажет ls -a, чего не покажет ls без флагов?', options: ['Права доступа к файлам', 'Скрытые файлы — те, чьё имя начинается с точки, включая . и ..', 'Только каталоги, без файлов', 'Размер файлов в байтах'], correct: 1, why: '-a (all) добавляет скрытые файлы: обычный ls их не показывает, но они всегда есть.' },
  { q: 'Что означает первый символ - или d в начале строки вывода ls -l?', options: ['Права на удаление файла', 'Тип объекта: - обычный файл, d — каталог (directory)', 'Размер в байтах', 'Дату последнего изменения'], correct: 1, why: 'Первый символ строки ls -l — тип: тире для файла, d для каталога.' },
  { q: 'Чем cd docs отличается от cd /docs?', options: ['Ничем, это одно и то же', 'cd docs — относительный путь от текущего каталога; cd /docs — абсолютный путь от корня системы', 'cd /docs работает быстрее', 'cd docs работает только в домашнем каталоге'], correct: 1, why: 'Путь без ведущего / — относительный, отсчитывается от текущего места. Путь с / — абсолютный, всегда от корня.' },
  { q: 'Что сделает команда cd без аргументов вообще?', options: ['Покажет ошибку — аргумент обязателен', 'Вернёт в домашний каталог пользователя, как и cd ~', 'Поднимет на уровень вверх, как cd ..', 'Ничего не сделает и ничего не выведет'], correct: 1, why: 'cd без аргумента — то же самое, что cd ~: переход в домашний каталог.' },
]} />
</Block>

<Block kind="trainer" title="Настоящий терминал: pwd, ls, cd" chapterId="linux-terminal" blockId="trainer-terminal-free">
<TerminalSim initialFs={{
  project: {
    'README.md': 'Проект для чемпионата Профессионалы\n',
    docs: {},
    src: { 'main.py': '' },
  },
}} />
</Block>

<SectionAnchor chapterId="linux-terminal" sectionId="navigation" />

## Работа с файлами и каталогами

Пять команд, которые создают, копируют, перемещают и удаляют. С последней стоит быть особенно аккуратным — почему, разберём отдельно.

### `mkdir` — создать каталог

`mkdir` (make directory) создаёт новый пустой каталог. Обычный вызов создаёт только один уровень и требует, чтобы родитель уже существовал.

Флаг `-p` (parents) снимает оба ограничения разом — создаёт сразу цепочку вложенных каталогов, даже если промежуточных ещё нет:

<Block kind="breakdown" title="Разбор по частям: mkdir -p app/assets/images" chapterId="linux-terminal" blockId="breakdown-mkdir">
<SyntaxBreakdown parts={[
  { text: 'mkdir ', label: 'команда', note: 'создать каталог (make directory)' },
  { text: '-p ', label: 'флаг', note: 'parents — создать все промежуточные каталоги по пути, если их ещё нет, и не выдавать ошибку, если конечный каталог уже существует' },
  { text: 'app/assets/images', label: 'аргумент', note: 'путь сразу на три уровня вложенности: app, затем assets внутри него, затем images внутри assets' },
]} />
</Block>

После `mkdir -p app/assets/images` появляются сразу все три уровня:

```text
app:
assets

app/assets:
images

app/assets/images:
```

**Типичная ошибка** — попытка создать вложенный каталог без `-p`, когда промежуточных ещё нет:

```text
mkdir: cannot create directory 'app/assets/images': No such file or directory
```

### `touch` — создать пустой файл

`touch` создаёт пустой файл (file), если его ещё нет, а если он уже есть — просто обновляет дату последнего изменения, не трогая содержимое. Аргументов можно передать сколько угодно через пробел — команда обработает каждый по очереди.

<Block kind="breakdown" title="Разбор по частям: touch с тремя файлами" chapterId="linux-terminal" blockId="breakdown-touch">
<SyntaxBreakdown parts={[
  { text: 'touch ', label: 'команда', note: 'создать пустой файл или обновить дату изменения у существующего' },
  { text: 'README.md ', label: 'первый аргумент', note: 'файл в текущем каталоге' },
  { text: 'src/main.py ', label: 'второй аргумент', note: 'путь src/main.py — файл main.py внутри уже существующего подкаталога src' },
  { text: 'docs/notes.md', label: 'третий аргумент', note: 'ещё один файл в другом подкаталоге; touch принимает сколько угодно аргументов через пробел и обрабатывает каждый по очереди' },
]} />
</Block>

<Hint id="linux-terminal-h1" type="tip">
Команда ничего не выводит, если всё получилось, — молчание в терминале почти всегда значит «успех», это общее правило для многих Unix-утилит.
</Hint>

**Типичная ошибка** — как и у `mkdir`, `touch` не создаёт промежуточные каталоги сама:

```text
touch: cannot touch 'nope/file.txt': No such file or directory
```

### `cp` — скопировать

`cp` (copy) копирует файл или каталог. Обычная форма — `cp источник назначение` — работает только с файлами; каталоги требуют флага `-r`.

<Block kind="breakdown" title="Разбор по частям: cp -r src src_backup" chapterId="linux-terminal" blockId="breakdown-cp">
<SyntaxBreakdown parts={[
  { text: 'cp ', label: 'команда', note: 'скопировать (copy)' },
  { text: '-r ', label: 'флаг', note: 'recursive — скопировать каталог вместе со всем содержимым; без него cp отказывается копировать каталоги вообще' },
  { text: 'src ', label: 'источник', note: 'что копируем' },
  { text: 'src_backup', label: 'назначение', note: 'куда копируем — имя новой копии' },
]} />
</Block>

После `cp -r src src_backup` внутри `src_backup` оказывается точная копия содержимого `src`:

```text
total 8
drwxr-xr-x 2 student student 4096 Aug 31 18:36 .
drwxr-x--- 3 student student 4096 Aug 31 18:36 ..
-rw-r--r-- 1 student student    0 Aug 31 18:36 main.py
```

**Типичная ошибка** — забыли `-r` при копировании каталога:

```text
cp: -r not specified; omitting directory 'src'
```

### `mv` — переместить или переименовать

`mv` (move) не различает «переместить» и «переименовать» — для файловой системы это одно и то же действие: изменить запись о том, где лежит файл и как называется. `mv старое_имя новое_имя` в той же папке — переименование. `mv файл другая_папка/` — перемещение.

Пример: `mv docs/notes.md docs/README.md` переименовывает файл прямо внутри `docs`, а `mv docs/README.md .` — переносит тот же файл из `docs` в текущий каталог (`.` — обозначение «сюда»). А вот `mv README.md .` для файла, который уже лежит в текущем каталоге, ничего не переместит: источник и назначение указывают на один и тот же файл, и `mv` откажется, сообщив об этом ошибкой.

**Типичная ошибка** — источник не существует:

```text
mv: cannot stat 'ghost.txt': No such file or directory
```

### `rm` — удалить (и почему это по-настоящему опасно)

`rm` (remove) удаляет файлы. Обычный вызов работает только с файлами; для каталогов, как и у `cp`, нужен флаг `-r` (recursive) — удалить каталог вместе со всем содержимым, рекурсивно, то есть спускаясь во все вложенные подкаталоги.

**Типичная ошибка** — забыли `-r` при удалении каталога:

```text
rm: cannot remove 'app_copy': Is a directory
```

<Hint id="linux-terminal-h2" type="important">
А вот с чем нужно быть по-настоящему осторожным: **в терминале нет корзины**. То, что удалила `rm`, не отправляется во «Недавно удалённые» — оно стирается сразу, восстановить штатными средствами нельзя. Никакого диалога «вы уверены?» тоже не будет: команда молча делает ровно то, что написано.
</Hint>

<Figure scheme="lx-rm-no-trash" caption="Удаление в проводнике попадает в корзину — можно вернуть. rm удаляет сразу и безвозвратно." />

Самый известный способ прострелить себе ногу — `rm -rf /`: удалить рекурсивно (`-r`) и без вопросов (`f` — force) от самого корня файловой системы. Это буквально «сотри всё, что вообще есть в системе». К счастью, у современных версий `rm` встроена защита именно от этого конкретного случая — попробуй прочитать реальный ответ:

<Block kind="breakdown" title="Разбор по словам: защита rm от '/'" chapterId="linux-terminal" blockId="breakdown-rm-failsafe">
<SyntaxBreakdown parts={[
  { text: 'rm', label: 'источник', note: 'программа, которая печатает сообщение' },
  { text: ': ', label: 'разделитель', note: 'двоеточие перед сутью проблемы' },
  { text: 'it is dangerous ', label: '«это опасно»', note: 'прямое предупреждение — необычно для rm: обычно она молчит и просто выполняет команду, а тут сама остановилась' },
  { text: 'to operate ', label: '«оперировать, работать»', note: 'глагол to operate — про само действие команды' },
  { text: 'recursively ', label: '«рекурсивно»', note: 'наречие от recursive — то самое поведение, которое включает флаг -r: спуститься во все вложенные каталоги' },
  { text: 'on ', label: '«на»', note: 'предлог перед объектом действия' },
  { text: "'/'", label: 'корень файловой системы', note: 'в одинарных кавычках — сам аргумент команды, ради которого сработала защита' },
]} />
</Block>

Полностью: `rm: it is dangerous to operate recursively on '/'`, за ней вторая строка — `rm: use --no-preserve-root to override this failsafe` («используй --no-preserve-root, чтобы отключить эту защиту»). Именно поэтому фраза заканчивается словом failsafe — «страховка на случай отказа». Дословный перевод: «rm: опасно рекурсивно оперировать над '/'».

Важно понимать границы этой страховки — она спасает только от одного, самого буквального случая: аргумента, который в точности равен `/`. Она не спасёт от `rm -rf ~` (снесёт весь домашний каталог), от `rm -rf .` в неправильной папке или от команды внутри чужого скрипта, где вместо ожидаемого пути неожиданно оказалась пустая переменная — тогда `rm -rf "$papka/$podpapka"` при пустых `$papka` и `$podpapka` превращается ровно в `rm -rf /`, но уже без кавычек-подсказки и без единой пустой переменной в поле зрения. Именно так в 2016 году владелец одного хостинга случайно уничтожил данные полутора тысяч серверов одной автоматизированной командой — реальная история есть в разделе «Куда дальше» этой главы. Отсюда практическое правило: перед `rm -r`, а тем более перед `rm -rf`, дважды проверь, что стоит после неё — особенно если команда собрана из переменных или скопирована из интернета, а не написана своими руками.

<Block kind="quiz" title="Файлы и каталоги" chapterId="linux-terminal" blockId="quiz-files-dirs">
<SelfCheck chapterId="linux-terminal" quizId="quiz-files-dirs" questions={[
  { q: 'Что нужно добавить к mkdir, чтобы создать сразу три вложенных уровня: app/assets/images?', options: ['Ничего, mkdir и так создаёт все уровни', 'Флаг -p (parents)', 'Флаг -r (recursive)', 'Три отдельных запуска команды cp'], correct: 1, why: '-p создаёт все промежуточные каталоги по пути, если их ещё нет, и не ругается на уже существующий конечный каталог.' },
  { q: 'Чем rm отличается от cp и mv в вопросе флага -r?', options: ['У rm вообще нет флага -r', 'У всех трёх -r означает одно и то же: применить действие рекурсивно ко всему содержимому каталога', 'У rm -r означает «спросить подтверждение», а у cp — «скопировать рекурсивно»', 'rm -r работает только с файлами, не с каталогами'], correct: 1, why: '-r (recursive) у cp, mv (у mv флаг не обязателен для переименования, но нужен для каталогов в некоторых сценариях) и rm означает одно: спуститься во всё вложенное содержимое.' },
  { q: 'От чего именно защищает встроенная страховка rm при аргументе /, а от чего — нет?', options: ['Защищает от любого удаления важных файлов вообще', 'Защищает только от буквального аргумента / целиком; не защищает от rm -rf ~, rm -rf . в чужой папке или от пустых переменных в скрипте', 'Защищает только если команда запущена без sudo', 'Такой защиты у rm не существует вовсе'], correct: 1, why: 'Страховка срабатывает на точное совпадение аргумента с /. Любой другой опасный путь — включая случайно опустевшую переменную в скрипте — она не ловит.' },
  { q: 'Почему rm не спрашивает «вы уверены?» перед удалением, в отличие от корзины в графическом интерфейсе?', options: ['Это баг, который скоро починят', 'В терминале нет корзины: rm сразу и молча делает то, что написано в команде, без подтверждений', 'rm спрашивает подтверждение, просто окно не всегда видно', 'Подтверждение появляется только для каталогов'], correct: 1, why: 'У терминальных команд по умолчанию нет диалогов — они выполняют ровно то, что напечатано, поэтому проверять нужно до Enter, а не после.' },
]} />
</Block>

<SectionAnchor chapterId="linux-terminal" sectionId="files-dirs" />

## От проводника к терминалу: GUI и командная строка — один и тот же смысл

Если файлами до сих пор управлял мышкой — в проводнике Windows, Finder на macOS или Files (Nautilus) в GNOME/Ubuntu, — хорошая новость: команды `mv`, `cp` и `mkdir` не появились из ниоткуда. Это буквально то, что происходит «под капотом», когда ты переименовываешь, копируешь или перетаскиваешь файл мышью. Вот прямой мостик — проверено по официальной документации GNOME Files:

<Block kind="cheatsheet" title="Таблица-мостик: GUI-действие → команда терминала" chapterId="linux-terminal" blockId="cheatsheet-gui-cli-bridge" favPayload={{
  kind: 'table',
  head: ['Действие мышью в GNOME Files', 'Что происходит под капотом'],
  rows: [
    ['F2 или ПКМ → «Переименовать»', 'mv старое_имя новое_имя — переименование и есть перемещение в той же папке'],
    ['Ctrl+C, затем Ctrl+V', 'cp — копия остаётся и в старом, и в новом месте'],
    ['Ctrl+X, затем Ctrl+V', 'mv — оригинал переезжает, копии не остаётся'],
    ['Перетащить (drag) на том же диске', 'mv — файловый менеджер молча перемещает'],
    ['Перетащить (drag) на другой диск/флешку', 'cp — файловый менеджер молча копирует'],
    ['Ctrl+перетащить', 'принудительно cp, даже на том же диске'],
    ['Shift+перетащить', 'принудительно mv, даже на другой диск'],
    ['Ctrl+Shift+N (неофициально)', 'mkdir «Новая папка» — устойчиво воспроизводится в GNOME Files, но в официальной документации это сочетание не встретилось: не полагайся на него на 100%'],
  ],
}}>

| Действие мышью в GNOME Files | Что происходит под капотом |
|---|---|
| F2 или ПКМ → «Переименовать» | `mv старое_имя новое_имя` — переименование и есть перемещение в той же папке |
| Ctrl+C, затем Ctrl+V | `cp` — копия остаётся и в старом, и в новом месте |
| Ctrl+X, затем Ctrl+V | `mv` — оригинал переезжает, копии не остаётся |
| Перетащить (drag) на том же диске | `mv` — файловый менеджер молча перемещает |
| Перетащить (drag) на другой диск/флешку | `cp` — файловый менеджер молча копирует |
| Ctrl+перетащить | принудительно `cp`, даже на том же диске |
| Shift+перетащить | принудительно `mv`, даже на другой диск |
| Ctrl+Shift+N *(неофициально)* | `mkdir` «Новая папка» — устойчиво воспроизводится в GNOME Files, но в официальной документации это сочетание не встретилось: не полагайся на него на 100% |

</Block>

Самое неочевидное здесь — перетаскивание: GNOME Files решает `mv` это или `cp`, глядя не на твоё намерение, а буквально на то, один это диск или разные (официальная формулировка: «move it if the destination is on the same device, or copy it if the destination is on a different device»). Поэтому перетаскивание файла с флешки на диск копирует, а между двумя папками на одном диске — переносит. Модификаторы `Ctrl`/`Shift` при перетаскивании силой переключают это поведение в обратную сторону.

<Hint id="linux-hint-rename-is-mv" type="fact">
Переименование файла и перемещение файла в файловых системах Linux — операция одна и та же: системный вызов `rename()`. `mv report.txt summary.txt` и клик «Переименовать» → `summary.txt` физически делают одно и то же — меняют запись в каталоге, не трогая содержимое файла на диске. Именно поэтому переименование гигабайтного видео происходит мгновенно, а копирование того же файла — нет.
</Hint>

Потренируй те же самые действия — переименование, перемещение, копирование — в игрушечном файловом менеджере: перетаскивай мышью, выделяй Ctrl/Shift+кликом несколько файлов сразу, жми правой кнопкой. Журнал внизу покажет, какая команда терминала спряталась за каждым твоим действием.

<Block kind="trainer" title="Мини-файловый менеджер" chapterId="linux-terminal" blockId="trainer-file-manager">
<MiniFileManager chapterId="linux-terminal" trainerId="trainer-file-manager" />
</Block>

<SectionAnchor chapterId="linux-terminal" sectionId="gui-cli-bridge" />

## Просмотр содержимого: `cat` и `less`

Две команды показывают, что внутри текстового файла, но по-разному годятся для разных размеров.

### `cat` — вывести файл целиком

`cat` (concatenate — «объединить, сцепить»; так же называется вся команда) печатает содержимое файла прямо в терминал, от начала до конца, без остановок:

```text
$ cat README.md
Проект для чемпионата Профессионалы
```

Если передать несколько файлов сразу — `cat a.txt b.txt` — они выведутся один за другим подряд, будто склеенные в один поток текста: отсюда и название команды.

**Типичная ошибка** — попытка вывести не файл, а целый каталог:

```text
cat: src: Is a directory
```

`cat` умеет читать только файлы; у каталога нет «содержимого» в этом смысле — посмотреть, что внутри каталога, можно только через `ls`.

### `less` — листать длинный файл постранично

У `cat` есть слабое место: файл на тысячу строк вывалится в терминал целиком, и первые сотни строк улетят за верхний край экрана. `less` решает это иначе — показывает файл по одному экрану за раз, а листать можно клавишами:

- `Space` (пробел) или `f` — на страницу вперёд;
- `b` — на страницу назад;
- стрелки ↑ / ↓ — на строку;
- `/слово` — искать слово по тексту, `n` — перейти к следующему совпадению;
- `q` (quit) — выйти и вернуться в обычный терминал.

<Block kind="breakdown" title="Разбор по частям: less numbers.txt" chapterId="linux-terminal" blockId="breakdown-less">
<SyntaxBreakdown parts={[
  { text: 'less ', label: 'команда', note: 'открыть файл постраничным просмотрщиком (pager)' },
  { text: 'numbers.txt', label: 'аргумент', note: 'путь к файлу, который нужно пролистать' },
]} />
</Block>

Открытая в браузере страница не может по-настоящему запустить `less` — он захватывает весь терминал целиком и живёт, пока ты не нажмёшь `q`, поэтому здесь его не потрогать интерактивно. Компенсируем это самопроверкой ниже и практикой на настоящей Ubuntu.

**Типичная ошибка** — файла не существует:

```text
ghost.txt: No such file or directory
```

Обрати внимание: тут нет привычной приставки `less:` перед текстом — это особенность конкретно этой команды, сообщение выводится прямо в статусной строке внизу экрана, без имени программы впереди.

<Hint id="linux-terminal-h3" type="fact">
Кстати, у названия `less` есть история: пейджер (pager — программа для постраничного просмотра) с похожим названием `more` появился раньше и умел листать только вперёд. Новую версию, которая умеет ещё и назад, шутливо назвали `less` — обыгрывая английскую поговорку «less is more» («меньше значит больше»). По иронии `less` умеет строго больше, чем `more`.
</Hint>

<Block kind="quiz" title="cat и less" chapterId="linux-terminal" blockId="quiz-cat-less">
<SelfCheck chapterId="linux-terminal" quizId="quiz-cat-less" questions={[
  { q: 'Почему команда cat так называется?', options: ['От слова «кот» — случайное совпадение', 'От concatenate — «сцепить»: несколько файлов, переданных ей, выводятся подряд, как один склеенный текст', 'Cat — это акроним от Console Access Tool', 'Название не имеет отношения к её работе'], correct: 1, why: 'cat умеет выводить сразу несколько файлов один за другим — отсюда и «сцепить, объединить».' },
  { q: 'В чём главное неудобство cat на большом файле и как его решает less?', options: ['cat вообще не открывает большие файлы; less их сжимает', 'cat выводит всё сразу, и начало файла улетает за край экрана; less показывает содержимое по одному экрану, с листанием', 'Разницы никакой, less — просто устаревший cat', 'less работает только с файлами меньше килобайта'], correct: 1, why: 'less — постраничный просмотрщик (pager): показывает ровно один экран текста и ждёт команды листать дальше.' },
  { q: 'Какой клавишей выходят из less обратно в терминал?', options: ['Ctrl+C', 'q', 'Esc', 'Enter'], correct: 1, why: 'q (quit) — стандартный выход почти из всех терминальных пейджеров и текстовых интерфейсов, включая less и man.' },
  { q: 'Что сделает cat src, если src — это каталог, а не файл?', options: ['Выведет список файлов внутри, как ls', 'Откажется и напечатает cat: src: Is a directory', 'Ничего не сделает и не выведет', 'Удалит каталог'], correct: 1, why: 'cat умеет читать только файлы. У каталога нет текстового «содержимого» для вывода — за это отвечает ls.' },
]} />
</Block>

<SectionAnchor chapterId="linux-terminal" sectionId="view-files" />

## Практикум: собери и разбери проект за 10 шагов

Открой терминал в Ubuntu (Ctrl+Alt+T) и пройди весь путь только командами, без единого клика мышью. Каждый шаг — новая команда из этой главы.

<KeysTabs items={[
  { combo: { mac: '⌘Пробел, затем «Terminal»', win: 'Win, затем «Terminal»', linux: 'Ctrl+Alt+T' }, action: 'Открыть терминал' },
]} />

**Шаг 1.** Узнай, где ты сейчас:

```bash
pwd
```

**Шаг 2.** Создай сразу два вложенных каталога одной командой:

```bash
mkdir -p project/src project/docs
```

**Шаг 3.** Перейди внутрь и проверь, что оказался там, где нужно:

```bash
cd project
pwd
```

**Шаг 4.** Создай три файла одной командой — по одному в разных местах:

```bash
touch README.md src/main.py docs/notes.md
```

**Шаг 5.** Посмотри, что получилось, в подробном формате со скрытыми файлами:

```bash
ls -la
```

**Шаг 6.** Сделай резервную копию каталога `src` целиком:

```bash
cp -r src src_backup
```

**Шаг 7.** Переименуй `notes.md` внутри `docs` в `README.md`:

```bash
mv docs/notes.md docs/README.md
```

**Шаг 8.** Запиши в главный `README.md` одну строку и прочитай её обратно (`>` перенаправляет вывод команды `echo` в файл вместо экрана — подробно про перенаправление будет отдельная глава про файлы):

```bash
echo "Проект для чемпионата Профессионалы" > README.md
cat README.md
```

**Шаг 9.** Резервная копия больше не нужна — удали её вместе с содержимым:

```bash
rm -r src_backup
```

**Шаг 10.** Финальная проверка — структура должна остаться чистой: `README.md`, `docs/`, `src/`, без `src_backup`:

```bash
ls -la
```

Несколько команд можно склеить в одну строку через `&&`: вторая выполнится, только если первая завершилась без ошибки. Например, шаги 6 и 10 можно было бы записать как `cp -r src src_backup && ls -la` — если копирование провалится, лишний `ls` просто не запустится.

<Block kind="trainer" title="Квест: построй дерево проекта" chapterId="linux-terminal" blockId="trainer-terminal-quest">
<TerminalSim
  quest={{
    title: 'Построй дерево проекта',
    requiredPaths: ['project/README.md', 'project/src/main.py', 'project/docs/README.md'],
    forbiddenPaths: ['project/src_backup'],
  }}
  chapterId="linux-terminal"
  trainerId="trainer-terminal-quest"
/>
</Block>

<SectionAnchor chapterId="linux-terminal" sectionId="practice-project" />

## Типичные ошибки

Шесть ситуаций, с которыми терминал столкнёт тебя гарантированно в первую же неделю. Каждая — с воспроизводящим примером, точным текстом (проверено вживую), переводом, причиной и починкой.

### 1. Каталог уже существует

Попытка создать каталог, который уже есть:

```bash
mkdir src
```

```text
mkdir: cannot create directory 'src': File exists
```

**Перевод:** «mkdir: невозможно создать каталог src: уже существует».

**Причина:** каталог с этим именем в этом месте уже создан — возможно, ещё в прошлой сессии, а ты забыл.

**Как починить:** если каталог и правда нужен таким, как есть, — просто ничего не делай, он уже есть. Если хотелось создать его и не упасть с ошибкой, даже если он существует, — используй `mkdir -p src`: с этим флагом повторное создание не считается ошибкой.

### 2. Забыли `-r` при копировании каталога

```bash
cp src app_copy
```

```text
cp: -r not specified; omitting directory 'src'
```

**Перевод:** «cp: -r не указан; пропускаю каталог src».

**Причина:** `cp` без `-r` копирует только файлы. Наткнувшись на каталог, она не падает с фатальной ошибкой, а просто пропускает его и продолжила бы работу с остальными аргументами, если бы они были.

**Как починить:** добавить флаг — `cp -r src app_copy`.

### 3. Забыли `-r` при удалении каталога

```bash
rm app_copy
```

```text
rm: cannot remove 'app_copy': Is a directory
```

**Перевод:** «rm: невозможно удалить app_copy: это каталог».

**Причина:** обычный `rm` умеет удалять только файлы — это осознанное ограничение безопасности, чтобы случайно набранный `rm` без раздумий не снёс папку со всем содержимым.

**Как починить:** добавить флаг — `rm -r app_copy`, и только убедившись, что внутри действительно ничего ценного.

### 4. Опечатка в имени каталога у `cd`

```bash
cd nope
```

```text
bash: cd: nope: No such file or directory
```

**Перевод:** «bash: cd: nope: нет такого файла или каталога».

**Причина:** каталога с именем `nope` не существует в текущем месте — либо опечатка, либо он ещё не создан, либо ты не в той родительской папке, откуда думал.

**Как починить:** проверить точное имя через `ls`, проверить, в какой ты папке, через `pwd`, и при необходимости создать каталог через `mkdir` перед тем, как в него заходить.

### 5. `cat` на каталоге вместо файла

```bash
cat src
```

```text
cat: src: Is a directory
```

**Перевод:** «cat: src: это каталог».

**Причина:** `src` — не файл, а каталог; `cat` умеет читать только содержимое файлов.

**Как починить:** если нужно посмотреть, что внутри каталога, — используй `ls src` вместо `cat src`.

### 6. `Ctrl+C` в терминале не копирует — он прерывает программу

Самая частая путаница у новичков: во всех остальных программах `Ctrl+C` копирует выделенное (см. главу про печать). В терминале — нет. Запусти что-нибудь долгое:

```bash
ping 127.0.0.1
```

```text
PING 127.0.0.1 (127.0.0.1) 56(84) bytes of data.
64 bytes from 127.0.0.1: icmp_seq=1 ttl=64 time=0.653 ms
64 bytes from 127.0.0.1: icmp_seq=2 ttl=64 time=0.109 ms
^C
--- 127.0.0.1 ping statistics ---
8 packets transmitted, 8 received, 0% packet loss, time 7154ms
```

Нажми `Ctrl+C` — на экране появится `^C` (терминал так показывает нажатие), команда `ping` остановится и допечатает итоговую статистику. Ничего не скопировалось: **`Ctrl+C` в терминале означает «прервать выполняющуюся программу», а не «скопировать»**.

**Причина — на уровне ядра, не конкретной команды:** в терминальном драйвере (termios) клавиша `Ctrl+C` жёстко закреплена за символом `INTR` (interrupt). Получив его, ядро операционной системы посылает сигнал `SIGINT` всей группе процессов переднего плана — той самой программе, что сейчас читает твой ввод. Это не решение автора `ping` или `bash`, а поведение самого терминального устройства, одинаковое для абсолютно любой команды в терминале:

> VINTR … Interrupt character (INTR). Send a SIGINT signal. — man7.org, termios(3)

<Figure scheme="lx-ctrlc-sigint" caption="Ctrl+C — не буфер обмена: клавиша посылает сигнал SIGINT работающей программе, и та останавливается." />

**Как копировать и вставлять текст в самом терминале — по системам:**

<Block kind="cheatsheet" title="Копировать/вставить в терминале — по ОС" chapterId="linux-terminal" blockId="cheatsheet-terminal-copy" favPayload={{
  kind: 'table',
  head: ['ОС / терминал', 'Копировать', 'Вставить', 'Что делает голый Ctrl+C'],
  rows: [
    ['Ubuntu, GNOME Terminal', 'Ctrl+Shift+C', 'Ctrl+Shift+V', 'Всегда SIGINT — прерывает программу, копировать им нельзя вообще'],
    ['Windows Terminal', 'Ctrl+C — «умный»: копирует, если есть выделение', 'Ctrl+V', 'Если выделения нет — ведёт себя как обычный SIGINT'],
    ['macOS Terminal.app / iTerm2', 'Cmd+C', 'Cmd+V', 'Ctrl+C свой, отдельный от Cmd+C — всегда SIGINT'],
  ],
}}>

| ОС / терминал | Копировать | Вставить | Что делает голый Ctrl+C |
|---|---|---|---|
| Ubuntu, GNOME Terminal | `Ctrl+Shift+C` | `Ctrl+Shift+V` | Всегда `SIGINT` — прерывает программу, копировать им нельзя вообще |
| Windows Terminal | `Ctrl+C` — «умный»: копирует, если есть выделение | `Ctrl+V` | Если выделения нет — ведёт себя как обычный `SIGINT` |
| macOS Terminal.app / iTerm2 | `Cmd+C` | `Cmd+V` | `Ctrl+C` свой, отдельный от `Cmd+C` — всегда `SIGINT` |

</Block>

Windows Terminal — единственный из трёх, где `Ctrl+C` иногда всё-таки копирует: по умолчанию за ним закреплено действие `copy`, которое копирует выделенный текст, если он есть, а если выделения нет — пропускает нажатие дальше, в саму программу, как обычный `SIGINT` (официально: learn.microsoft.com/windows/terminal/customize-settings/actions, действие `copy`). До того как это стало поведением по умолчанию, авторы Windows Terminal отдельно обсуждали именно эту путаницу — issue [microsoft/terminal#2285](https://github.com/microsoft/terminal/issues/2285) «Ctrl C, when text is highlighted, should copy not cancel». GNOME Terminal на Ubuntu пошёл другим путём — официальная документация прямо говорит: «The standard keyboard shortcuts, such as Ctrl+C, cannot be used to copy and paste text» — обычный `Ctrl+C` там не копирует вообще никогда, только `Ctrl+Shift+C`.

**Как починить (точнее — как не пугаться):** если случайно прервал что-то нужное — просто запусти команду заново. Если хотел скопировать вывод терминала — выдели его мышью (тройной клик по строке или протяжка, см. главу про печать) и используй сочетание из таблицы выше для своей системы.

<Block kind="quiz" title="Типичные ошибки" chapterId="linux-terminal" blockId="quiz-typical-errors">
<SelfCheck chapterId="linux-terminal" quizId="quiz-typical-errors" questions={[
  { q: 'Чем отличается поведение cp и rm без флага -r на каталоге?', options: ['Ничем — обе команды выполняют операцию как обычно', 'cp пропускает каталог с предупреждением и продолжает, rm вообще отказывается что-либо удалить и останавливается с ошибкой', 'cp удаляет каталог, а rm его копирует', 'Обе команды падают с одинаковой ошибкой Is a directory'], correct: 1, why: 'cp: -r not specified; omitting directory — пропускает и идёт дальше. rm: cannot remove ... Is a directory — останавливается полностью.' },
  { q: 'mkdir src выдал File exists. Что это означает и что делать?', options: ['Каталог src повреждён, нужно удалить и создать заново', 'Каталог src уже существует; если это нормально — ничего делать не нужно, либо использовать mkdir -p, чтобы не считать это ошибкой', 'Не хватает прав доступа', 'Диск переполнен'], correct: 1, why: 'File exists — просто констатация факта: каталог с таким именем уже есть. mkdir -p не ругается на этот случай.' },
  { q: 'При bash: cd: nope: No such file or directory какие две команды помогут понять, что пошло не так?', options: ['rm и mv', 'ls (что реально есть рядом) и pwd (где я сейчас нахожусь)', 'cat и less', 'mkdir и touch'], correct: 1, why: 'ls покажет реальные имена каталогов рядом, pwd — подтвердит, в каком именно месте файловой системы ты находишься.' },
  { q: 'Почему у cat src нет способа «на всякий случай» показать хоть что-то, как это иногда бывает у других команд?', options: ['У каталога нет текстового содержимого в том смысле, в котором его читает cat — только список записей, а это работа ls, а не cat', 'Это баг конкретной версии cat', 'cat специально запрещает работать с папкой student', 'На самом деле cat всё же покажет список файлов'], correct: 0, why: 'cat построен вокруг чтения байтов файла подряд. У каталога иная природа — перечень записей, а не поток текста, поэтому за него отвечает ls.' },
  { q: 'Почему Ctrl+C в терминале не копирует текст (в отличие от почти всех остальных программ)?', options: ['Это просто старая привычка разработчиков терминалов, без технической причины', 'Ctrl+C жёстко закреплён в термин-драйвере за отправкой сигнала SIGINT программе — это поведение на уровне ядра ОС, а не решение конкретной команды', 'В терминале вообще нельзя ничего скопировать', 'Ctrl+C копирует, просто результат не виден'], correct: 1, why: 'Символ INTR (обычно Ctrl+C) в termios посылает SIGINT всей группе процессов переднего плана — так решено на уровне драйвера терминала, одинаково для любой программы.' },
]} />
</Block>

<SectionAnchor chapterId="linux-terminal" sectionId="typical-errors" />

## Слова главы

<Block kind="vocab" title="Словарь главы" chapterId="linux-terminal" blockId="vocab-glossary">
<Flashcards chapterId="linux-terminal" cards={[
  { term: 'terminal', translation: 'терминал', note: 'окно текстового диалога с компьютером: команда — Enter — ответ' },
  { term: 'shell', translation: 'оболочка', note: 'программа внутри терминала, которая разбирает и выполняет команды; в Ubuntu — bash' },
  { term: 'command', translation: 'команда', note: 'инструкция оболочке: ls, cd, mkdir и другие' },
  { term: 'directory', translation: 'каталог', note: 'то же, что папка; создаётся командой mkdir' },
  { term: 'flag', translation: 'флаг', note: 'уточнение к команде, начинается с дефиса: ls -la' },
  { term: 'argument', translation: 'аргумент', note: 'то, над чем работает команда, например путь к файлу' },
  { term: 'path', translation: 'путь', note: 'адрес файла или каталога, например /home/student' },
  { term: 'root', translation: 'корень', note: 'самая верхняя точка файловой системы, обозначается /' },
]} />
</Block>

<SectionAnchor chapterId="linux-terminal" sectionId="vocab" />

## Потренируйся печатать

Сначала команда, которую наберёшь чаще всего:

<Block kind="trainer" title="Команды главы" chapterId="linux-terminal" blockId="trainer-chapter-commands">
<CodeTyping pools={[{ label: 'Команды главы', snippets: [
  'mkdir -p project/src project/docs',
  'touch README.md src/main.py docs/notes.md',
  'ls -la /home/student',
  'cp -r src src_backup',
  'mv docs/notes.md docs/README.md',
  'rm -r src_backup',
  'cat README.md',
  'mv reports/*.txt reports/archive/',
] }]} keyboard chapterId="linux-terminal" trainerId="trainer-chapter-commands" targetCpm={100} targetAccuracy={90} />
</Block>

Теперь цепочка из двух команд через `&&` — резервное копирование и его же удаление:

<Block kind="trainer" title={'Цепочка через &&'} chapterId="linux-terminal" blockId="trainer-chain">
<CodeTyping snippet={'cp -r src src_backup && rm -r src_backup'} chapterId="linux-terminal" trainerId="trainer-chain" targetCpm={110} targetAccuracy={90} />
</Block>

<Block kind="cheatsheet" title="Тренажёр командной строки" chapterId="linux-terminal" blockId="cheatsheet-practice-tools">

<ToolCard
  name="CMD Challenge"
  url="https://cmdchallenge.com/"
  desc="Задачки по командной строке прямо в браузере: терминал уже открыт, ставить ничего не нужно."
  embeddable
/>

</Block>

<SectionAnchor chapterId="linux-terminal" sectionId="practice-typing" />

## Команды и флаги главы

Шпаргалка по всем командам и флагам, которые встретились. Вернись сюда, если забудешь, что делает конкретная команда.

<Block kind="cheatsheet" title="Команды и флаги главы" chapterId="linux-terminal" blockId="cheatsheet-commands" favPayload={{
  kind: 'table',
  head: ['Команда', 'Флаг', 'Что делает', 'Пример'],
  rows: [
    ['pwd', '—', 'показать полный путь к текущему каталогу', 'pwd'],
    ['ls', 'без флагов', 'показать имена файлов и каталогов в столбик', 'ls'],
    ['ls', '-l', 'подробный формат: права, владелец, размер, дата', 'ls -l'],
    ['ls', '-a', 'показать и скрытые файлы (начинающиеся с точки)', 'ls -a'],
    ['ls', '-la', 'подробный формат и скрытые файлы вместе', 'ls -la'],
    ['cd', '—', 'перейти в каталог по абсолютному или относительному пути', 'cd project'],
    ['cd', '—', 'подняться к родительскому каталогу', 'cd ..'],
    ['cd', '—', 'вернуться в домашний каталог', 'cd ~ или cd'],
    ['mkdir', 'без флагов', 'создать один каталог (родитель должен существовать)', 'mkdir project'],
    ['mkdir', '-p', 'создать сразу цепочку вложенных каталогов', 'mkdir -p app/assets/images'],
    ['touch', '—', 'создать пустой файл или обновить его дату изменения', 'touch notes.txt'],
    ['cp', 'без флагов', 'скопировать файл', 'cp a.txt b.txt'],
    ['cp', '-r', 'скопировать каталог рекурсивно, вместе с содержимым', 'cp -r src src_backup'],
    ['mv', '—', 'переместить файл или каталог, либо переименовать на месте', 'mv old.txt new.txt'],
    ['rm', 'без флагов', 'удалить файл безвозвратно', 'rm notes.txt'],
    ['rm', '-r', 'удалить каталог рекурсивно, вместе с содержимым — ОПАСНО', 'rm -r src_backup'],
    ['cat', '—', 'вывести содержимое файла целиком в терминал', 'cat README.md'],
    ['less', '—', 'листать файл постранично: Space/b, стрелки, /поиск, q — выход', 'less numbers.txt'],
    ['--help', '—', 'краткая справка по флагам любой команды', 'ls --help'],
    ['man', '—', 'полное руководство по команде на весь экран', 'man ls'],
  ],
}}>

| Команда | Флаг | Что делает | Пример |
|---|---|---|---|
| `pwd` | — | показать полный путь к текущему каталогу | `pwd` |
| `ls` | без флагов | показать имена файлов и каталогов в столбик | `ls` |
| `ls` | `-l` | подробный формат: права, владелец, размер, дата | `ls -l` |
| `ls` | `-a` | показать и скрытые файлы (начинающиеся с точки) | `ls -a` |
| `ls` | `-la` | подробный формат и скрытые файлы вместе | `ls -la` |
| `cd` | — | перейти в каталог по абсолютному или относительному пути | `cd project` |
| `cd` | — | подняться к родительскому каталогу | `cd ..` |
| `cd` | — | вернуться в домашний каталог | `cd ~` или `cd` |
| `mkdir` | без флагов | создать один каталог (родитель должен существовать) | `mkdir project` |
| `mkdir` | `-p` | создать сразу цепочку вложенных каталогов | `mkdir -p app/assets/images` |
| `touch` | — | создать пустой файл или обновить его дату изменения | `touch notes.txt` |
| `cp` | без флагов | скопировать файл | `cp a.txt b.txt` |
| `cp` | `-r` | скопировать каталог рекурсивно, вместе с содержимым | `cp -r src src_backup` |
| `mv` | — | переместить файл или каталог, либо переименовать на месте | `mv old.txt new.txt` |
| `rm` | без флагов | удалить файл безвозвратно | `rm notes.txt` |
| `rm` | `-r` | удалить каталог рекурсивно, вместе с содержимым — ОПАСНО | `rm -r src_backup` |
| `cat` | — | вывести содержимое файла целиком в терминал | `cat README.md` |
| `less` | — | листать файл постранично: Space/b, стрелки, `/поиск`, `q` — выход | `less numbers.txt` |
| `--help` | — | краткая справка по флагам любой команды | `ls --help` |
| `man` | — | полное руководство по команде на весь экран | `man ls` |

</Block>

<details>
<summary>Углубиться</summary>

**Не все команды одинаковы: встроенные и внешние.** `cd` и `pwd` — не отдельные программы, а builtin-команды (встроенные), они живут прямо внутри самой оболочки bash. А вот `ls`, `mkdir`, `cp`, `mv`, `rm`, `cat` — обычные исполняемые файлы где-то на диске, например `/usr/bin/ls`; bash находит их, просматривая список папок в переменной окружения `$PATH`. Проверить разницу можно командой `type`: `type cd` ответит `cd is a shell builtin`. С `ls` есть нюанс: стандартный `~/.bashrc` в Ubuntu заводит алиас `ls='ls --color=auto'`, поэтому `type ls` сначала расскажет про алиас, а настоящий путь к программе покажет `type -P ls` — `/usr/bin/ls` (или `type -a ls`, который выведет обе строки сразу). У `cd` есть веская причина быть builtin: обычная программа получает свою собственную временную рабочую область (процесс) и не может дотянуться и поменять «текущий каталог» у оболочки, которая её запустила, — она просто исчезнет, как только закончит работу, а оболочка останется на прежнем месте. Сменить каталог оболочки может только код, который выполняется внутри самой оболочки, — то есть только builtin.

**Права доступа `rwx` — отдельная большая тема.** В выводе `ls -l` строка вроде `drwxr-xr-x` — это тип объекта и три тройки прав: для владельца, для группы и для всех остальных, каждая из трёх букв `r` (read — читать), `w` (write — писать), `x` (execute — исполнять для файла, входить внутрь для каталога). Разобрать это подробно, включая команду `chmod`, которая эти права меняет, — задача отдельной главы про файлы и права доступа.

**История less.** Пейджер (pager) — не изобретение Linux, первые появились ещё в 1970-х на Unix. Первым был `more`, но он умел листать только вперёд; `less` дописали позже с полноценной прокруткой в обе стороны и назвали в шутку наоборот — «меньше значит больше».

Дальше по теме:
- [GNU Bash Manual — Bourne Shell Builtins](https://www.gnu.org/software/bash/manual/html_node/Bourne-Shell-Builtins.html) — официальное руководство bash: полное описание cd и других встроенных команд, включая флаги, о которых мы не говорили
- [Filesystem Hierarchy Standard](https://en.wikipedia.org/wiki/Filesystem_Hierarchy_Standard) — что означают каталоги верхнего уровня вроде /home, /etc, /usr и зачем файловая система Linux устроена именно так

→ Отдельная тема: [Регулярные выражения для grep](/docs/advanced/grep-regex) — как искать в файлах не точный текст, а шаблон: классы символов, якоря и квантификаторы с нуля.

</details>

<SectionAnchor chapterId="linux-terminal" sectionId="cheatsheet-commands" />

## Куда дальше: проверенные ресурсы

<Block kind="cheatsheet" title="Ресурсы про терминал" chapterId="linux-terminal" blockId="cheatsheet-resources">

- [Selectel — основные команды Linux терминала с примерами](https://selectel.ru/blog/basic-linux-commands/) — открывай, если хочется того же материала, но одним длинным читаемым текстом с примерами команд подряд, без интерактива.
- [Cloud.ru — 15 основных команд Linux для новичков](https://cloud.ru/blog/15-osnovnykh-komand-linux-dlya-novichkov) — открывай, чтобы увидеть, какие команды идут следующими после этой главы: chmod, ps, top и другие, с которыми предстоит познакомиться позже.
- [Видеоурок: терминал Linux — команды ls, rm, mkdir, pwd, cd, echo](https://www.youtube.com/watch?v=XAfDrMeqoHY) — открывай, если проще воспринимать на слух и глазами, чем текстом: те же команды, но с живым терминалом на экране.
- [Хабр — история массового удаления данных командой rm -rf](https://habr.com/ru/articles/392925/) — открывай, чтобы увидеть не выдуманную страшилку, а реальный случай: как пустая переменная в скрипте превратила безобидную с виду команду в `rm -rf /` и стоила полутора тысяч серверов.
- [Ubuntu — The Linux command line for beginners](https://ubuntu.com/tutorials/command-line-for-beginners) — официальный учебник Ubuntu на английском: история командной строки, устройство файловой системы и практика с нуля.
- [man-страница ls](https://man7.org/linux/man-pages/man1/ls.1.html) — полное описание ls со всеми флагами, не только теми тремя, что разобраны в этой главе: так выглядит настоящая документация Linux.

- [Видео и материалы сообщества — ниже](#community-sources) — ролики по теме и ссылки от студентов и преподавателей смотри в самом низу страницы.

</Block>

<SectionAnchor chapterId="linux-terminal" sectionId="resources" />

## Челлендж ⭐

В каталоге `reports` лежат три файла с отчётами и один посторонний: `jan.txt`, `feb.txt`, `mar.txt`, `notes.md`. Все они уже созданы — тебе нужно организовать их без единой ошибки, только командами из этой главы.

**Ступень 1.** Создай внутри `reports` подкаталог `archive` и перенеси туда все три `.txt`-файла, используя `mv` для каждого файла по отдельности (три отдельные команды `mv`). `notes.md` должен остаться на месте, в `reports`. Проверь результат через `ls reports` и `ls reports/archive`.

**Ступень 2 ⭐.** Три отдельных `mv` — не проблема для трёх файлов, но что если бы их было триста? Есть способ переместить сразу все файлы с расширением `.txt` **одной командой**, не называя ни одного из них по имени. Специальный символ подстановки (wildcard) `*` заменяет собой «что угодно» в имени файла. Разберись сам: что означает `*.txt` и как его применить к `mv`. Подсказка спрятана в самом вопросе.

<details>
<summary>Решение (сначала попробуй сам)</summary>

Ступень 1 (создание файлов для тренировки и перенос по одному):

```bash
mkdir reports
touch reports/jan.txt reports/feb.txt reports/mar.txt reports/notes.md
mkdir reports/archive
mv reports/jan.txt reports/archive/
mv reports/feb.txt reports/archive/
mv reports/mar.txt reports/archive/
```

Ступень 2 ⭐:

```bash
mv reports/*.txt reports/archive/
```

`*` — это подстановка имени файла (wildcard), которую разворачивает сама оболочка bash ещё до того, как `mv` вообще увидит команду: `*.txt` превращается в список всех файлов каталога, чьё имя заканчивается на `.txt` — в нашем случае в `jan.txt feb.txt mar.txt`. Файл `notes.md` под шаблон не попадает и остаётся на месте. По сути, `mv reports/*.txt reports/archive/` — это ровно те же три команды из ступени 1, просто bash сам подставил список файлов вместо того, чтобы перечислять их руками.

</details>

<SectionAnchor chapterId="linux-terminal" sectionId="challenge" />

<Block kind="quiz" title="Экзамен главы" chapterId="linux-terminal" blockId="exam">
<ChapterExam chapterId="linux-terminal" questions={[
  { q: 'Ты по ошибке набрал pwd project вместо cd project. Что сделает pwd?', options: ['Выдаст ошибку — pwd не принимает аргументов', 'Перейдёт в каталог project, как cd', 'Молча проигнорирует слово project и напечатает текущий путь как обычно', 'Создаст каталог project и перейдёт в него'], correct: 2, why: 'pwd не ищет каталог project и не ругается на лишний аргумент — она молча игнорирует всё, что ей передали, и печатает путь как ни в чём не бывало. Это типичная ловушка невнимательности, а не ошибка.' },
  { q: 'В GNOME Files ты перетаскиваешь файл с диска на подключённую флешку, не зажимая ни Ctrl, ни Shift. Какая команда терминала спрячется за этим действием?', options: ['mv — файл переместится', 'cp — файл скопируется, потому что назначение на другом устройстве', 'Ничего не произойдёт без модификатора', 'rm, а затем mkdir'], correct: 1, why: 'GNOME Files решает, mv это или cp, глядя на то, один это диск или разные: перетаскивание на другое устройство (например, флешку) копирует, а не перемещает. Ctrl/Shift переключают это поведение принудительно.' },
  { q: 'Почему переименование гигабайтного видеофайла через mv происходит мгновенно, а копирование того же файла командой cp — нет?', options: ['mv сжимает файл перед переименованием', 'mv использует более быстрый диск для временного хранения', 'mv (rename()) только меняет запись в каталоге, не трогая содержимое на диске, а cp физически копирует все байты', 'Разницы во времени на самом деле нет'], correct: 2, why: 'Переименование и перемещение в файловых системах Linux — это системный вызов rename(): он меняет запись о том, где лежит файл, не трогая его содержимое. cp, наоборот, честно копирует данные — отсюда и разница в скорости.' },
  { q: 'Команда type cd возвращает cd is a shell builtin, а type -P ls — /usr/bin/ls. Почему cd обязана быть builtin, а не отдельной программой вроде ls?', options: ['Потому что cd работает быстрее, если она встроена', 'Обычная программа запускается в своём собственном процессе и не может поменять текущий каталог оболочки, которая её вызвала, — это может сделать только код внутри самой оболочки', 'Потому что cd не принимает аргументов', 'Исторически так сложилось, технической причины нет'], correct: 1, why: 'Программа-процесс исчезает, как только заканчивает работу, а оболочка остаётся на прежнем месте — она не может дотянуться и поменять «текущий каталог» родительской оболочки. Сменить его может только код, выполняющийся внутри самой оболочки, то есть builtin.' },
  { q: 'По тексту главы, что стало причиной реальной истории 2016 года, когда владелец одного хостинга случайно уничтожил данные полутора тысяч серверов?', options: ['Целенаправленная атака хакеров', 'Автоматизированная команда, где путь собирался из переменных, а одна из них оказалась пустой — команда превратилась ровно в rm -rf /', 'Сбой жёсткого диска на всех серверах одновременно', 'Забытый флаг -p у mkdir'], correct: 1, why: 'Глава приводит это как реальный пример: если rm -rf "$papka/$podpapka" собран из переменных, а обе оказались пустыми, команда буквально превращается в rm -rf / — без единой явно пустой переменной в поле зрения.' },
  { q: 'После неудавшегося rm -rf / современный rm печатает вторую строку: rm: use --no-preserve-root to override this failsafe. Что делает флаг --no-preserve-root?', options: ['Показывает список всех файлов перед удалением', 'Отключает встроенную защиту, которая как раз и остановила rm от удаления корня /', 'Восстанавливает удалённые файлы', 'Ограничивает rm только текущим каталогом'], correct: 1, why: 'Это буквально то, что говорит сообщение: --no-preserve-root отключает страховку (failsafe), которая только что остановила rm -rf / — обычному пользователю эта опция ни для чего не нужна.' },
  { q: 'Команда less numbers.txt на несуществующем файле в этой главе выводит ghost.txt: No such file or directory — без привычной приставки less: перед текстом. Почему?', options: ['Это баг, который скоро починят', 'Такая ошибка выводится прямо в статусной строке внизу экрана — особенность именно команды less', 'less вообще не умеет сообщать об ошибках', 'Приставка появляется только при запуске less с флагом -v'], correct: 1, why: 'Глава отдельно отмечает эту особенность: в отличие от большинства команд, less показывает сообщение об ошибке в статусной строке внизу экрана, без имени программы впереди.' },
  { q: 'В практикуме шаги 6 и 10 предлагается склеить в одну строку: cp -r src src_backup && ls -la. Если cp -r не сможет скопировать (например, src не существует), что произойдёт с ls -la?', options: ['ls -la всё равно выполнится', 'ls -la не выполнится — && запускает вторую команду только если первая завершилась без ошибки', 'Обе команды выполнятся одновременно, независимо друг от друга', 'Терминал покажет синтаксическую ошибку и не выполнит ни одну из команд'], correct: 1, why: 'Глава объясняет: несколько команд можно склеить через &&, и вторая выполнится только если первая завершилась без ошибки — «если копирование провалится, лишний ls просто не запустится».' },
]} />
</Block>

## Что должен уметь

- Объяснять разницу между терминалом и оболочкой (shell) и почему в Ubuntu это конкретно bash.
- Разбирать любую команду на три части: команда, флаги, аргумент — на примере `ls -la /home/student`.
- Перемещаться по файловой системе командами `pwd`, `ls`, `cd`, используя абсолютные и относительные пути, `cd ..` и `cd ~`.
- Показывать содержимое каталога тремя способами — `ls`, `ls -l`, `ls -a`, `ls -la` — и объяснять разницу между ними.
- Создавать структуру каталогов и файлов командами `mkdir` (в том числе с `-p`) и `touch`.
- Копировать и перемещать файлы и целые каталоги через `cp` (с `-r` для каталогов) и `mv`.
- Формулировать, чем `rm` по-настоящему опасна, что защищает и чего не защищает страховка от `rm -rf /`, и проверять команду перед запуском.
- Выводить содержимое файла через `cat` и объяснять, чем `less` лучше подходит для длинных файлов.
- Узнавать по точному тексту сообщения шесть типичных ошибок этой главы и знать, как каждую починить.

<SectionAnchor chapterId="linux-terminal" sectionId="what-you-should-know" />

<Hint id="linux-terminal-h4" type="fact">
В учебном терминале этой главы спрятаны пасхалки из настоящего Linux: попробуй набрать `sl`, `cowsay Привет`, `fortune`, `neofetch`, `figlet PGK`, `matrix` — и попроси что-нибудь у `sudo`.
</Hint>
