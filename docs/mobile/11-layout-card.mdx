---
title: 'Карточка по макету: от картинки к коду'
audience: мобилка
level: база
order: 11
sidebar_position: 11
---

import ChapterCover from '@site/src/components/ChapterCover';

<ChapterCover chapterId="layout-card" />

import Figure from '@site/src/components/Figure';
import SyntaxBreakdown from '@site/src/components/SyntaxBreakdown';
import CodeTyping from '@site/src/components/CodeTyping';
import Flashcards from '@site/src/components/Flashcards';
import KotlinPlay from '@site/src/components/KotlinPlay';
import ComposePreview from '@site/src/components/ComposePreview';
import SelfCheck from '@site/src/components/SelfCheck';
import ChapterExam from '@site/src/components/ChapterExam';
import Block from '@site/src/components/Block';
import Hint from '@site/src/components/Hint';
import CollapsibleToc from '@site/src/components/CollapsibleToc';
import ChapterProgress from '@site/src/components/ChapterProgress';
import SectionAnchor from '@site/src/components/SectionAnchor';
import CardAssembler from '@site/src/components/CardAssembler';

# Карточка по макету: от картинки к коду

<ChapterProgress chapterId="layout-card" totalSections={10} totalQuizzes={4} totalTrainers={3} />

## Зачем это нужно

Прошлая глава научила читать макет: где `dp`, где `sp`, какой контейнер выбрать, как работает `Arrangement`. Теперь надо сделать то, ради чего всё это нужно — собрать по макету настоящий элемент.

Разберём карточку товара. Это один из самых частых элементов на экранах интернет-магазинов и один из самых частых элементов в заданиях по мобильной разработке на чемпионате. Собирать будем по шагам, в том же порядке, в каком карточку обычно и собирают на практике: сначала внешняя рамка, потом слой с изображением и бейджем, потом текст, потом ряд с ценой.

А в конце — то, что отличает готовую работу от наброска: вынос чисел макета в именованные константы и разбор пяти ошибок, на которых карточка ломается чаще всего.

<Figure scheme="lc-four-steps" caption="Карточку собирают снаружи внутрь, и каждый шаг проверяют отдельно" />

Готового набора цветов, отступов и текстовых стилей на все случаи в этой главе ещё нет — так, как проект обычно и начинается: с одной карточки и конкретных чисел из макета. Собрать из таких чисел переиспользуемую систему — тема следующей главы.

<CollapsibleToc toc={toc} chapterId="layout-card" />

<SectionAnchor chapterId="layout-card" sectionId="why" />
## Перенос макета в код: карточка товара шаг за шагом

Спецификация из первого раздела уже собрана. Дальше — тот же порядок, в котором обычно и собирают карточку на практике: сначала внешняя рамка, потом слой с изображением и бейджем, потом текст, потом ряд с ценой.

### Шаг 1. Внешняя рамка карточки

Карточка целиком — вертикальный список из трёх блоков (изображение, заголовок, цена), значит внешний контейнер — `Column`. Из первой строки таблицы спецификации («Карточка целиком»): фон белый (`#FFFFFF`), внутренний отступ — 12dp со всех сторон, ширина — на весь доступный экран.

```kotlin
@Composable
fun ProductCard(title: String, price: String, discountPercent: Int) {
    Column(
        modifier = Modifier
            .fillMaxWidth()
            .background(Color(0xFFFFFFFF))
            .padding(12.dp)
    ) {
        // содержимое — дальше, шаг за шагом
    }
}
```

`Modifier.background(...)` — ещё один модификатор из той же цепочки, что и `.fillMaxSize()`/`.padding()` из прошлой главы: принимает `Color` и красит фон элемента этим цветом, ничего больше не меняя в остальной цепочке.

### Шаг 2. Изображение и бейдж скидки — слой на Box

Внутри карточки первым идёт изображение — но по спецификации на него ещё накладывается бейдж скидки. Это тот самый случай «два элемента в одном месте», под который заведён `Box`.

```kotlin
Box {
    Box(
        modifier = Modifier
            .fillMaxWidth()
            .height(120.dp)
            .background(Color(0xFFEEEEEE))
    )
    Text(
        text = "-$discountPercent%",
        fontSize = 12.sp,
        modifier = Modifier
            .align(Alignment.TopEnd)
            .background(Color(0xFFE53935))
            .padding(4.dp)
    )
}
```

Обрати внимание: здесь ДВА вложенных `Box` — внешний просто группирует «слой с изображением», а внутренний, поменьше, служит серой заглушкой самого изображения (в настоящем проекте на его месте была бы функция `AsyncImage` для загрузки настоящей картинки по `imageUrl`, но она не входит в тему этой главы). `Modifier.align(Alignment.TopEnd)` у `Text` — то самое переопределение позиции для ОДНОГО ребёнка, разобранное в прошлом разделе: заглушка изображения растянута на весь `Box` без выравнивания, а вот бейдж явно прижат в правый верхний угол.

### Шаг 3. Заголовок и цена с кнопкой

Заголовок — обычный `Text` с `fontSize` из спецификации. Цена и кнопка — `Row` с `Arrangement.SpaceBetween`, как и определено в таблице выше.

```kotlin
Text(text = title, fontSize = 16.sp)
Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.SpaceBetween,
    verticalAlignment = Alignment.CenterVertically
) {
    Text(text = price, fontSize = 18.sp)
    Button(onClick = { }) {
        Text(text = "В корзину")
    }
}
```

<Hint id="layout-card-h3" type="tip">
`verticalAlignment = Alignment.CenterVertically` здесь не из таблицы спецификации напрямую, а логическое следствие того, что цена и кнопка — разной высоты (текст ниже кнопки), и без выравнивания по центру они бы «прилипли» к верху ряда неровно.
</Hint>

### Собираем всё вместе

```kotlin
@Composable
fun ProductCard(title: String, price: String, discountPercent: Int) {
    Column(
        modifier = Modifier
            .fillMaxWidth()
            .background(Color(0xFFFFFFFF))
            .padding(12.dp)
    ) {
        Box {
            Box(
                modifier = Modifier
                    .fillMaxWidth()
                    .height(120.dp)
                    .background(Color(0xFFEEEEEE))
            )
            Text(
                text = "-$discountPercent%",
                fontSize = 12.sp,
                modifier = Modifier
                    .align(Alignment.TopEnd)
                    .background(Color(0xFFE53935))
                    .padding(4.dp)
            )
        }
        Text(text = title, fontSize = 16.sp)
        Row(
            modifier = Modifier.fillMaxWidth(),
            horizontalArrangement = Arrangement.SpaceBetween,
            verticalAlignment = Alignment.CenterVertically
        ) {
            Text(text = price, fontSize = 18.sp)
            Button(onClick = { }) {
                Text(text = "В корзину")
            }
        }
    }
}
```

### Разбор по строкам

- **Строка 2: `fun ProductCard(title: String, price: String, discountPercent: Int)`** — три параметра снаружи, ровно столько, сколько реально меняется от товара к товару; всё остальное (отступы, цвета, размеры шрифта) — фиксированные числа из макета, а не параметры.
- **Строки 3–8: внешний `Column`** — из шага 1, без изменений: фон, ширина на весь экран, отступ 12dp со всех сторон.
- **Строки 9–24: внешний `Box` с двумя детьми** — из шага 2: серая заглушка изображения растянута на весь `Box` (`.fillMaxWidth().height(120.dp)`), а `Text` с бейджем прижат в угол через `.align(Alignment.TopEnd)`, покрашен красным и получил свой собственный маленький отступ в 4dp — ВНУТРИ бейджа, а не относительно всей карточки.
- **Строка 25: `Text(text = title, fontSize = 16.sp)`** — заголовок, второй по счёту ребёнок внешнего `Column`, значит встанет НИЖЕ блока с изображением.
- **Строки 26–35: `Row` с ценой и кнопкой** — третий и последний ребёнок `Column`; `SpaceBetween` разводит цену и кнопку по краям строки. Всего у внешнего `Column` три ребёнка: `Box` со слоями, `Text` заголовка и этот `Row`.

**Что должно появиться на экране:** белая карточка на всю ширину, сверху — серый прямоугольник изображения с красным бейджем `-N%` в правом верхнем углу, ниже — заголовок товара, ещё ниже — строка, где слева цена, а справа кнопка «В корзину».

Собранную карточку можно покрутить вживую прямо здесь: слева — упрощённый мокап экрана, справа — Compose-псевдокод, сгенерированный из того же самого дерева вёрстки. Подвигай padding, arrangement и fontSize и смотри, как едет вёрстка — и как синхронно переписывается код. Одно упрощение: `Modifier.align(Alignment.TopEnd)` эта песочница не умеет, поэтому бейдж скидки здесь прижат к углу по умолчанию — `TopStart`.

<Block kind="trainer" title="Живой мокап: покрути карточку товара" chapterId="layout-card" blockId="trainer-card-preview">
<ComposePreview
  editable
  tree={{
    type: 'Column',
    fillMaxWidth: true,
    background: '#FFFFFF',
    padding: 12,
    children: [
      {
        type: 'Box',
        children: [
          { type: 'Box', fillMaxWidth: true, height: 120, background: '#EEEEEE' },
          { type: 'Text', text: '-20%', fontSize: 12, background: '#E53935', padding: 4 },
        ],
      },
      { type: 'Text', text: 'Заголовок товара', fontSize: 16 },
      {
        type: 'Row',
        fillMaxWidth: true,
        arrangement: 'spaceBetween',
        alignment: 'center',
        children: [
          { type: 'Text', text: '1 990 ₽', fontSize: 18 },
          { type: 'Button', text: 'В корзину' },
        ],
      },
    ],
  }}
  chapterId="layout-card"
  trainerId="trainer-card-preview"
/>
</Block>

А чтобы структура «внешний контейнер → изображение → заголовок → ряд с ценой» уложилась в голове, собери её ещё раз сам — из перемешанных строк вёрстки.

<Block kind="trainer" title="Собери структуру карточки" chapterId="layout-card" blockId="trainer-card-assembler">
<CardAssembler chapterId="layout-card" trainerId="trainer-card-assembler" />
</Block>

<Block kind="quiz" title="Карточка товара" chapterId="layout-card" blockId="quiz-product-card">
<SelfCheck chapterId="layout-card" quizId="quiz-product-card" questions={[
  { q: 'Почему в собранном ProductCard изображение и бейдж лежат внутри Box, а не внутри ещё одного Column?', options: ['Просто для разнообразия кода', 'Потому что бейдж должен оказаться ПОВЕРХ изображения, в одном и том же месте экрана, а не под ним — а это именно то, для чего существует Box', 'Column не поддерживает Text внутри Text', 'Box работает быстрее, чем Column, для двух элементов'], correct: 1, why: 'Column раскладывает элементы в отдельные места одно под другим; для наложения (изображение + бейдж в одном месте) годится только Box.' },
  { q: 'В коде .padding(4.dp) стоит на самом Text с бейджем, ПОСЛЕ .background(Color(0xFFE53935)). Что произойдёт, если поменять их местами — сначала .padding(4.dp), потом .background(...)?', options: ['Ничего не изменится, порядок модификаторов здесь не важен', 'Отступ окажется СНАРУЖИ красной заливки — фон станет меньше текста с бейджем, а не будет включать отступ вокруг него', 'Программа перестанет компилироваться', 'Текст бейджа исчезнет с экрана'], correct: 1, why: 'Как и в прошлой главе с .clickable/.padding, порядок модификаторов задаёт порядок "слоёв": .background().padding() красит сначала всю доступную область, потом добавляет отступ поверх покрашенного; в обратном порядке заливка охватила бы только сам текст, без отступа вокруг.' },
  { q: 'Строка Text(text = "-$discountPercent%", ...) — что здесь делает символ $ перед discountPercent?', options: ['Обозначает денежную сумму', 'Строковый шаблон — подставляет значение параметра discountPercent прямо в текст строки', 'Это опечатка, символ лишний', 'Указывает тип параметра'], correct: 1, why: 'Тот же строковый шаблон $переменная из самой первой главы курса — здесь он подставляет число скидки в надпись бейджа, например discountPercent = 20 даст текст "-20%".' },
  { q: 'Что было бы, если у Row с ценой и кнопкой убрать verticalAlignment = Alignment.CenterVertically?', options: ['Row вообще перестанет компилироваться без этого параметра', 'Цена и кнопка встанут по умолчанию вверх ряда (Alignment.Top), и из-за разной высоты текста и кнопки они окажутся визуально не выровнены по центру друг друга', 'Кнопка станет невидимой', 'horizontalArrangement перестанет работать без verticalAlignment'], correct: 1, why: 'verticalAlignment — необязательный параметр со значением по умолчанию (Alignment.Top для Row); без явного CenterVertically кнопка и текст цены просто прижались бы к верхнему краю строки, а не выровнялись по центру.' },
]} />
</Block>

<SectionAnchor chapterId="layout-card" sectionId="product-card" />

## Вынос размеров в константы

<Figure scheme="lc-magic-numbers" caption="Одно и то же число в четырёх местах против одной именованной константы" />

В собранном `ProductCard` числа — `12.dp`, `120.dp`, `4.dp`, `16.sp`, `18.sp`, `12.sp` и два цвета — разбросаны по всему телу функции. Пока карточка одна, это терпимо. Но макет обычно содержит МНОГО похожих элементов — карточки в списке, другие экраны с теми же отступами, — и голое число `12.dp`, повторённое в десяти местах, называют магическим числом (magic number): непонятно без контекста, что оно значит, и любое его изменение требует искать и править каждое повторение по отдельности.

Решение — вынести числа в именованные константы один раз и обращаться к ним по имени. В Compose для этого чаще всего заводят отдельный объект (object — уже знакомый механизм из примера с `Arrangement` выше, только теперь свой собственный):

```kotlin
object Dimens {
    val CardPadding = 12.dp
    val ImageHeight = 120.dp
    val BadgePadding = 4.dp
    val TitleFontSize = 16.sp
    val PriceFontSize = 18.sp
    val BadgeFontSize = 12.sp
}
```

<Block kind="breakdown" title="Разбор по частям: val CardPadding = 12.dp" chapterId="layout-card" blockId="breakdown-val-dimens">
<SyntaxBreakdown parts={[
  { text: 'val', label: 'неизменяемое хранилище — но НЕ const val', note: 'здесь важно именно val, а не const val: Dp и Sp создаются вызовом функции (.dp/.sp — свойства-расширения), а const val в Kotlin разрешён только для примитивных типов и String, вычисляемых уже на этапе компиляции. Что случится при попытке const val — отдельная типичная ошибка ниже' },
  { text: ' CardPadding', label: 'имя константы', note: 'по соглашению Kotlin — с большой буквы, как и другие val внутри object, которые фактически играют роль констант' },
  { text: ' = 12.dp', label: 'значение', note: 'то же самое число из макета, что и раньше — просто теперь оно существует в единственном месте' },
]} />
</Block>

Правило `val`, а не `const val`, специфично именно для `Dp`/`Sp`/`Color` и подобных типов Compose. Там, где значение — обычный примитив (`Int`, `String`, `Boolean`), `const val` работает и даже предпочтительнее: он вычисляется на этапе компиляции, а не при первом обращении. Разница видна на простом примере — рефакторинг магических чисел «до и после», где сами числа — обычные `Int`, без единиц измерения:

<KotlinPlay code={`fun describeCardBefore() {
    println("ДО: отступ=12, высота изображения=120, мин. высота карточки=\${120 + 12 * 2}")
}

object Dimens {
    const val CardPadding = 12
    const val ImageHeight = 120
}

fun describeCardAfter() {
    println("ПОСЛЕ: отступ=\${Dimens.CardPadding}, высота изображения=\${Dimens.ImageHeight}, мин. высота карточки=\${Dimens.ImageHeight + Dimens.CardPadding * 2}")
}

fun main() {
    describeCardBefore()
    describeCardAfter()
}`} />

**Что выведет:** одинаковые числа в обеих строках — `144` для минимальной высоты что «до», что «после». Рефакторинг НЕ меняет результат — и не должен: цель не в новом поведении, а в том, что теперь `12` и `120` существуют в коде ровно один раз, под понятным именем, а не разбросаны по функциям. Если на макете отступ карточки завтра станет `16` вместо `12`, в версии «после» достаточно поменять одну строку — `const val CardPadding = 12` на `16` — и значение обновится сразу везде, где встречается `Dimens.CardPadding`. В версии «до» пришлось бы искать каждое отдельное вхождение числа `12` по всему файлу и гадать, какое из них — тот самый отступ, а какое — случайное совпадение с другим смыслом.

<Block kind="quiz" title="Вынос в константы" chapterId="layout-card" blockId="quiz-dimens">
<SelfCheck chapterId="layout-card" quizId="quiz-dimens" questions={[
  { q: 'Почему магическое число называют именно "магическим", а не просто "числом"?', options: ['Потому что такие числа выбирают случайным образом', 'Потому что без контекста непонятно, ЧТО оно значит и откуда взялось — как будто появилось "по волшебству"', 'Это официальный термин Kotlin для чисел больше 100', 'Магические числа существуют только в Compose'], correct: 1, why: 'Магическое число — это буквально то, что видно в коде без объяснения смысла: 12.dp где-то в середине функции ничего не говорит о том, что это именно отступ карточки, а не что-то другое.' },
  { q: 'Почему для object Dimens с Dp-значениями нужно писать val, а не const val, хотя для Int-значений const val работает нормально?', options: ['const val вообще запрещён в Kotlin', 'const val разрешён только для примитивных типов (Int, Boolean и т.п.) и String — значений, известных уже на этапе компиляции; Dp создаётся вызовом функции .dp во время выполнения, а не является таким значением', 'val быстрее работает, чем const val, для любых типов', 'Dimens вообще нельзя объявлять через object'], correct: 1, why: '.dp — это вызов свойства-расширения, а не литерал; компилятор не может заранее, на этапе компиляции, вычислить такое значение, а именно это и требуется для const val.' },
  { q: 'В рефакторинге describeCardBefore/After — что изменилось в РЕЗУЛЬТАТЕ работы программы?', options: ['Результат стал другим — числа увеличились', 'Ничего не изменилось: обе версии печатают одинаковые числа, разница только в том, откуда код их берёт', 'Программа стала работать быстрее в несколько раз', 'До рефакторинга программа не компилировалась'], correct: 1, why: 'Вынос в константы — это рефакторинг: изменение структуры кода без изменения его поведения. Числа на экране одинаковые что до, что после — меняется только то, насколько легко их найти и обновить в будущем.' },
  { q: 'Если Dimens.CardPadding используется в пяти разных местах файла ProductCard.kt, сколько строк нужно изменить, чтобы поменять отступ карточки на новое значение?', options: ['Пять — по одной на каждое место использования', 'Одну — саму строку val CardPadding = ... внутри object Dimens', 'Ни одной, значение меняется автоматически', 'Все пять и ещё дополнительно саму декларацию'], correct: 1, why: 'В этом и есть выигрыш: единственный источник истины (тот же принцип single source of truth, что и у состояния в прошлой главе) — значение хранится в одном месте, а используется в пяти, но правка нужна только там, где оно объявлено.' },
]} />
</Block>

<SectionAnchor chapterId="layout-card" sectionId="dimens-constants" />

## Типичные ошибки

Все пять ошибок этой главы воспроизведены и проверены вживую в этой же песочнице — тем же компилятором Kotlin, что стоит за встроенным KotlinPlay, — на минимальных классах, повторяющих структуру настоящих Compose-типов (`Dp`, `Sp`, `Modifier`, `Alignment`, `RowScope`) ровно настолько, насколько нужно, чтобы получить ту же самую диагностику компилятора.

### 1. sp передан туда, где ожидается Dp (и наоборот)

<KotlinPlay code={`@JvmInline
value class Dp(val value: Float)
@JvmInline
value class Sp(val value: Float)
val Int.dp: Dp get() = Dp(this.toFloat())
val Int.sp: Sp get() = Sp(this.toFloat())

fun setFontSize(fontSize: Sp) {}

fun main() {
    setFontSize(16.dp)
}`} />

```text
Argument type mismatch: actual type is 'Dp', but 'Sp' was expected.
```

**Перевод:** «несовпадение типа аргумента: фактический тип Dp, а ожидался Sp». В настоящем Compose параметр `fontSize` ждёт значение типа `TextUnit` (тип, в который превращает число `.sp`), а `16.dp` — это `Dp`, совсем другой тип: числа выглядят одинаково («шестнадцать»), но единицы измерения, а значит и типы Kotlin, разные.

**Как починить:** заменить суффикс на правильный по смыслу — `.sp` для размера текста, `.dp` для всего остального: `setFontSize(16.sp)`.

### 2. Позиционный аргумент Box перепутан местами

```kotlin
Box(Alignment.TopEnd) {
    Text(text = "Бейдж")
}
```

```text
Argument type mismatch: actual type is 'Alignment', but 'Modifier' was expected.
```

**Перевод:** «несовпадение типа аргумента: фактический тип Alignment, а ожидался Modifier». У `Box` первый ПОЗИЦИОННЫЙ параметр (без явного имени) — `modifier`, а `contentAlignment` — только второй; если написать `Box(Alignment.TopEnd)` без имени `contentAlignment =`, компилятор пытается подставить `Alignment.TopEnd` на место `modifier` и обнаруживает несовпадение типов. Формулировка проверена на минимальном аналоге с той же сигнатурой параметров, что и у настоящего `Box`.

**Как починить:** указать имя параметра явно — `Box(contentAlignment = Alignment.TopEnd) { ... }`. Именованные аргументы для необязательных параметров, идущих не первыми, — не стиль, а необходимость: без имени компилятор ориентируется строго по порядку.

### 3. weight() вызван вне Row или Column

<KotlinPlay code={`interface RowScope
fun RowScope.weight(value: Float): Unit {}

fun main() {
    weight(1f)
}`} />

```text
Unresolved reference 'weight'.
```

**Перевод:** «неразрешённая ссылка weight». В настоящем Compose `weight(...)` (задаёт, какую долю свободного места в `Row`/`Column` займёт элемент) — не отдельная свободная функция, а функция-расширение, объявленная специально для `RowScope`/`ColumnScope`. Такую функцию можно вызвать ТОЛЬКО у объекта с подходящим типом-получателем (receiver) — то есть только внутри трейлинг-лямбды `Row { ... }` или `Column { ... }`, где Compose сам подставляет нужный scope. Внутри `Box` или вообще вне какого-либо контейнера тот же вызов `weight(...)` не находит подходящей функции — ровно то же самое случилось бы с `Modifier.align(...)` вне `Box`, разобранным чуть раньше в этой главе.

**Как починить:** использовать `weight` только внутри `Row { ... }` или `Column { ... }`, а не внутри `Box` или отдельно; если нужно занять долю свободного места ИМЕННО внутри `Box`, `weight` не подходит вообще — там за размер отвечают `fillMaxWidth(fraction = ...)` и подобные модификаторы без ограничения по scope.

### 4. const val с типом Dp — не примитив и не String

<Figure scheme="lc-const-val" caption="Int компилятор вычислит сам, а .dp — это вызов, который случится только при запуске" />

<KotlinPlay code={`@JvmInline
value class Dp(val value: Float)
val Int.dp: Dp get() = Dp(this.toFloat())
const val CardPadding = 12.dp

fun main() {
    println(CardPadding)
}`} />

```text
Const 'val' has type 'Dp'. Only primitive types and 'String' are allowed.
```

**Перевод:** «const val имеет тип Dp. Разрешены только примитивные типы и String». `const val` требует значения, которое компилятор может вычислить ЦЕЛИКОМ на этапе компиляции, без запуска программы, — а `16.dp` фактически вызывает функцию (`.dp` — свойство-расширение), результат которой известен только во время выполнения. Именно поэтому в разделе про константы `object Dimens` использует обычный `val`, а не `const val`, для всех `Dp`/`Sp`-значений.

**Как починить:** заменить `const val` на обычный `val`: `val CardPadding = 12.dp`. `const val` оставить только для настоящих компайл-тайм значений — `Int`, `String`, `Boolean` и подобных примитивов, как в демо с `Dimens.CardPadding: Int` в разделе про рефакторинг.

### 5. Опечатка в имени именованного аргумента

<KotlinPlay code={`fun myRow(horizontalArrangement: String = "start", content: () -> Unit) {}

fun main() {
    myRow(arrangement = "center") { }
}`} />

```text
No parameter with name 'arrangement' found.
```

**Перевод:** «параметр с именем arrangement не найден». Настоящий параметр `Row` называется `horizontalArrangement`, а не просто `arrangement` — сокращённое имя, которое иногда кажется логичным по аналогии с `contentAlignment` у `Box`, компилятор не подставляет автоматически: имя именованного аргумента должно совпадать буквально.

**Как починить:** использовать точное имя параметра — `horizontalArrangement` у `Row`, `verticalArrangement` у `Column`. Автодополнение Android Studio почти всегда подсказывает правильное имя раньше, чем успеваешь ошибиться, но при написании кода руками (или на бумаге на чемпионате) эта ошибка встречается часто.

<Block kind="quiz" title="Типичные ошибки" chapterId="layout-card" blockId="quiz-typical-errors">
<SelfCheck chapterId="layout-card" quizId="quiz-typical-errors" questions={[
  { q: 'Какая из пяти ошибок этой главы возникает из-за того, что первый позиционный (безымянный) параметр Box — именно modifier, а не contentAlignment?', options: ['Unresolved reference weight (ошибка 3)', "Argument type mismatch: actual type is 'Alignment', but 'Modifier' was expected. (ошибка 2)", "Const 'val' has type 'Dp' (ошибка 4)", 'No parameter with name arrangement (ошибка 5)'], correct: 1, why: 'Box(Alignment.TopEnd) без имени параметра пытается подставить Alignment на место первого параметра — modifier, а не второго — contentAlignment.' },
  { q: 'Чем ошибка 3 (weight вне Row/Column) объясняется на уровне механизма Kotlin, а не просто "так работает Compose"?', options: ['Это специальная магия компилятора Compose, недоступная обычному Kotlin', 'weight — функция-расширение с типом-получателем RowScope; вызвать её можно только там, где есть подходящий receiver — то есть внутри Row/Column, а не в произвольном месте', 'weight вообще не является функцией', 'Ошибка возникает только на старых версиях Kotlin'], correct: 1, why: 'Функции-расширения со scope-получателем — обычный механизм языка Kotlin (как getValue/setValue у делегатов из прошлой главы), а не что-то специфичное только для Compose; Compose просто активно использует этот механизм для weight и align.' },
  { q: 'Почему val CardPadding = 12.dp работает без ошибок, а const val CardPadding = 12.dp — нет?', options: ['val и const val — это одно и то же в Kotlin', 'const val требует значения, вычислимого на этапе компиляции (примитив или String); 12.dp — результат вызова функции-расширения, известный только во время выполнения, поэтому не подходит', 'const val работает только внутри функций, а не внутри object', 'Числа с плавающей точкой вообще нельзя использовать с const val'], correct: 1, why: 'Именно эта проверка — "вычислимо ли значение ДО запуска программы" — отличает const val от обычного val, и Dp, создаваемый вызовом .dp, эту проверку не проходит.' },
  { q: 'myRow(arrangement = "center") при объявлении fun myRow(horizontalArrangement: String = "start", ...) — почему это не сработает даже с правильным типом значения (String)?', options: ['Тип значения тоже не совпадает', 'Имя именованного аргумента должно буквально совпадать с именем параметра в объявлении функции — "arrangement" и "horizontalArrangement" для компилятора два разных имени, даже если оба выглядят логично', 'В Kotlin вообще нельзя передавать String в параметр по умолчанию', 'Ошибка появляется только при отсутствии content-лямбды'], correct: 1, why: 'Именованные аргументы ищутся строго по точному имени параметра из сигнатуры функции — компилятор не подбирает "похожие по смыслу" имена, даже если тип значения подходит идеально.' },
]} />
</Block>

<SectionAnchor chapterId="layout-card" sectionId="typical-errors" />


## Слова главы

<Block kind="vocab" title="Словарь главы" chapterId="layout-card" blockId="vocab-glossary">
<Flashcards chapterId="layout-card" cards={[
  { term: 'product card', translation: 'карточка товара (product card)', note: 'повторяющийся элемент списка: изображение, заголовок, цена и действие в одной рамке' },
  { term: 'magic number', translation: 'магическое число (magic number)', note: 'голое числовое значение в коде без объяснения смысла, кандидат на вынос в именованную константу' },
  { term: 'const val', translation: 'константа времени компиляции (const val)', note: 'значение, известное до запуска программы; принимает только примитивы и String' },
  { term: 'object', translation: 'объект-одиночка (object)', note: 'в Kotlin — единственный экземпляр, удобное место для набора именованных констант' },
  { term: 'badge', translation: 'бейдж (badge)', note: 'небольшая метка поверх изображения: скидка, статус, количество' },
  { term: 'scope', translation: 'область видимости (scope)', note: 'то, что делает weight доступным внутри Row и Column, а align — внутри Box' },
]} />
</Block>

<SectionAnchor chapterId="layout-card" sectionId="vocab" />

## Потренируйся печатать

<Block kind="trainer" title="Строки карточки" chapterId="layout-card" blockId="trainer-card-lines">
<CodeTyping pools={[{ label: 'Сборка карточки', snippets: [
  'Column(modifier = Modifier.padding(Dimens.CardPadding)) {',
  'Box(modifier = Modifier.fillMaxWidth().height(Dimens.ImageHeight)) {',
  'Modifier.align(Alignment.TopEnd).padding(8.dp)',
  'Row(horizontalArrangement = Arrangement.SpaceBetween) {',
  'object Dimens { val CardPadding = 12.dp }',
  'Text(text = price, fontSize = 18.sp)',
] }]} keyboard chapterId="layout-card" trainerId="trainer-card-lines" targetCpm={110} targetAccuracy={90} />
</Block>

<SectionAnchor chapterId="layout-card" sectionId="practice-typing" />

## Символы и приёмы главы

<Block kind="cheatsheet" title="Символы и приёмы главы" chapterId="layout-card" blockId="cheatsheet-symbols-techniques" favPayload={{
  kind: 'table',
  head: ['Символ / приём', 'Как называется', 'Что делает', 'Пример'],
  rows: [
    ['object Dimens { val X = ... }', 'объект-хранилище констант', 'выносит повторяющиеся числа макета в единственное именованное место', 'Dimens.CardPadding'],
    ['val, не const val', 'константа времени выполнения', 'единственный вариант для Dp и Sp: значение вычисляется при создании объекта', 'val CardPadding = 12.dp'],
    ['Modifier.fillMaxWidth()', 'растянуть по ширине', 'занять всю доступную ширину родителя', 'Modifier.fillMaxWidth().height(120.dp)'],
    ['Modifier.padding(...)', 'отступ', 'внутренний отступ элемента; порядок в цепочке модификаторов важен', 'Modifier.padding(12.dp).background(color)'],
    ['Arrangement.SpaceBetween', 'развести по краям', 'прижимает первый элемент к началу, последний — к концу', 'Row(horizontalArrangement = Arrangement.SpaceBetween)'],
  ],
}}>

| Символ / приём | Как называется | Что делает | Пример |
|---|---|---|---|
| `object Dimens { val X = ... }` | объект-хранилище констант | выносит повторяющиеся числа макета в единственное именованное место | `Dimens.CardPadding` |
| `val`, не `const val` | константа времени выполнения | единственный вариант для `Dp` и `Sp` | `val CardPadding = 12.dp` |
| `Modifier.fillMaxWidth()` | растянуть по ширине | занять всю доступную ширину родителя | `Modifier.fillMaxWidth().height(120.dp)` |
| `Modifier.padding(...)` | отступ | внутренний отступ; порядок в цепочке модификаторов важен | `Modifier.padding(12.dp).background(color)` |
| `Arrangement.SpaceBetween` | развести по краям | прижимает первый элемент к началу, последний — к концу | `Row(horizontalArrangement = Arrangement.SpaceBetween)` |

</Block>

<SectionAnchor chapterId="layout-card" sectionId="cheatsheet" />

## Куда дальше: проверенные ресурсы

<Block kind="cheatsheet" title="Материалы по теме" chapterId="layout-card" blockId="cheatsheet-resources">

- [Замена магического числа символьной константой — Refactoring.Guru](https://refactoring.guru/ru/replace-magic-number-with-symbolic-constant) — классическое объяснение рефакторинга «магическое число → константа» вне контекста Android, тем же приёмом, что применён к `Dimens` в этой главе.
- [Compose layout basics](https://developer.android.com/develop/ui/compose/layouts/basics) — первоисточник примеров про Row, Column и Box.
- [Basic Layouts in Compose (codelab)](https://developer.android.com/codelabs/jetpack-compose-layouts) — пошаговый практикум: собрать целый экран с поиском, карточками и адаптивной навигацией.
- [Compose Modifiers](https://developer.android.com/develop/ui/compose/modifiers) — официальный разбор цепочки модификаторов и того, почему их порядок имеет значение.

</Block>

<SectionAnchor chapterId="layout-card" sectionId="resources" />

## Челлендж ⭐

`ProductCard` уже готов. Дальше — без эмулятора, только чтением и написанием кода: реши обе ступени сам, а потом сверься с решением.

**Ступень 1.** На макете под ценой и кнопкой добавлена ещё одна строка — рейтинг товара: слева текст `"★ 4.8"`, справа — `"132 отзыва"`, разведённые по краям строки точно так же, как цена и кнопка выше. Допиши `ProductCard` вторым по счёту `Row` — последним, четвёртым ребёнком внешнего `Column`, — используя то же значение `Arrangement`, что и в ряду с ценой.

**Ступень 2 ⭐.** Представь, что кто-то в команде решил «сделать константы понадёжнее» и переписал `object Dimens` так:

```kotlin
object Dimens {
    const val CardPadding = 12.dp
    const val ImageHeight = 120.dp
}
```

Объясни своими словами, какую именно ошибку это вызовет и почему `val` здесь работал, а `const val` — нет.

<details>
<summary>Решение (сначала попробуй сам)</summary>

Ступень 1:

```kotlin
Row(
    modifier = Modifier.fillMaxWidth(),
    horizontalArrangement = Arrangement.SpaceBetween
) {
    Text(text = "★ 4.8")
    Text(text = "132 отзыва")
}
```

Этот `Row` — четвёртый и последний ребёнок внешнего `Column` (после `Box`, заголовка и ряда с ценой), значит встанет ниже ряда с ценой и кнопкой; `Arrangement.SpaceBetween` разводит два текста по краям строки той же стратегией, что и раньше.

Ступень 2 ⭐:

Ошибка будет той же, что разобрана в разделе «Типичные ошибки»: `Const 'val' has type 'Dp'. Only primitive types and 'String' are allowed.` — «const val имеет тип Dp. Разрешены только примитивные типы и String». `12.dp` — не число, известное компилятору заранее, а результат вызова свойства-расширения `.dp`, который вычисляется во время выполнения программы; `const val` же требует значения, полностью готового ещё ДО того, как программа запустится. `Int`, `String`, `Boolean` компилятор может вычислить сам на этапе компиляции — а вызов функции, даже такой маленькой, как `.dp`, для него уже не «просто число», а код, который нужно выполнить. Починка — вернуть обычный `val`.

</details>

<SectionAnchor chapterId="layout-card" sectionId="challenge" />

## Проверка по главе

<Block kind="quiz" title="Экзамен главы" chapterId="layout-card" blockId="exam">
<ChapterExam chapterId="layout-card" questions={[
  { q: 'В каком порядке собирают карточку по макету?', options: ['С самого мелкого элемента внутрь наружу', 'Снаружи внутрь: рамка, слои, текст, ряды', 'В произвольном порядке', 'Сначала цвета, потом размеры'], correct: 1, why: 'Тот же порядок, в каком макет читают глазами, и каждый шаг проверяется отдельно.' },
  { q: 'Где в карточке появляется Box?', options: ['Везде вместо Column', 'Там, где на макете одно лежит поверх другого — изображение и бейдж', 'Для ряда с ценой', 'Для заголовка'], correct: 1, why: 'Box кладёт детей друг на друга по порядку вызова.' },
  { q: 'Зачем выносить 12.dp в object Dimens?', options: ['Так быстрее работает', 'Число перестаёт повторяться, у него появляется имя, и правка макета делается в одном месте', 'Так требует Compose', 'Для экономии памяти'], correct: 1, why: 'Имя объясняет смысл: CardPadding понятнее, чем 12 в пятый раз.' },
  { q: 'const val CardPadding = 12.dp даёт ошибку. Почему?', options: ['const val нельзя в object', "12.dp — вызов свойства-расширения, а const val требует значения, известного до запуска", 'Dp не поддерживается', 'Нужен другой импорт'], correct: 1, why: "Сообщение: Const 'val' has type 'Dp'. Only primitive types and 'String' are allowed." },
  { q: 'Чем val отличается от const val для Dp?', options: ['Ничем', 'val вычисляется при создании объекта, во время работы программы — и Dp принимает', 'val медленнее', 'val нельзя в object'], correct: 1, why: 'То же правило действует для Sp, Color и прочих не-примитивов.' },
  { q: 'Modifier.padding(12.dp).background(color) и Modifier.background(color).padding(12.dp) — это', options: ['одно и то же', 'разное: в первом случае фон не заходит под отступ, во втором — заходит', 'ошибка компиляции во втором случае', 'разница только в скорости'], correct: 1, why: 'Порядок в цепочке модификаторов меняет результат — это частая причина расхождения с макетом.' },
  { q: 'Arrangement.SpaceBetween в ряду с ценой и кнопкой', options: ['ставит их вплотную', 'прижимает цену к левому краю, кнопку к правому', 'центрирует оба', 'растягивает кнопку'], correct: 1, why: 'Свободное место уходит в промежуток между элементами, по краям не остаётся ничего.' },
]} />
</Block>

## Что должен уметь

- Собрать карточку по макету в четыре шага: рамка, слои, текст, ряд.
- Понять по макету, где нужен `Box`, а где хватит `Column` или `Row`.
- Прочитать получившийся код построчно и объяснить каждый модификатор.
- Вынести повторяющиеся числа макета в `object` с именованными константами.
- Объяснить, почему для `Dp` и `Sp` нужен `val`, а не `const val`.
- Узнать по сообщению компилятора пять типичных ошибок сборки и починить каждую.

<SectionAnchor chapterId="layout-card" sectionId="what-you-should-know" />
