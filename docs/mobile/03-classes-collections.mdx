---
title: Классы и коллекции
audience: мобилка
level: база
order: 3
---

import ChapterCover from '@site/src/components/ChapterCover';

<ChapterCover chapterId="classes-collections" />

import Figure from '@site/src/components/Figure';
import SyntaxBreakdown from '@site/src/components/SyntaxBreakdown';
import PredictOutput from '@site/src/components/PredictOutput';
import CodeTyping from '@site/src/components/CodeTyping';
import Flashcards from '@site/src/components/Flashcards';
import KotlinPlay from '@site/src/components/KotlinPlay';
import SelfCheck from '@site/src/components/SelfCheck';
import ChapterExam from '@site/src/components/ChapterExam';
import Block from '@site/src/components/Block';
import Hint from '@site/src/components/Hint';
import ToolCard from '@site/src/components/ToolCard';
import CollapsibleToc from '@site/src/components/CollapsibleToc';
import ChapterProgress from '@site/src/components/ChapterProgress';
import SectionAnchor from '@site/src/components/SectionAnchor';
import MapFilterViz from '@site/src/components/MapFilterViz';
import DataClassBuilder from '@site/src/components/DataClassBuilder';

# Классы и коллекции

<ChapterProgress chapterId="classes-collections" totalSections={15} totalQuizzes={6} totalTrainers={6} />

## Зачем это нужно

В главах 1 и 2 мы хранили данные по одному: отдельная переменная `name`, отдельная переменная `score`. Пока участник один, это работает. Но чемпионат «Профессионалы» — это не один участник, а команда. Заводить `name1`, `score1`, `name2`, `score2`, `name3`, `score3` и дальше вручную — плохая идея: код разрастается, а связь между именем и очками конкретного участника нигде не записана явно, её приходится держать в голове по номеру переменной.

Класс (class) решает эту задачу: это чертёж, который группирует связанные данные и поведение в один тип. Вместо `name1` и `score1` мы объявим один класс `Competitor` с полями «имя» и «очки», а дальше будем создавать сколько угодно объектов (object) — конкретных экземпляров (instance) — по этому чертежу. У каждого объекта свои значения полей, но структура одна и та же.

А чтобы работать сразу с несколькими объектами — командой из пяти, десяти, ста участников, — нужны коллекции (collections): `List` для упорядоченного списка, `Map` для пар «ключ — значение», `Set` для набора уникальных значений. И раз участников много, придётся на что-то реагировать по-разному в зависимости от результата — для этого в Kotlin есть `when`, более мощная замена цепочке `if / else if`.

В этой главе разберём: как объявить класс и создать объект, чем `data class` отличается от обычного класса и что именно генерирует слово `data` — увидим это не на словах, а в реальном выводе `toString()` и `equals()` — затем пройдём три основные коллекции и закончим `when`.

<CollapsibleToc toc={toc} chapterId="classes-collections" />

<SectionAnchor chapterId="classes-collections" sectionId="why" />

## Класс и объект: чертёж и вещь по чертежу

Прежде чем писать код, зафиксируем два слова, которые легко перепутать.

- **Класс (class)** — это чертёж, описание. Он не хранит конкретные данные, а описывает, какие данные и какое поведение будут у объектов этого типа. Так же как чертёж дома — не сам дом: по одному чертежу можно построить сколько угодно домов.
- **Объект (object), он же экземпляр (instance)**, — конкретная вещь, построенная по чертежу. У объекта уже есть настоящие значения: не «класс Competitor хранит имя», а «у этого конкретного объекта имя — „Алиса“».

Это то же самое различие, что между типом и значением из главы 1: `String` — тип, `"Алиса"` — значение этого типа. Класс — как бы «тип, который придумываешь сам», а объект — «значение этого типа».

- **Свойство (property)** — данные внутри объекта, объявленные через `val` или `var`. У `Competitor` это будут `name` и `score`.
- **Метод (method)** — функция, привязанная к классу. Мы уже вызывали чужие методы (`.sum()`, `.map { }` из глав 1–2); теперь научимся писать свои.

## Первый класс: `class Competitor(val name: String, var score: Int)`

Вот минимальное объявление класса участника чемпионата. Разберём сигнатуру по символам.

<Block kind="breakdown" title="Разбор по символам: объявление класса" chapterId="classes-collections" blockId="breakdown-class-declaration">
<SyntaxBreakdown parts={[
  { text: 'class ', label: 'ключевое слово', note: 'объявляет класс (class). Как fun объявляет функцию, class объявляет тип. Пробел после него обязателен' },
  { text: 'Competitor', label: 'имя класса', note: 'придумываешь сам. По соглашению имена классов в Kotlin пишут с ЗАГЛАВНОЙ буквы (в отличие от имён переменных и функций) — так их легко отличить в коде' },
  { text: '(', label: 'открывающая скобка', note: 'начинает основной конструктор (primary constructor) — список того, что нужно передать, чтобы создать объект этого класса' },
  { text: 'val name: String', label: 'свойство-параметр', note: 'это одновременно параметр конструктора И свойство объекта. val перед параметром — новинка этой главы: она превращает обычный параметр в постоянное свойство, доступное как name у любого объекта' },
  { text: ', ', label: 'запятая-разделитель', note: 'отделяет одно свойство от другого, как и у параметров функции' },
  { text: 'var score: Int', label: 'изменяемое свойство-параметр', note: 'var вместо val — то же самое, но значение можно будет менять после создания объекта: bogdan.score = 96' },
  { text: ')', label: 'закрывающая скобка', note: 'конструктор закончился' },
]} />
</Block>

### Что означает каждый символ

- **`class`** — ключевое слово, как `fun` и `val`. Нельзя назвать так переменную или функцию.
- **Заглавная буква в имени класса** — это соглашение (convention), не требование компилятора: код скомпилируется и с `class competitor`, но так в Kotlin не пишут. `Competitor`, `String`, `Int` — все типы начинаются с заглавной, а переменные и функции — с маленькой.
- **`val`/`var` внутри скобок конструктора** — самое важное новое здесь. Без `val`/`var` то, что в скобках, было бы обычным параметром конструктора, доступным только внутри тела класса, но не снаружи через точку. С `val`/`var` это ещё и свойство (property): к нему можно обратиться у любого объекта как `bogdan.name`. Разница между `val` и `var` та же, что и у обычных переменных из главы 1: `val`-свойство нельзя переприсвоить после создания объекта, `var`-свойство — можно.

<KotlinPlay code={`fun main() {
    val alice = Competitor("Алиса", 82)
    val bogdan = Competitor("Богдан", 91)
    println(alice.name)
    println(bogdan.score)
}

class Competitor(val name: String, var score: Int)`} />

### Разбор по строкам

- **Строка 2: `val alice = Competitor("Алиса", 82)`** — создание объекта (instantiation): имя класса используется как вызов функции, аргументы в скобках попадают в свойства `name` и `score` по порядку, как обычные аргументы функции из главы 2. Слева — `val alice`: переменная, в которой лежит ссылка на новый объект. Заметь, что `Competitor` здесь пишется без `new` — в отличие от некоторых других языков (например, Java), в Kotlin создание объекта — это просто вызов имени класса.
- **Строка 3** — то же самое для `bogdan`, с другими значениями свойств.
- **Строка 4: `println(alice.name)`** — точка после `alice` работает так же, как точка перед методом из главы 1 (`listOf(1, 2).sum()`), только здесь после неё не вызов метода, а обращение к свойству: «возьми свойство `name` у объекта `alice`».
- **Строка 5: `println(bogdan.score)`** — то же самое для `score` объекта `bogdan`.
- **Строка 8: `class Competitor(val name: String, var score: Int)`** — само объявление класса. Как и с функциями в главе 2, класс можно объявить после места, где он используется: компилятор сначала видит все объявления в файле, а потом проверяет тела.

**Что выведет:**
```text
Алиса
91
```
Первая строка — свойство `name` объекта `alice`, вторая — свойство `score` объекта `bogdan`. Каждый объект хранит свою собственную копию свойств: значения `alice` никак не влияют на `bogdan`.

<Figure scheme="class-blueprint-instances" caption="Один класс — чертёж; alice и bogdan — конкретные объекты по этому чертежу, каждый со своими значениями полей." />

Раз `score` объявлен через `var`, его можно менять после создания объекта — снаружи, через точку, точно так же, как меняли обычную `var`-переменную в главе 1.

<KotlinPlay code={`fun main() {
    val bogdan = Competitor("Богдан", 91)
    bogdan.score += 5
    println(bogdan.score)
}

class Competitor(val name: String, var score: Int)`} />

### Разбор по строкам

- **Строка 3: `bogdan.score += 5`** — тот же оператор `+=` из главы 1 («прибавь к текущему значению»), только слева от него теперь не голая переменная, а свойство объекта через точку. Работает это только потому, что `score` — `var`: попробуй так же изменить `bogdan.name` — получишь ошибку компиляции, разберём её в разделе «Типичные ошибки».

<Hint id="classes-collections-h1" type="tip">
Заметь: сама переменная `bogdan` объявлена как `val` — и это не противоречие. `val bogdan` означает «ссылка на объект не поменяется» (мы не положим в `bogdan` другой объект), а не «объект нельзя менять изнутри». `val` фиксирует ссылку, а свойства внутри объекта живут своей жизнью, по своим правилам `val`/`var`.
</Hint>

**Что выведет:** `96` — потому что 91 + 5 = 96.

<Block kind="quiz" title="Класс и объект" chapterId="classes-collections" blockId="quiz-class-object">
<SelfCheck chapterId="classes-collections" quizId="q1" questions={[
  { q: 'Что делает val перед параметром в class Competitor(val name: String, var score: Int)?', options: ['Ничего не меняет по сравнению с обычным параметром', 'Превращает параметр конструктора в свойство объекта, доступное снаружи через точку', 'Запрещает создавать объекты этого класса', 'Делает name типом класса'], correct: 1, why: 'val/var в скобках основного конструктора — не просто параметр, а объявление свойства: к нему можно обратиться как alice.name у любого созданного объекта.' },
  { q: 'Почему можно написать val bogdan = Competitor(...), а потом bogdan.score += 5, если bogdan — val?', options: ['Это ошибка, так писать нельзя', 'val bogdan фиксирует ссылку на объект, а не запрещает менять var-свойства внутри него', '+= работает только с val', 'score на самом деле не свойство, а отдельная переменная'], correct: 1, why: 'val у переменной bogdan означает «нельзя положить в bogdan другой объект». Свойства внутри объекта подчиняются своим собственным val/var, объявленным в классе.' },
  { q: 'Что выведет println(alice.name), если alice = Competitor("Алиса", 82)?', options: ['Competitor', 'Алиса', 'name', 'Ошибку компиляции'], correct: 1, why: 'Точка после объекта читает его свойство. alice.name — это значение, переданное первым аргументом при создании объекта, — строка "Алиса".' },
  { q: 'Почему имя класса Competitor пишут с заглавной буквы, а имя переменной alice — с маленькой?', options: ['Это требование компилятора, иначе ошибка', 'Это соглашение об именовании: типы (классы) — с заглавной, переменные и функции — с маленькой', 'Заглавная буква ускоряет компиляцию', 'Так работает только в Kotlin, в других языках наоборот'], correct: 1, why: 'Это конвенция (code convention), а не правило синтаксиса — код скомпилируется и без неё, но так код читается предсказуемо: сразу видно, где тип, а где значение.' },
]} />
</Block>

<SectionAnchor chapterId="classes-collections" sectionId="first-class" />

## Метод внутри класса и значение по умолчанию

Класс может содержать не только свойства, но и функции — их называют методами (methods), когда они принадлежат классу. Объявляются они так же, через `fun`, только внутри фигурных скобок тела класса — после списка свойств в скобках конструктора.

<KotlinPlay code={`fun main() {
    val newcomer = Competitor("Соня")
    newcomer.addPoints(12)
    newcomer.addPoints(8)
    println("\${newcomer.name}: \${newcomer.score}")
}

class Competitor(val name: String, var score: Int = 0) {
    fun addPoints(points: Int) {
        score += points
    }
}`} />

### Разбор по строкам

- **Строка 8: `class Competitor(val name: String, var score: Int = 0) {`** — у `score` появилось значение по умолчанию (default argument) — то же самое `= 0`, что мы разбирали для параметров функций в главе 2. Раз оно есть, `score` можно не передавать при создании объекта. После скобок конструктора появилась `{` — тело класса: место для методов и дополнительной логики.
- **Строка 9: `fun addPoints(points: Int) {`** — метод внутри тела класса. Выглядит как обычная функция из главы 2, только теперь у неё есть доступ к свойствам того объекта, у которого её вызвали.
- **Строка 10: `score += points`** — внутри метода можно обращаться к `score` без `this.` и точки: Kotlin понимает, что раз мы внутри класса `Competitor`, `score` — это свойство текущего объекта. `this` (ключевое слово «этот объект») можно писать явно — `this.score` — но когда путаницы с одноимённым параметром нет, это не обязательно.
- **Строка 2: `val newcomer = Competitor("Соня")`** — создаём объект, передав только `name`. `score` не указан — берётся значение по умолчанию `0`, точно как с параметрами функций.
- **Строки 3–4: `newcomer.addPoints(12)` / `newcomer.addPoints(8)`** — вызов метода: `объект.метод(аргументы)`, тот же синтаксис, что и у чужих методов вроде `.sum()`.

**Что выведет:** `Соня: 20` — начали с `score = 0` (по умолчанию), прибавили 12, потом 8: 0 + 12 + 8 = 20.

<SectionAnchor chapterId="classes-collections" sectionId="method-default" />

## data class: что генерирует ключевое слово `data`

У обычного класса `Competitor` есть неудобство: если попробовать напечатать объект напрямую или сравнить два объекта с одинаковыми полями, результат окажется не таким, как ожидаешь. Слово `data` перед `class` чинит это — компилятор автоматически дописывает за нас несколько методов. Разберём каждый по отдельности, с реальным выводом.

<Block kind="breakdown" title="Разбор по символам: слово data" chapterId="classes-collections" blockId="breakdown-data-keyword">
<SyntaxBreakdown parts={[
  { text: 'data ', label: 'ключевое слово', note: 'ставится перед class. Говорит компилятору: «сгенерируй для этого класса toString(), equals(), hashCode(), copy() и componentN() на основе свойств из основного конструктора»' },
  { text: 'class Competitor(val name: String, var score: Int)', label: 'обычное объявление класса', note: 'всё, что мы уже разобрали выше, не меняется — data только добавляет сгенерированный код, ничего не убирает' },
]} />
</Block>

<Hint id="classes-collections-h2" type="important">
Правило, о котором легко забыть: у `data class` в основном конструкторе должен быть хотя бы один параметр, и каждый параметр обязан быть `val` или `var` — иначе он не станет свойством и не попадёт ни в один из сгенерированных методов.
</Hint>

<Figure scheme="data-class-codegen" caption="Одно слово data перед class — и компилятор сам дописывает toString, equals, hashCode, copy и componentN." />

### toString(): как объект печатает себя

Сначала — что выводит **обычный** класс, если передать объект прямо в `println`, не залезая в свойства по отдельности.

<KotlinPlay code={`fun main() {
    val bogdan = Competitor("Богдан", 91)
    println(bogdan)
}

class Competitor(val name: String, var score: Int)`} />

**Что выведет:** что-то вроде `Competitor@2c7b84de` — имя класса, знак `@` и шестнадцатеричный адрес объекта в памяти. У тебя это будет другое число: адрес каждый раз новый, он ничего не говорит о содержимом объекта. Это стандартный `toString()`, унаследованный от `Any` (базового типа всех классов в Kotlin) — он не знает, что внутри объекта, и просто печатает служебную метку.

Теперь та же программа, только с `data` перед `class`:

<KotlinPlay code={`fun main() {
    val bogdan = Competitor("Богдан", 91)
    println(bogdan)
}

data class Competitor(val name: String, var score: Int)`} />

**Что выведет:** `Competitor(name=Богдан, score=91)` — читаемое представление: имя класса и все свойства с их значениями. Это и есть сгенерированный `toString()` — он написан за нас, потому что мы пометили класс словом `data`.

### equals(): по ссылке или по содержимому

Ещё заметнее разница проявляется при сравнении `==`. Два объекта обычного класса с одинаковыми полями:

<KotlinPlay code={`fun main() {
    val a = Competitor("Богдан", 91)
    val b = Competitor("Богдан", 91)
    println(a == b)
}

class Competitor(val name: String, var score: Int)`} />

**Что выведет:** `false`. У обычного класса `==` по умолчанию сравнивает ссылки (referential equality): «это буквально один и тот же объект в памяти?» `a` и `b` — два разных объекта, просто с одинаковым содержимым полей, поэтому сравнение — `false`, даже несмотря на одинаковые `name` и `score`.

Тот же код, но класс — `data class`:

<KotlinPlay code={`fun main() {
    val a = Competitor("Богдан", 91)
    val b = Competitor("Богдан", 91)
    println(a == b)
}

data class Competitor(val name: String, var score: Int)`} />

**Что выведет:** `true`. Сгенерированный `equals()` сравнивает не ссылки, а содержимое (structural equality): «одинаковы ли значения всех свойств?» У `a` и `b` они совпадают — значит, объекты равны, даже если это два разных объекта в памяти. Вместе с `equals()` компилятор всегда генерирует и согласованный `hashCode()` — это важно, если объекты потом кладут в `Set` или используют как ключи `Map` (увидим дальше в главе): без согласованных `equals()`/`hashCode()` такие коллекции будут вести себя непредсказуемо.

### copy(): новый объект с изменённым полем

У `data class` появляется ещё один сгенерированный метод — `copy()`: создаёт новый объект на основе старого, позволяя заменить только нужные свойства.

<KotlinPlay code={`fun main() {
    val bogdan = Competitor("Богдан", 91)
    val bogdanNextRound = bogdan.copy(score = 96)
    println(bogdan)
    println(bogdanNextRound)
}

data class Competitor(val name: String, var score: Int)`} />

### Разбор по строкам

- **Строка 3: `bogdan.copy(score = 96)`** — `copy()` принимает именованные аргументы (named arguments, глава 2) для тех свойств, которые нужно поменять; остальные копируются как есть. Здесь передан только `score` — значит, `name` в новом объекте останется тем же, `"Богдан"`.
- Важно: `copy()` создаёт **новый** объект, `bogdan` не меняется. Это видно по выводу: `bogdan` остался со `score = 91`, а `bogdanNextRound` — отдельный объект со `score = 96`.

**Что выведет:**
```text
Competitor(name=Богдан, score=91)
Competitor(name=Богдан, score=96)
```

Проверь себя на связке `toString()` + `copy()` — сначала предскажи обе строки вывода, потом запускай:

<Block kind="trainer" title="Предскажи вывод: toString и copy" chapterId="classes-collections" blockId="trainer-predict-data-copy">
<PredictOutput kotlin expected={'Competitor(name=Богдан, score=96)\n91'} chapterId="classes-collections" trainerId="trainer-predict-data-copy" code={`fun main() {
    val bogdan = Competitor("Богдан", 91)
    val next = bogdan.copy(score = 96)
    println(next)
    println(bogdan.score)
}

data class Competitor(val name: String, var score: Int)`} />
</Block>

### Бонус: деструктуризация через componentN()

Последний сгенерированный кусок — функции `component1()`, `component2()` и так далее, по одной на каждое свойство основного конструктора, в порядке объявления. Их редко вызывают напрямую — вместо этого есть удобный синтаксис: разложить объект на отдельные переменные одной строкой.

<KotlinPlay code={`fun main() {
    val bogdan = Competitor("Богдан", 91)
    val (name, score) = bogdan
    println("$name -> $score")
}

data class Competitor(val name: String, var score: Int)`} />

**Что выведет:** `Богдан -> 91`. `val (name, score) = bogdan` — деструктуризация (destructuring declaration): `name` берётся из `component1()` (первое свойство, `name`), `score` — из `component2()` (второе свойство, `score`). Работает это только благодаря `data`: у обычного класса `component1()`/`component2()` не сгенерированы, и такая запись не скомпилируется.

Теперь собери свой `data class` сам: задай имя и поля — и посмотри живьём, что дадут сгенерированные `toString()`, `equals()` и `copy()`.

<Block kind="trainer" title="Собери свой data class" chapterId="classes-collections" blockId="trainer-data-class-builder">
<DataClassBuilder chapterId="classes-collections" trainerId="trainer-data-class-builder" />
</Block>

<Block kind="quiz" title="data class" chapterId="classes-collections" blockId="quiz-data-class">
<SelfCheck chapterId="classes-collections" quizId="q2" questions={[
  { q: 'Что печатает println(bogdan) для обычного (не data) класса?', options: ['Все свойства объекта в виде текста', 'Имя класса, @ и адрес объекта в памяти вроде Competitor@2c7b84de', 'Пустую строку', 'Ошибку компиляции — println нельзя вызывать для объектов'], correct: 1, why: 'Обычный класс наследует стандартный toString() от Any, который не знает содержимого объекта и печатает только служебную метку с адресом.' },
  { q: 'Почему a == b выводит false для обычного класса, даже если у a и b одинаковые name и score?', options: ['== вообще не работает для классов', 'У обычного класса == по умолчанию сравнивает ссылки: это разные объекты в памяти, даже с одинаковым содержимым', 'Kotlin сравнивает только первое свойство', 'Это баг компилятора'], correct: 1, why: 'Без data == проверяет referential equality — «один и тот же объект?». a и b — два разных объекта, значит false, независимо от содержимого полей.' },
  { q: 'Что делает bogdan.copy(score = 96)?', options: ['Меняет score у самого bogdan на 96', 'Создаёт новый объект с тем же name, но score = 96; bogdan не меняется', 'Удаляет bogdan и заменяет его новым объектом', 'Работает только у обычных классов, не у data class'], correct: 1, why: 'copy() — сгенерированный метод data class: возвращает новый объект, копируя все свойства, кроме явно переданных именованных аргументов.' },
  { q: 'Что обязательно должно быть верно для параметров основного конструктора data class?', options: ['Все они должны быть типа String', 'Каждый параметр должен быть val или var, и конструктор не может быть пустым', 'У них не может быть значений по умолчанию', 'Их не может быть больше двух'], correct: 1, why: 'Параметр без val/var не становится свойством и не попадёт в toString/equals/copy; пустой конструктор у data class запрещён компилятором.' },
]} />
</Block>

<SectionAnchor chapterId="classes-collections" sectionId="data-class" />

## Списки: `List` и `MutableList`

Теперь, когда есть тип для одного участника, соберём их в команду. `List` — упорядоченная коллекция элементов одного типа: у каждого элемента есть индекс (номер позиции), начиная с `0`.

<Figure scheme="collections-shelf" caption="Три полки для данных: List хранит по порядку и номеру, Map — по ключу, Set — только уникальные значения без порядка." />

<Block kind="breakdown" title="Разбор по символам: цикл for" chapterId="classes-collections" blockId="breakdown-for-loop">
<SyntaxBreakdown parts={[
  { text: 'for ', label: 'ключевое слово', note: 'начинает цикл (loop) — блок кода, который выполняется многократно, по одному разу на каждый элемент' },
  { text: '(', label: 'открывающая скобка', note: 'начинает заголовок цикла' },
  { text: 'name', label: 'переменная цикла', note: 'имя, которое ты придумываешь сам; на каждом шаге цикла в него кладётся очередной элемент коллекции' },
  { text: ' in ', label: 'ключевое слово in', note: 'читается «в»: «name — очередной элемент ИЗ names». Тот же самый in встретится дальше в этой главе ещё в одной роли — в when' },
  { text: 'names', label: 'коллекция', note: 'список, по которому идём. После in всегда стоит что-то, что можно перебрать: список, множество, диапазон' },
  { text: ')', label: 'закрывающая скобка', note: 'заголовок цикла закончился' },
  { text: ' {', label: 'начало тела цикла', note: 'фигурные скобки — то же самое, что тело функции: блок кода, который выполнится на каждой итерации' },
]} />
</Block>

<KotlinPlay code={`fun main() {
    val names = listOf("Алиса", "Богдан", "Соня")
    for (name in names) {
        println(name)
    }
    println(names[1])
    println(names.size)
}`} />

### Разбор по строкам

- **Строка 2: `listOf("Алиса", "Богдан", "Соня")`** — функция-фабрика (как `mapOf`/`setOf` дальше в главе): создаёт `List<String>` — список строк. Список, созданный через `listOf`, — read-only: в него нельзя ни добавить, ни убрать элемент (попробуем и увидим ошибку компиляции в разделе «Типичные ошибки»).
- **Строки 3–5: `for (name in names) { println(name) }`** — цикл проходит по всем трём элементам по очереди: сначала `name = "Алиса"`, затем `name = "Богдан"`, затем `name = "Соня"`, печатая каждый.
- **Строка 6: `names[1]`** — новый символ, квадратные скобки `[ ]` — оператор индексации (index operator): достаёт элемент списка по номеру позиции. Отсчёт всегда идёт с нуля: `names[0]` — «Алиса», `names[1]` — «Богдан», `names[2]` — «Соня». `names[1]` — это не то же самое, что «второй по счёту» в обычной речи, а «элемент под индексом 1».
- **Строка 7: `names.size`** — свойство `size` (без скобок — это не метод, а свойство, готовое значение) хранит количество элементов.

**Что выведет:**
```text
Алиса
Богдан
Соня
Богдан
3
```
Первые три строки — цикл, четвёртая — `names[1]` («Богдан» под индексом 1), пятая — `names.size` (в списке три элемента).

Списки можно строить и из своих классов — например, из `Competitor`, а не только из строк и чисел. Тогда к каждому элементу применимы уже знакомые из главы 1 `map`/`filter`/`sum`.

<KotlinPlay code={`fun main() {
    val team = listOf(
        Competitor("Алиса", 82),
        Competitor("Богдан", 91),
        Competitor("Соня", 77)
    )
    val totalScore = team.map { it.score }.sum()
    println(totalScore)
}

data class Competitor(val name: String, var score: Int)`} />

### Разбор по строкам

- **Строки 2–6: `val team = listOf(...)`** — список из трёх объектов `Competitor`, каждый со своим именем и очками. Список можно писать в несколько строк — запятая после каждого элемента, кроме последнего, работает так же, как и в одну строку.
- **Строка 7: `team.map { it.score }.sum()`** — та же цепочка `map`/`sum` из главы 1, только теперь `it` — не число, а целый объект `Competitor`. `it.score` достаёт из каждого объекта одно свойство, `map` собирает из этих свойств новый список чисел, `sum()` складывает их.

**Что выведет:** `250` — потому что 82 + 91 + 77 = 250.

Та же команда, но теперь цепочка `filter`/`map` из главы 1 — предскажи, какой список напечатается:

<Block kind="trainer" title="Предскажи вывод: filter и map по команде" chapterId="classes-collections" blockId="trainer-predict-filter-map">
<PredictOutput kotlin expected={'[Алиса, Богдан]'} chapterId="classes-collections" trainerId="trainer-predict-filter-map" code={`fun main() {
    val team = listOf(
        Competitor("Алиса", 82),
        Competitor("Богдан", 91),
        Competitor("Соня", 77)
    )
    val qualified = team.filter { it.score >= 80 }.map { it.name }
    println(qualified)
}

data class Competitor(val name: String, var score: Int)`} />
</Block>

Та же цепочка — теперь в замедленной съёмке: прогони каждый элемент через конвейер `filter`/`map` по шагам и посмотри, где элемент отбрасывается, а где превращается.

<Block kind="trainer" title="Конвейер map и filter" chapterId="classes-collections" blockId="trainer-map-filter-viz">
<MapFilterViz chapterId="classes-collections" trainerId="trainer-map-filter-viz" />
</Block>

Список из `listOf` — неизменяемый (в том смысле, что через сам этот `List` нельзя ни добавить, ни удалить элемент). Когда список должен расти и меняться, используют `mutableListOf` — тот же список, но с методами `add`/`remove`.

<KotlinPlay code={`fun main() {
    val roster = mutableListOf("Алиса", "Богдан")
    roster.add("Соня")
    roster.remove("Алиса")
    println(roster)
}`} />

### Разбор по строкам

- **Строка 2: `mutableListOf("Алиса", "Богдан")`** — создаёт `MutableList<String>`: список с теми же методами, что у обычного `List` (`size`, `[индекс]`, `map`, `filter`...), плюс методы изменения.
- **Строка 3: `roster.add("Соня")`** — добавляет элемент в конец списка.
- **Строка 4: `roster.remove("Алиса")`** — убирает из списка первое вхождение значения `"Алиса"` (не по индексу, а по значению).
- **Строка 5: `println(roster)`** — списки печатаются в квадратных скобках через запятую — то же текстовое представление, что мы уже видели в главе 1 у результата `map`.

**Что выведет:** `[Богдан, Соня]` — «Соня» добавилась в конец, «Алиса» удалилась, порядок остальных элементов сохранился.

<Block kind="quiz" title="Списки" chapterId="classes-collections" blockId="quiz-lists">
<SelfCheck chapterId="classes-collections" quizId="q3" questions={[
  { q: 'Что означает for (name in names)?', options: ['Проверяет, есть ли name внутри names', 'Присваивает name каждый элемент коллекции names по очереди и выполняет тело цикла для каждого', 'Создаёт новую переменную names', 'Удаляет name из списка names'], correct: 1, why: 'in в заголовке цикла перебирает коллекцию: на каждой итерации name получает следующий элемент names.' },
  { q: 'Чем names[1] отличается от «второго элемента» в обычной речи?', options: ['Ничем, это одно и то же', 'Индексация начинается с 0, поэтому names[1] — это элемент под номером два по счёту, а не первый', 'names[1] всегда возвращает первый элемент', '[ ] используется только для строк, не для списков'], correct: 1, why: 'Индексы в Kotlin (как и в большинстве языков) начинаются с 0: names[0] — первый элемент, names[1] — второй.' },
  { q: 'Чем listOf(...) отличается от mutableListOf(...)?', options: ['listOf быстрее работает', 'У списка из listOf нет методов add/remove — он только для чтения, mutableListOf добавляет их', 'mutableListOf нельзя перебрать циклом for', 'Это два названия одной и той же функции'], correct: 1, why: 'List — интерфейс только для чтения, MutableList расширяет его методами изменения содержимого: add, remove и другими.' },
  { q: 'Что выведет team.map { it.score }.sum(), если team — список из трёх объектов Competitor?', options: ['Список из трёх объектов Competitor', 'Список из трёх чисел score', 'Одно число — сумму всех score', 'Ошибку компиляции: у it нет свойства score'], correct: 2, why: 'map { it.score } достаёт из каждого объекта поле score и собирает список чисел, а sum() складывает их в одно число.' },
]} />
</Block>

<SectionAnchor chapterId="classes-collections" sectionId="lists" />

## Map: пары ключ — значение

`Map` хранит не просто значения, а пары «ключ → значение» (key-value pair): вместо доступа по номеру позиции, как у `List`, доступ идёт по ключу — например, по имени участника.

<Block kind="breakdown" title="Разбор по символам: mapOf и пара to" chapterId="classes-collections" blockId="breakdown-mapof">
<SyntaxBreakdown parts={[
  { text: 'mapOf', label: 'функция-фабрика карты', note: 'создаёт Map — коллекцию пар «ключ → значение». Как listOf для списков и setOf для множеств дальше в главе' },
  { text: '(', label: 'открывающая скобка', note: 'начало списка пар' },
  { text: '"Алиса" to 82', label: 'пара ключ-значение', note: 'to — инфиксная функция (infix function): пишется между двумя значениями без точки и скобок, как оператор. "Алиса" to 82 создаёт Pair("Алиса", 82) — «Алиса» станет ключом, 82 — значением' },
  { text: ', ', label: 'запятая', note: 'отделяет одну пару от другой' },
  { text: '"Богдан" to 91', label: 'ещё одна пара', note: 'то же самое: строка слева от to — ключ, число справа — значение' },
  { text: ')', label: 'закрывающая скобка', note: 'список пар закончился, mapOf собирает из них Map' },
]} />
</Block>

<KotlinPlay code={`fun main() {
    val scores = mapOf("Алиса" to 82, "Богдан" to 91, "Соня" to 77)
    println(scores["Богдан"])
    for ((name, score) in scores) {
        println("$name: $score")
    }
}`} />

### Разбор по строкам

- **Строка 2** — создание карты: три пары «имя → очки».
- **Строка 3: `scores["Богдан"]`** — тот же оператор индексации `[ ]`, что и у списков, только теперь в скобках не номер позиции, а ключ. Возвращает значение, связанное с этим ключом.
- **Строка 4: `for ((name, score) in scores)`** — перебор карты: на каждой итерации `for` даёт не один элемент, а пару, которую сразу деструктурируют (та же деструктуризация, что мы видели у `data class`) на `name` (ключ) и `score` (значение).
- **Строка 5: `println("$name: $score")`** — строковый шаблон из главы 1, теперь с двумя подстановками подряд.

**Что выведет:**
```text
91
Алиса: 82
Богдан: 91
Соня: 77
```
Первая строка — прямой доступ по ключу `"Богдан"`. Дальше — цикл: `mapOf` сохраняет порядок, в котором пары были перечислены при создании, поэтому вывод идёт именно в порядке Алиса → Богдан → Соня, а не в каком-то случайном.

Как и у `List`, есть изменяемая версия — `MutableMap`, с методами добавления и обновления записей.

<KotlinPlay code={`fun main() {
    val scores = mutableMapOf("Алиса" to 82, "Богдан" to 91)
    scores["Соня"] = 77
    scores["Алиса"] = 85
    println(scores)
    println(scores.getOrDefault("Дима", 0))
}`} />

### Разбор по строкам

- **Строка 3: `scores["Соня"] = 77`** — новая роль тех же квадратных скобок: индексация слева от `=` не читает значение, а кладёт его в карту. Если ключа `"Соня"` ещё не было — добавляет новую пару; если был бы — заменил бы значение.
- **Строка 4: `scores["Алиса"] = 85`** — ключ `"Алиса"` уже существовал (со значением 82), поэтому это не добавление, а замена: новое значение 85 затирает старое 82.
- **Строка 6: `scores.getOrDefault("Дима", 0)`** — безопасный способ спросить значение по ключу, которого может не быть. Если бы мы написали просто `scores["Дима"]`, то получили бы `null` («ничего») — про `null` в этом курсе будет отдельная глава, а пока `getOrDefault` обходит эту проблему: если ключа нет, возвращает второй аргумент как запасное значение.

**Что выведет:**
```text
{Алиса=85, Богдан=91, Соня=77}
0
```
Карта печатается в фигурных скобках, пары через запятую в формате `ключ=значение`. `"Алиса"` теперь `85`, а не `82` — значение заменилось. Для несуществующего ключа `"Дима"` вернулся запасной `0`.

<SectionAnchor chapterId="classes-collections" sectionId="map" />

## Set: только уникальные значения

`Set` — коллекция без повторов: если добавить значение, которое уже есть, ничего не произойдёт — набор просто не станет длиннее.

<KotlinPlay code={`fun main() {
    val uniqueScores = setOf(82, 91, 82, 77, 91)
    println(uniqueScores)
    println(uniqueScores.size)
}`} />

### Разбор по строкам

- **Строка 2: `setOf(82, 91, 82, 77, 91)`** — передали пять чисел, но `82` и `91` встречаются дважды. `Set` хранит только уникальные значения, поэтому повторы схлопываются в один экземпляр каждое.
- **Строка 3–4** — печать: `Set`, как и `List`, выводится в квадратных скобках, порядок — тот, в котором значения встретились впервые.

**Что выведет:**
```text
[82, 91, 77]
3
```
Из пяти переданных чисел осталось три уникальных: `82` и `91` каждое учтено один раз, несмотря на то что были указаны дважды. Как и у списков и карт, есть `mutableSetOf` с методами `add`/`remove` — работает по тем же правилам, что и `mutableListOf`.

<Hint id="classes-collections-h3" type="important">
Именно здесь особенно важна связка `equals()`/`hashCode()` из раздела про `data class`: `Set` и ключи `Map` определяют уникальность через них. У `data class` она согласована автоматически — два структурно одинаковых объекта `Competitor` в `Set` схлопнутся в один, как схлопнулись одинаковые числа выше. У обычного класса это не сработает: два объекта с одинаковыми полями будут считаться разными, потому что сравниваются по ссылке.
</Hint>

<Block kind="quiz" title="Map и Set" chapterId="classes-collections" blockId="quiz-map-set">
<SelfCheck chapterId="classes-collections" quizId="q4" questions={[
  { q: 'Что делает "Алиса" to 82 в mapOf("Алиса" to 82)?', options: ['Складывает строку и число', 'Создаёт пару ключ-значение (Pair): "Алиса" — ключ, 82 — значение', 'Сравнивает "Алиса" с 82', 'Это ошибка синтаксиса'], correct: 1, why: 'to — инфиксная функция, создающая Pair. В mapOf такие пары становятся записями карты: слева ключ, справа значение.' },
  { q: 'Что делает scores["Соня"] = 77 для MutableMap?', options: ['Читает значение по ключу "Соня"', 'Добавляет новую запись или заменяет значение для ключа "Соня" на 77', 'Удаляет ключ "Соня"', 'Работает только для MutableList, не для карт'], correct: 1, why: 'Индексация слева от = — запись в карту: если ключа не было, добавляется новая пара, если был — старое значение заменяется.' },
  { q: 'Что выведет setOf(82, 91, 82, 77, 91)?', options: ['[82, 91, 82, 77, 91] — все пять чисел', '[82, 91, 77] — только уникальные значения', 'Ошибку компиляции: Set не принимает повторы', '82 — только первое число'], correct: 1, why: 'Set хранит уникальные значения: повторяющиеся 82 и 91 схлопываются, остаётся три уникальных числа в порядке первого появления.' },
  { q: 'Почему getOrDefault("Дима", 0) безопаснее, чем scores["Дима"], если ключа "Дима" нет?', options: ['getOrDefault работает быстрее', 'scores["Дима"] вернёт null, а getOrDefault сразу даёт запасное значение вместо null', 'getOrDefault добавляет "Дима" в карту автоматически', 'Разницы нет, это синонимы'], correct: 1, why: 'Доступ по отсутствующему ключу через [ ] возвращает null («ничего»); getOrDefault сразу подставляет второй аргумент как безопасную замену.' },
]} />
</Block>

<SectionAnchor chapterId="classes-collections" sectionId="set" />

## when: замена цепочке if / else if

Когда вариантов больше двух, цепочка `if / else if / else` быстро становится длинной и трудночитаемой. `when` — конструкция Kotlin для точно такой задачи: выбрать один вариант из нескольких.

<Block kind="breakdown" title="Разбор по символам: when без аргумента" chapterId="classes-collections" blockId="breakdown-when-conditions">
<SyntaxBreakdown parts={[
  { text: 'when ', label: 'ключевое слово', note: 'начинает многоветвевую проверку условий (multi-branch conditional). Здесь — без аргумента в скобках, поэтому каждая ветка сама по себе — независимое условие' },
  { text: '{', label: 'открывающая фигурная скобка', note: 'начинает список веток. Та же роль, что у тела функции или лямбды — блок, ограниченный { }' },
  { text: 'score >= 90 -> ', label: 'условие и стрелка', note: 'если условие слева от -> истинно (true), выполняется то, что справа. -> здесь — та же стрелка, что отделяла параметры лямбды от тела в главе 2, только в новой роли: «если — то»' },
  { text: '"Золото"', label: 'результат ветки', note: 'значение, которое даст эта ветка, если её условие оказалось первым истинным' },
  { text: 'else -> ', label: 'ветка по умолчанию', note: 'сработает, если ни одно из условий выше не подошло. Когда when используется как выражение (присваивается переменной), else обязателен — иначе компилятор не может гарантировать результат на все случаи' },
  { text: '}', label: 'закрывающая фигурная скобка', note: 'список веток закончился' },
]} />
</Block>

<KotlinPlay code={`fun main() {
    val score = 91
    val level = when {
        score >= 90 -> "Золото"
        score >= 75 -> "Серебро"
        score >= 50 -> "Бронза"
        else -> "Участие"
    }
    println(level)
}`} />

### Разбор по строкам

- **Строка 3: `val level = when { ... }`** — `when` используется как выражение (expression): результат кладётся прямо в переменную — так же, как в главе 2 в переменную попадал результат функции с телом-выражением.
- **Строки 4–6** — ветки проверяются по порядку сверху вниз. `when` без аргумента в скобках — это по сути список независимых условий: как только одно оказывается истинным, выполняется его ветка, а остальные — даже не проверяются.
- Для `score = 91`: первое условие `score >= 90` уже истинно, поэтому результат — `"Золото"`, а до `score >= 75` дело даже не доходит.

**Что выведет:** `Золото`.

Когда сравнивают одно значение с набором конкретных вариантов, а не с диапазонами, `when` можно писать с аргументом в скобках — тогда каждая ветка сравнивается с этим значением напрямую, а не пишет своё условие заново.

<Block kind="breakdown" title="Разбор по символам: when с аргументом" chapterId="classes-collections" blockId="breakdown-when-subject">
<SyntaxBreakdown parts={[
  { text: 'when (place) ', label: 'when с аргументом', note: 'place — значение, которое будет сравниваться с каждой веткой. В скобках после when — то, что мы разбираем, как разбирали x в главе 1' },
  { text: '1 -> "Золото"', label: 'ветка с одним значением', note: 'сработает, если place равно 1' },
  { text: '2, 3 -> "Призёр"', label: 'ветка с несколькими значениями', note: 'запятая между значениями — ветка срабатывает, если place равно ЛЮБОМУ из перечисленных: 2 или 3' },
  { text: 'in 4..10 -> "Финалист"', label: 'проверка диапазона', note: 'in здесь — тот же самый in, что мы видели в for (name in names), но в новой роли: «place входит в диапазон 4..10?». 4..10 — диапазон (range), то есть все целые числа от 4 до 10 включительно; в Kotlin-коде курса такая запись встречается здесь впервые' },
]} />
</Block>

<KotlinPlay code={`fun main() {
    val place = 2
    val medal = when (place) {
        1 -> "Золото"
        2, 3 -> "Призёр"
        in 4..10 -> "Финалист"
        else -> "Участник"
    }
    println(medal)
}`} />

### Разбор по строкам

- **Строка 2: `val place = 2`** — участник занял второе место.
- **Строка 4: `2, 3 -> "Призёр"`** — `place` сравнивается с каждым значением через запятую; `place == 2` истинно, значит эта ветка и сработает.
- Строки `1 -> ...` выше и `in 4..10 -> ...` ниже даже не проверяются дальше — `when` останавливается на первой подошедшей ветке, как и версия без аргумента.

**Что выведет:** `Призёр`.

<Block kind="quiz" title="when" chapterId="classes-collections" blockId="quiz-when">
<SelfCheck chapterId="classes-collections" quizId="q5" questions={[
  { q: 'Чем when { score >= 90 -> ... } (без скобок после when) отличается от when (place) { 1 -> ... }?', options: ['Ничем, это одна и та же конструкция', 'Без аргумента каждая ветка — своё независимое условие; с аргументом каждая ветка сравнивается с этим значением', 'Без аргумента when нельзя использовать как выражение', 'Версия с аргументом работает только с числами'], correct: 1, why: 'when без скобок — список произвольных булевых условий. when (значение) — сравнение одного значения с каждой веткой по очереди.' },
  { q: 'Что делает ветка 2, 3 -> "Призёр" в when (place) { ... }?', options: ['Сработает, только если place равно и 2, и 3 одновременно', 'Сработает, если place равно 2 ИЛИ 3', 'Это синтаксическая ошибка — нельзя перечислять значения через запятую', 'Сработает для любого числа от 2 до 3 с шагом 0.5'], correct: 1, why: 'Запятая между значениями в одной ветке — это «любое из перечисленных»: ветка выполнится, если значение совпало хотя бы с одним.' },
  { q: 'Зачем нужна ветка else, если when используется как выражение (val x = when { ... })?', options: ['Не нужна никогда, это стиль', 'Компилятор должен гарантировать, что when вернёт значение при любых условиях — else покрывает все случаи, не описанные явно', 'else ускоряет выполнение', 'else обязателен только для чисел'], correct: 1, why: 'Когда результат when кладётся в переменную, у выражения должно быть значение на любой вход. Без else компилятор не может это гарантировать.' },
  { q: 'Что проверяет in 4..10 в ветке when?', options: ['Есть ли переменная 4..10', 'Входит ли сравниваемое значение в диапазон от 4 до 10 включительно', 'Равно ли значение строке "4..10"', 'Это ошибка — in работает только в for'], correct: 1, why: 'in переиспользуется и для циклов (перебор коллекции), и для проверки диапазона: «значение принадлежит range 4..10?».' },
]} />
</Block>

<SectionAnchor chapterId="classes-collections" sectionId="when" />

## Типичные ошибки

Все сообщения ниже сняты вживую с той же песочницы Kotlin Playground, что работает в кнопках Run этой главы — тексты приведены ровно такими, какими их показывает компилятор.

### 1. Не передан аргумент конструктора

<KotlinPlay code={`fun main() {
    val newcomer = Competitor("Дима")
    println(newcomer.score)
}

class Competitor(val name: String, var score: Int)`} />

```text
No value passed for parameter 'score'.
```

**Перевод:** «Не передано значение для параметра score». У `score` в этом классе нет значения по умолчанию — он обязателен, а конструктор вызван только с одним аргументом.

**Как починить:** передать оба аргумента — `Competitor("Дима", 0)`, — либо дать `score` значение по умолчанию в объявлении класса: `var score: Int = 0`.

### 2. Переприсваивание `val`-свойства

<KotlinPlay code={`fun main() {
    val bogdan = Competitor("Богдан", 91)
    bogdan.name = "Николай"
}

class Competitor(val name: String, var score: Int)`} />

```text
'val' cannot be reassigned.
```

**Перевод:** «val нельзя переприсвоить». Та же ошибка, что мы видели в главе 1 для обычной переменной — только теперь `name` не отдельная переменная, а `val`-свойство объекта. Правило то же: `val` фиксируется один раз и навсегда, у него просто нет сеттера (setter) — функции, которая могла бы записать новое значение.

**Как починить:** если имя участника действительно должно меняться — объявить свойство как `var name: String`. Если нет — не переприсваивать, а создать новый объект, например через `.copy(name = "Николай")`, если класс — `data class`.

### 3. `add()` у `List`, а не у `MutableList`

<KotlinPlay code={`fun main() {
    val names: List<String> = listOf("Алиса", "Богдан")
    names.add("Соня")
}`} />

```text
Unresolved reference 'add'.
```

**Перевод:** «Неразрешённая ссылка add». `List` — интерфейс только для чтения: у него нет метода `add`, он просто не существует у этого типа, поэтому компилятор его не находит — так же, как не нашёл бы опечатку в имени переменной.

**Как починить:** объявить список как изменяемый — `val names: MutableList<String> = mutableListOf("Алиса", "Богдан")`, — и тогда `add` появится.

### 4. `when`-выражение без `else`

<KotlinPlay code={`fun main() {
    val place = 2
    val medal = when (place) {
        1 -> "Золото"
        2 -> "Серебро"
    }
    println(medal)
}`} />

```text
'when' expression must be exhaustive. Add an 'else' branch.
```

**Перевод:** «when-выражение должно быть исчерпывающим (exhaustive). Добавьте ветку else». `place` — обычный `Int`, у него бесконечно много возможных значений, а перечислены только `1` и `2`. Раз результат `when` кладётся в `val medal`, компилятору нужна гарантия, что значение появится при любом `place`, — без `else` такой гарантии нет.

**Как починить:** добавить `else -> "Участник"` последней веткой.

### 5. `Map` возвращает значение, которого может не быть

<KotlinPlay code={`fun main() {
    val scores = mapOf("Алиса" to 82, "Богдан" to 91)
    val aliceScore: Int = scores["Алиса"]
    println(aliceScore)
}`} />

```text
Initializer type mismatch: expected 'Int', actual 'Int?'.
```

**Перевод:** «Несовпадение типа начального значения: ожидался Int, а по факту Int?». Обращение к карте по ключу через `[ ]` всегда возвращает «может быть Int, а может быть ничего» (`Int?` — nullable-тип, потому что ключа могло и не быть) — даже если ключ в этот раз точно есть. Компилятор судит по типу, а не по конкретным данным, а тип `scores["Алиса"]` — всегда `Int?`, не `Int`.

**Как починить:** либо изменить тип переменной на `Int?` — `val aliceScore: Int? = scores["Алиса"]`, — либо использовать `getOrDefault("Алиса", 0)` или оператор `!!` (разберём в будущей главе про null-безопасность), если ты точно уверен, что ключ есть.

<Figure scheme="nullable-box" caption="Обращение к Map по ключу возвращает Int?, а не Int: значение может быть, а может — не быть, и тип честно об этом предупреждает." />

<Block kind="quiz" title="Типичные ошибки" chapterId="classes-collections" blockId="quiz-typical-errors">
<SelfCheck chapterId="classes-collections" quizId="q6" questions={[
  { q: 'Почему Competitor("Дима") выдаёт "No value passed for parameter \'score\'."?', options: ['Класс Competitor вообще нельзя создавать с одним аргументом', 'У score нет значения по умолчанию, а конструктор требует оба аргумента', 'Дима — недопустимое имя', 'Это предупреждение, а не ошибка'], correct: 1, why: 'score объявлен без = значение по умолчанию, значит он обязателен при каждом вызове конструктора.' },
  { q: 'Почему bogdan.name = "Николай" не компилируется для class Competitor(val name: String, ...)?', options: ['name — зарезервированное слово', 'name объявлен через val — у val-свойства нет сеттера, переприсвоить его нельзя', 'Строки в Kotlin вообще нельзя менять', 'Нужно сначала вызвать bogdan.unlock()'], correct: 1, why: 'val в конструкторе создаёт свойство без сеттера — то же самое правило val, что и для обычных переменных, просто применённое к свойству объекта.' },
  { q: 'Почему names.add("Соня") не работает, если names: List<String> = listOf(...)?', options: ['add — устаревший метод', 'У List нет метода add: он есть только у MutableList', 'add работает только для чисел', 'Нужно писать names.add("Соня", 0)'], correct: 1, why: 'List — интерфейс только для чтения, без методов изменения. add появляется только у MutableList/mutableListOf.' },
  { q: 'Почему scores["Алиса"] нельзя напрямую положить в val x: Int?', options: ['Синтаксис [ ] запрещён для Map', 'Доступ по ключу возвращает Int? (может быть значение, а может — ничего), а не гарантированный Int', 'scores — не Map, а List', 'Нужно писать scores.get("Алиса") вместо [ ]'], correct: 1, why: 'Тип результата [ ] у Map — всегда nullable (Int?), потому что ключа могло не быть. Компилятор требует явно решить, что делать с этой неопределённостью.' },
]} />
</Block>

<SectionAnchor chapterId="classes-collections" sectionId="typical-errors" />

## Символы главы

Шпаргалка по всем новым знакам и конструкциям этой главы.

<Block kind="cheatsheet" title="Символы главы" chapterId="classes-collections" blockId="cheatsheet-symbols" favPayload={{
  kind: 'table',
  head: ['Символ / конструкция', 'Как называется', 'Что делает', 'Пример'],
  rows: [
    ['class', 'ключевое слово', 'объявляет класс — чертёж для создания объектов', 'class Competitor(...)'],
    ['val/var в скобках конструктора', 'свойство-параметр', 'параметр конструктора сразу становится свойством объекта, доступным через точку', 'class Competitor(val name: String)'],
    ['. после объекта', 'доступ к свойству/методу', 'читает свойство или вызывает метод конкретного объекта', 'bogdan.score'],
    ['data', 'ключевое слово', 'помечает класс данных: генерирует toString/equals/hashCode/copy/componentN', 'data class Competitor(...)'],
    ['==', 'оператор равенства', 'у data class — по содержимому полей, у обычного класса — по ссылке', 'a == b'],
    ['.copy(поле = значение)', 'метод data class', 'создаёт новый объект на основе старого с изменёнными полями', 'bogdan.copy(score = 96)'],
    ['val (a, b) = obj', 'деструктуризация', 'раскладывает свойства объекта по переменным через componentN()', 'val (name, score) = bogdan'],
    ['for (x in coll) { }', 'цикл по коллекции', 'присваивает x каждый элемент коллекции по очереди', 'for (name in names)'],
    ['[индекс] / [ключ]', 'оператор индексации', 'достаёт элемент списка по позиции или значение карты по ключу', 'names[1], scores["Алиса"]'],
    ['to', 'инфиксная функция', 'создаёт пару ключ-значение (Pair) для mapOf', '"Алиса" to 82'],
    ['[ключ] = значение', 'индексация со присваиванием', 'добавляет или заменяет значение в MutableMap по ключу', 'scores["Соня"] = 77'],
    ['when', 'ключевое слово', 'выбирает одну ветку из нескольких — замена цепочке if/else if', 'when (place) { ... }'],
    [', в ветке when', 'группировка значений', 'одна ветка срабатывает для нескольких перечисленных значений', '2, 3 -> "Призёр"'],
    ['in a..b в ветке when', 'проверка диапазона', 'ветка срабатывает, если значение входит в диапазон', 'in 4..10 -> "Финалист"'],
  ],
}}>

| Символ / конструкция | Как называется | Что делает | Пример |
|---|---|---|---|
| `class` | ключевое слово | объявляет класс — чертёж для создания объектов | `class Competitor(...)` |
| `val`/`var` в скобках конструктора | свойство-параметр | параметр конструктора сразу становится свойством объекта, доступным через точку | `class Competitor(val name: String)` |
| `.` после объекта | доступ к свойству/методу | читает свойство или вызывает метод конкретного объекта | `bogdan.score` |
| `data` | ключевое слово | помечает класс данных: генерирует toString/equals/hashCode/copy/componentN | `data class Competitor(...)` |
| `==` | оператор равенства | у data class — по содержимому полей, у обычного класса — по ссылке | `a == b` |
| `.copy(поле = значение)` | метод data class | создаёт новый объект на основе старого с изменёнными полями | `bogdan.copy(score = 96)` |
| `val (a, b) = obj` | деструктуризация | раскладывает свойства объекта по переменным через componentN() | `val (name, score) = bogdan` |
| `for (x in coll) { }` | цикл по коллекции | присваивает x каждый элемент коллекции по очереди | `for (name in names)` |
| `[индекс]` / `[ключ]` | оператор индексации | достаёт элемент списка по позиции или значение карты по ключу | `names[1]`, `scores["Алиса"]` |
| `to` | инфиксная функция | создаёт пару ключ-значение (Pair) для mapOf | `"Алиса" to 82` |
| `[ключ] = значение` | индексация со присваиванием | добавляет или заменяет значение в MutableMap по ключу | `scores["Соня"] = 77` |
| `when` | ключевое слово | выбирает одну ветку из нескольких — замена цепочке if/else if | `when (place) { ... }` |
| `,` в ветке when | группировка значений | одна ветка срабатывает для нескольких перечисленных значений | `2, 3 -> "Призёр"` |
| `in a..b` в ветке when | проверка диапазона | ветка срабатывает, если значение входит в диапазон | `in 4..10 -> "Финалист"` |

</Block>

<details>
<summary>Углубиться</summary>

**Почему `equals()` и `hashCode()` всегда генерируются парой.** Контракт (contract) между этими двумя методами простой: если `a == b`, то у них обязан совпадать и `hashCode()`. `Set` и `Map` под капотом используют `hashCode()`, чтобы быстро понять, в какую «корзину» положить объект, и только потом проверяют `equals()` внутри неё. Если бы `equals()` сравнивал по содержимому, а `hashCode()` — по ссылке (как это можно сделать вручную, переопределив только один из двух), одинаковые по значению объекты могли бы попасть в разные «корзины» и `Set` считал бы их разными, несмотря на `==`, вернувший `true`. Именно поэтому `data class` генерирует оба метода одновременно и никогда — только один из них.

**`List` — это интерфейс, а не гарантия неизменности.** `listOf(...)` возвращает объект, у типа `List` которого просто нет методов изменения — но это ограничение на уровне доступа через этот тип, а не свойство самих данных в памяти. Если тот же набор элементов виден откуда-то ещё и через `MutableList` (например, `mutableListOf` присвоили переменной типа `List`), изменить его всё ещё можно — просто не через ссылку `List`. Для настоящей неизменности данных в памяти в Kotlin есть отдельные immutable-структуры за пределами базового курса.

Дальше по теме:
- [Equality](https://kotlinlang.org/docs/equality.html) — официальная документация: структурное и ссылочное равенство, контракт equals()/hashCode() подробно.
- [Destructuring declarations](https://kotlinlang.org/docs/destructuring-declarations.html) — официальная документация про componentN() и деструктуризацию не только объектов, но и Map, и Pair.
- [Data classes](https://kotlinlang.org/docs/data-classes.html) — официальная документация на английском: те же toString/equals/copy, но с точным описанием, какие свойства учитываются, а какие — нет.

</details>

<SectionAnchor chapterId="classes-collections" sectionId="cheatsheet-symbols" />

## Слова главы

<Block kind="vocab" title="Словарь главы" chapterId="classes-collections" blockId="vocab-glossary">
<Flashcards chapterId="classes-collections" cards={[
  { term: 'class', translation: 'класс', note: 'чертёж для создания объектов с общими свойствами и методами' },
  { term: 'instance', translation: 'экземпляр', note: 'конкретный объект, созданный по чертежу класса — alice является экземпляром Competitor' },
  { term: 'property', translation: 'свойство', note: 'значение, которое хранится в объекте класса и объявляется через val/var' },
  { term: 'constructor', translation: 'конструктор', note: 'часть объявления класса, описывающая, какие данные нужны, чтобы создать объект' },
  { term: 'data class', translation: 'класс данных', note: 'класс с ключевым словом data: компилятор сам генерирует toString, equals, hashCode, copy и componentN' },
  { term: 'equality', translation: 'равенство', note: 'сравнение через ==: у обычного класса — по ссылке, у data class — по содержимому полей' },
  { term: 'mutable', translation: 'изменяемый', note: 'коллекция или свойство, которое можно менять после создания, например MutableList или var' },
  { term: 'key-value pair', translation: 'пара ключ-значение', note: 'элемент Map: у каждого значения есть свой уникальный ключ для доступа' },
  { term: 'iteration', translation: 'обход коллекции', note: 'последовательный проход по всем элементам списка, множества или карты, например через for' },
  { term: 'exhaustive', translation: 'исчерпывающий', note: 'свойство when-выражения: должны быть учтены все возможные варианты, иначе нужна ветка else' },
]} />
</Block>

<SectionAnchor chapterId="classes-collections" sectionId="vocab" />

## Потренируйся печатать

Сначала объявление класса с двумя свойствами в конструкторе:

<Block kind="trainer" title="Классы: объявления и методы" chapterId="classes-collections" blockId="trainer-classes">
<CodeTyping keyboard chapterId="classes-collections" trainerId="trainer-classes" targetCpm={110} targetAccuracy={90} pools={[
  { label: 'Классы', snippets: [
    'class Competitor(val name: String, var score: Int)',
    'data class Competitor(val name: String, var score: Int)',
    'fun addPoints(points: Int) { score += points }',
    'val bogdanNextRound = bogdan.copy(score = 96)',
    'val (name, score) = bogdan',
    'val totalScore = team.map { it.score }.sum()',
    'val qualified = team.filter { it.score >= 75 }.map { it.name }',
  ] },
]} />
</Block>

Теперь карта с инфиксной функцией to:

<Block kind="trainer" title="Коллекции: списки, карты, множества" chapterId="classes-collections" blockId="trainer-collections">
<CodeTyping chapterId="classes-collections" trainerId="trainer-collections" targetCpm={120} targetAccuracy={90} pools={[
  { label: 'Коллекции', snippets: [
    'val scores = mapOf("Алиса" to 82, "Богдан" to 91)',
    'val roster = mutableListOf("Алиса", "Богдан")',
    'scores["Соня"] = 77',
    'println(scores.getOrDefault("Дима", 0))',
    'val uniqueScores = setOf(82, 91, 82, 77, 91)',
    'for (name in names) { println(name) }',
    'for ((name, score) in scores) { println("$name: $score") }',
  ] },
]} />
</Block>

<SectionAnchor chapterId="classes-collections" sectionId="practice-typing" />

## Куда дальше: проверенные ресурсы

### Русские материалы с нуля

<Block kind="cheatsheet" title="Русские материалы с нуля" chapterId="classes-collections" blockId="cheatsheet-resources-ru">

- [kotlinlang.ru — Классы](https://kotlinlang.ru/docs/classes.html) — официальная документация в переводе: конструкторы, свойства, наследование — читай раздел про основной конструктор и свойства.
- [kotlinlang.ru — Классы данных](https://kotlinlang.ru/docs/data-classes.html) — тот же перевод про data class: что именно генерируется и какие есть ограничения на параметры конструктора.
- [kotlinlang.ru — Коллекции. Общий обзор](https://kotlinlang.ru/docs/collections-overview.html) — иерархия типов коллекций Kotlin: чем List/Set/Map отличаются от их изменяемых версий на уровне интерфейсов.
- [kotlinlang.ru — Условия и циклы](https://kotlinlang.ru/docs/control-flow.html) — в этой же главе официального перевода, ниже if, разобран when с примерами диапазонов и множественных веток.
- [Урок 11: ООП. Классы и объекты в Kotlin](https://ievetrov.ru/kotlin-s-nulya-uroki/urok-11-klassy-i-obekty/) — продолжение курса с уроков 1 и 10: классы объясняются через бытовые аналогии, с видео.
- [Урок 9: Списки в Kotlin, функции для работы с коллекциями](https://ievetrov.ru/kotlin-s-nulya-uroki/urok-9-spiski-i-funkczii-dlya-kollekczij/) — тот же курс: List и операции над коллекциями отдельным уроком, с видео.

</Block>

### Официальный тур Kotlin (на английском)

<Block kind="cheatsheet" title="Официальный тур Kotlin" chapterId="classes-collections" blockId="cheatsheet-resources-tour">

- [Kotlin tour: Classes](https://kotlinlang.org/docs/kotlin-tour-classes.html) — официальный тур: конструкторы, свойства и методы со встроенными упражнениями.
- [Kotlin tour: Collections](https://kotlinlang.org/docs/kotlin-tour-collections.html) — официальный тур: List, Set, Map и их изменяемые версии на практических примерах.

</Block>

### Потренироваться

<Block kind="cheatsheet" title="Потренироваться" chapterId="classes-collections" blockId="cheatsheet-resources-practice">

<ToolCard
  name="play.kotlinlang.org"
  url="https://play.kotlinlang.org"
  desc="Та же песочница, что работает в кнопках Run этой главы, но во весь экран."
  embeddable
/>

- [Kotlin Koans: Classes / Collections](https://play.kotlinlang.org/koans/overview) — в общем списке разделов Koans есть отдельные категории Classes и Collections — задачи с автопроверкой именно на темы этой главы.

- [Видео и материалы сообщества — ниже](#community-sources) — ролики по теме и ссылки от студентов и преподавателей смотри в самом низу страницы.

</Block>

<SectionAnchor chapterId="classes-collections" sectionId="resources" />

## Челлендж ⭐

Команда `Competitor` уже знакома: `Алиса` — 82 очка, `Богдан` — 91, `Соня` — 66.

**Ступень 1.** Организаторам нужен список имён тех, кто прошёл отбор — набрал `score >= 75`. Напиши **одно выражение** на цепочке `.filter { }.map { }` (по образцу из этой главы и главы 1), которое из списка `team` отбирает нужных участников и оставляет только их имена.

**Ступень 2 ⭐.** Напиши функцию `medalFor(score: Int): String`, использующую `when` с диапазонами (`>= 90` — `"Золото"`, `>= 75` — `"Серебро"`, `>= 50` — `"Бронза"`, иначе — `"Участие"`), и выведи для каждого участника команды строку вида `Богдан: Золото`.

<KotlinPlay code={`fun main() {
    val team = listOf(
        Competitor("Алиса", 82),
        Competitor("Богдан", 91),
        Competitor("Соня", 66)
    )
    // ступень 1: замени emptyList() на список имён с score >= 75
    val qualified = emptyList<String>()
    println("Прошли отбор: $qualified")
    // ступень 2 (со звёздочкой): допиши medalFor и выведи медаль каждого участника
}

data class Competitor(val name: String, var score: Int)

// ступень 2 (со звёздочкой): допиши функцию здесь
// fun medalFor(score: Int): String = when { ... }`} />

<details>
<summary>Решение (сначала попробуй сам)</summary>

Ступень 1:

```kotlin
val qualified = team.filter { it.score >= 75 }.map { it.name }
```

Ступень 2 ⭐:

```kotlin
fun medalFor(score: Int): String = when {
    score >= 90 -> "Золото"
    score >= 75 -> "Серебро"
    score >= 50 -> "Бронза"
    else -> "Участие"
}

fun main() {
    val team = listOf(
        Competitor("Алиса", 82),
        Competitor("Богдан", 91),
        Competitor("Соня", 66)
    )
    val qualified = team.filter { it.score >= 75 }.map { it.name }
    println("Прошли отбор: $qualified")
    for (competitor in team) {
        println("\${competitor.name}: \${medalFor(competitor.score)}")
    }
}

data class Competitor(val name: String, var score: Int)
```

Полный вывод:
```text
Прошли отбор: [Алиса, Богдан]
Алиса: Серебро
Богдан: Золото
Соня: Бронза
```
`Алиса` (82) и `Богдан` (91) прошли отбор — оба набрали 75 и больше; `Соня` с 66 — нет, поэтому в `qualified` её не будет, но `when` всё равно посчитает ей медаль «Бронза» (66 ≥ 50) при выводе полного списка.

</details>

<SectionAnchor chapterId="classes-collections" sectionId="challenge" />

<Block kind="quiz" title="Экзамен главы" chapterId="classes-collections" blockId="exam">
<ChapterExam chapterId="classes-collections" questions={[
  { q: 'data class Competitor(val name: String, var score: Int). val a = Competitor("Марат", 88); val b = a.copy(score = 95); println(a.score) — что выведет?', options: ['95', 'null', '88', 'Ошибка компиляции: copy() требует все аргументы'], correct: 2, why: 'copy() создаёт новый объект b с изменённым score, а исходный объект a не меняется — его score остаётся 88.' },
  { q: 'val place = 5; when (place) { 1 -> "Золото"; 2, 3 -> "Призёр"; in 4..10 -> "Финалист"; else -> "Участник" } — что окажется в medal?', options: ['Финалист', 'Призёр', 'Золото', 'Участник'], correct: 0, why: 'place = 5 не равно 1, не входит в {2, 3}, но входит в диапазон 4..10 — сработает ветка "Финалист".' },
  { q: 'class Competitor(val name: String, var score: Int). Есть alice = Competitor("Алиса", 82), и код alice.name = "Аля" — что скажет компилятор?', options: ['Скомпилируется — свойства объектов всегда можно менять', "No value passed for parameter 'name' — name обязателен", "Unresolved reference 'name' — такого свойства нет", "'val' cannot be reassigned — name объявлен через val, у него нет сеттера"], correct: 3, why: 'name объявлен как val в конструкторе — это свойство без сеттера, переприсвоить его нельзя, как и обычную val-переменную.' },
  { q: 'val scores = mapOf("Алиса" to 82, "Богдан" to 91); val bogdanScore: Int = scores["Богдан"] — почему это не скомпилируется?', options: ['Доступ к Map по ключу через [ ] всегда возвращает Int?, а не гарантированный Int — типы не совпадают', 'scores — не Map, а List, поэтому [ ] не работает', 'Ключа "Богдан" не существует в scores', 'mapOf нельзя использовать с числовыми значениями'], correct: 0, why: 'scores["Богдан"] имеет тип Int?, потому что ключ теоретически мог отсутствовать. Присвоить это val типа Int напрямую нельзя без явной обработки возможного отсутствия значения.' },
  { q: 'Зачем классу нужно ключевое слово data, если обычный class уже умеет хранить свойства?', options: ['data ускоряет создание объектов', 'data — единственный способ добавить в класс методы', 'data запрещает изменять свойства объекта', 'data заставляет компилятор сгенерировать toString(), equals(), hashCode(), copy() и componentN() на основе свойств конструктора'], correct: 3, why: 'Без data класс наследует только стандартный toString()/equals() от Any (печать адреса, сравнение по ссылке). data дописывает читаемый toString, сравнение по содержимому и copy().' },
  { q: 'Почему каждый параметр основного конструктора data class обязан быть val или var?', options: ['Иначе конструктор не будет исполняться во время выполнения', 'Это ограничение только для конструкторов с более чем двумя параметрами', 'Параметр без val/var не становится свойством и не попадёт в сгенерированные toString/equals/copy/componentN', 'На самом деле это не обязательно — правило только для обычных классов'], correct: 2, why: 'data class генерирует свои методы на основе свойств конструктора. Параметр без val/var — не свойство, а временное значение, поэтому в сгенерированный код он не попадёт.' },
  { q: 'Чем List отличается от MutableList на уровне доступных методов?', options: ['List хранит только строки, MutableList — любые типы', 'У List нет методов изменения содержимого (add/remove), у MutableList они есть', 'Это два названия одного и того же типа', 'List работает быстрее, MutableList — медленнее'], correct: 1, why: 'List — интерфейс только для чтения. MutableList расширяет его методами add/remove и другими для изменения содержимого.' },
]} />
</Block>

## Что должен уметь

- Объявлять класс с основным конструктором и свойствами `val`/`var`, объясняя, что делает каждый символ, включая `val`/`var` внутри скобок.
- Создавать объекты по классу и обращаться к их свойствам и методам через точку.
- Объяснять, что генерирует ключевое слово `data`, и приводить разницу в выводе `toString()` и `==` между обычным классом и `data class` на конкретном примере.
- Использовать `.copy()` для создания изменённой копии объекта, не трогая исходный.
- Отличать `List` от `MutableList` и `Map` от `MutableMap` по набору доступных методов, объясняя, почему `.add()` не работает на `List`.
- Создавать и обходить `List`, `Map` и `Set`, используя `[индекс]`/`[ключ]`, `for (x in coll)` и деструктуризацию `for ((k, v) in map)`.
- Строить пары ключ-значение через инфиксную функцию `to` и безопасно читать значение из карты через `getOrDefault`.
- Писать `when` и как список независимых условий, и как сравнение одного значения с ветками через запятую и диапазоны `in a..b`.
- Объяснять, почему `when`-выражение требует ветку `else` для исчерпывающести (exhaustiveness), когда его результат кладётся в переменную.
- Узнавать по сообщению компилятора пять типичных ошибок этой главы — непереданный аргумент конструктора, переприсваивание `val`-свойства, вызов `.add()` у `List`, неисчерпывающий `when`, обращение к `Map` без учёта `null` — и чинить каждую.

<SectionAnchor chapterId="classes-collections" sectionId="what-you-should-know" />
