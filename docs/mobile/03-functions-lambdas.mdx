---
title: Функции и лямбды
audience: мобилка
level: база
order: 3
---

import ChapterCover from '@site/src/components/ChapterCover';

<ChapterCover chapterId="functions-lambdas" />

import Figure from '@site/src/components/Figure';
import SyntaxBreakdown from '@site/src/components/SyntaxBreakdown';
import CodeTyping from '@site/src/components/CodeTyping';
import Flashcards from '@site/src/components/Flashcards';
import KotlinPlay from '@site/src/components/KotlinPlay';
import PredictOutput from '@site/src/components/PredictOutput';
import SelfCheck from '@site/src/components/SelfCheck';
import ChapterExam from '@site/src/components/ChapterExam';
import Block from '@site/src/components/Block';
import Hint from '@site/src/components/Hint';
import ToolCard from '@site/src/components/ToolCard';
import CollapsibleToc from '@site/src/components/CollapsibleToc';
import ChapterProgress from '@site/src/components/ChapterProgress';
import SectionAnchor from '@site/src/components/SectionAnchor';
import SignatureBuilder from '@site/src/components/SignatureBuilder';
import CallTracer from '@site/src/components/CallTracer';

# Функции и лямбды

<ChapterProgress chapterId="functions-lambdas" totalSections={14} totalQuizzes={4} totalTrainers={6} />

## Зачем это нужно

В прошлой главе мы уже пользовались готовыми функциями — `println`, `listOf`, `map`, `sum` — и даже написали свою первую лямбду внутри `map { it * 2 }`. Мы знали, как их вызывать, но не разбирали, как устроена функция изнутри и как написать свою.

Функция (function) — это именованный кусок кода, который можно вызвать сколько угодно раз с разными входными данными. Без функций код, который считает бонус за очки, пришлось бы копировать в каждое место, где он нужен: на экране игрока, в отчёте тренера, в уведомлении о победе. С функцией это один кусок кода в одном месте — и вызов по имени везде, где он нужен.

В этой главе разберём объявление функции по символам — что значит каждый знак сигнатуры, — научимся задавать параметры по умолчанию (default arguments) и именованные аргументы (named arguments), напишем собственные лямбды разных форм и разберёмся, что такое функция высшего порядка (higher-order function) на примере `map` и `filter`, которые уже видели в главе 1.

<CollapsibleToc toc={toc} chapterId="functions-lambdas" />

<SectionAnchor chapterId="functions-lambdas" sectionId="why" />

## Функция по кусочкам: `fun square(x: Int): Int`

Вот функция, которая возводит число в квадрат. Разберём сигнатуру — первую строку, где объявлены имя, параметры и тип возврата, — по символам.

<Figure scheme="lambda-box" caption="Функция — коробка: вход слева, тело внутри, выход справа. Лямбда — та же коробка без имени." />

<Block kind="breakdown" title="Разбор по символам: сигнатура fun square" chapterId="functions-lambdas" blockId="breakdown-square-signature">
<SyntaxBreakdown parts={[
  { text: 'fun ', label: 'ключевое слово', note: 'объявляет функцию (function); от английского fun — сокращение того же слова. Пробел после него обязателен, как и после val' },
  { text: 'square', label: 'имя функции', note: 'придумываешь сам; по нему функцию вызывают дальше в коде. Правила именования те же, что у переменных: латиница, без пробелов' },
  { text: '(', label: 'открывающая скобка параметров', note: 'начинает список входных данных функции (parameters) — то, что она принимает при вызове' },
  { text: 'x: Int', label: 'параметр с типом', note: 'x — имя параметра, Int — его тип. У параметра функции тип указывать обязательно: Kotlin не выводит его сам, в отличие от переменной' },
  { text: ')', label: 'закрывающая скобка параметров', note: 'список параметров закончился. Если бы параметров было несколько, их разделяла бы запятая с пробелом: (a: Int, b: Int)' },
  { text: ': ', label: 'двоеточие — тип возврата', note: 'то же двоеточие, что и у переменной, но теперь после скобок функции: означает «эта функция возвращает значение такого типа»' },
  { text: 'Int', label: 'тип возврата', note: 'тип значения, которое функция отдаёт наружу через return. Здесь — целое число' },
]} />
</Block>

### Что означает каждый символ

- **`fun`** — ключевое слово: объявляет функцию. Как и `val`/`var`, это слово нельзя использовать как имя переменной или функции.
- **скобки `( )`** — список параметров (parameters): переменных, которые функция получает при вызове. Пустые скобки `()` означают, что функция параметров не принимает — так была устроена `main()` в главе 1.
- **`: Тип`** после скобок — тип возврата (return type): что функция отдаёт наружу. Если функция ничего не возвращает — этот раздел можно опустить, разберём чуть ниже.
- **параметр и аргумент — разные слова.** Параметр (parameter) — это имя в объявлении функции (`x` в `fun square(x: Int)`). Аргумент (argument) — конкретное значение, которое передают при вызове (`4` в `square(4)`). Одно описывает место, другое — то, что в это место кладут.

<Figure scheme="function-signature-anatomy" caption="Сигнатура функции по частям: имя — параметры со своими типами — тип возврата. Три части между словом fun и открывающей скобкой тела." />

Теперь тело функции — то, что происходит внутри фигурных скобок, — и вызов.

<KotlinPlay code={`fun main() {
    println(square(4))
}

fun square(x: Int): Int {
    return x * x
}`} />

### Разбор по строкам

- **Строки 1–3: `fun main() { ... }`** — точка входа, как в главе 1. Внутри — единственный вызов `square(4)`, обёрнутый в `println`.
- **Строка 5: `fun square(x: Int): Int {`** — объявление функции `square`: принимает один параметр `x` типа `Int`, возвращает `Int`. Открывающая `{` начинает тело — список команд, которые выполнятся при вызове.
- **Строка 6: `return x * x`** — новое ключевое слово `return`: «немедленно закончи функцию и отдай наружу то, что справа». Здесь отдаётся `x * x` — квадрат переданного числа.
- **Строка 7: `}`** — конец тела функции.

<Hint id="functions-lambdas-h1" type="fact">
Функцию можно объявить и до, и после места, где её вызывают: в отличие от переменных, порядок объявления функций в файле не важен — компилятор сначала видит все объявления, а потом проверяет тела.
</Hint>

**Что выведет:** `16` — потому что `square(4)` вычислил `4 * 4` и вернул это значение в `println`.

<SectionAnchor chapterId="functions-lambdas" sectionId="function-anatomy" />

## Тело-выражение: одна строка вместо блока

Если тело функции — это ровно одно выражение, фигурные скобки и `return` можно убрать, заменив их знаком `=`. Это называется телом-выражением (expression body) — в противовес телу-блоку (block body), которое мы только что видели.

<Block kind="breakdown" title="Разбор по символам: тело-выражение" chapterId="functions-lambdas" blockId="breakdown-expression-body">
<SyntaxBreakdown parts={[
  { text: 'fun ', label: 'ключевое слово', note: 'то же самое объявление функции' },
  { text: 'square', label: 'имя функции', note: 'функция называется так же — это может быть даже другая перегрузка той же операции' },
  { text: '(x: Int)', label: 'параметр в скобках', note: 'один параметр x типа Int, как и раньше' },
  { text: ': Int', label: 'тип возврата', note: 'здесь его ещё можно писать явно — но, как увидим ниже, теперь необязательно' },
  { text: ' = ', label: 'оператор тела-выражения', note: 'это НЕ присваивание, как в val x = 5: здесь = говорит «результат функции — это значение выражения справа». Пробелы вокруг него, как и у val/var, для читаемости' },
  { text: 'x * x', label: 'единственное выражение', note: 'то, что раньше отдавали через return, теперь просто пишут после =. Ни фигурных скобок, ни return не нужно' },
]} />
</Block>

<KotlinPlay code={`fun main() {
    println(square(4))
}

fun square(x: Int): Int = x * x`} />

**Что выведет:** `16` — то же самое число, что и в блочной версии: это два разных способа написать одну и ту же функцию.

У тела-выражения есть бонус: раз результат — это значение стоящего справа выражения, Kotlin может вывести его тип сам, точно так же, как выводил тип переменной по значению справа от `=`. Поэтому `: Int` можно вообще убрать.

<KotlinPlay code={`fun main() {
    println(square(4))
}

fun square(x: Int) = x * x`} />

**Что выведет:** `16` — компилятор сам понял, что `x * x` — это `Int`, раз `x` типа `Int`, и присвоил такой же тип возврата функции. Это тот же вывод типов (type inference), что и у переменных, только применённый к функции.

<Hint id="functions-lambdas-h2" type="important">
Важно: **этот трюк работает только для тела-выражения.** У тела-блока (в фигурных скобках) тип возврата выводить неоткуда — Kotlin не станет анализировать все `return` внутри блока в поисках общего типа. Про это — отдельная ошибка в разделе «Типичные ошибки» ниже.
</Hint>

А что, если функции нечего возвращать — она просто что-то делает, например печатает текст? Тогда тип возврата вообще не пишут, и Kotlin считает его равным `Unit` — специальному типу «ничего не возвращаю».

<KotlinPlay code={`fun main() {
    greet("Соня")
}

fun greet(name: String) {
    println("Привет, $name!")
}`} />

### Разбор по строкам

- **Строка 5: `fun greet(name: String) {`** — после скобок нет `: Тип` — значит, тип возврата не указан, и Kotlin подставляет `Unit` сам. Писать `: Unit` руками можно, но так не делают — это единственный тип, который по соглашению не пишут явно.
- **Строка 6** — функция просто печатает строку и ничего не отдаёт наружу через `return`.

**Что выведет:** `Привет, Соня!` — функция выполнила действие, но значения не вернула.

<Block kind="quiz" title="Сигнатура и тело функции" chapterId="functions-lambdas" blockId="quiz-signature-body">
<SelfCheck chapterId="functions-lambdas" quizId="q1" questions={[
  { q: 'Что означает : Int после скобок в fun square(x: Int): Int?', options: ['Тип параметра x', 'Тип значения, которое функция возвращает (return type)', 'Имя переменной внутри функции', 'Количество параметров'], correct: 1, why: 'Двоеточие после скобок параметров — аннотация типа возврата: что именно функция отдаёт наружу через return.' },
  { q: 'Чем тело-выражение (= x * x) отличается от тела-блока ({ return x * x })?', options: ['Ничем, это синонимы', 'Тело-выражение — одна строка без фигурных скобок и return; тело-блок — полноценный блок кода с явным return', 'Тело-выражение работает медленнее', 'Тело-блок нельзя использовать с параметрами'], correct: 1, why: 'Это два способа описать тело функции. Тело-выражение годится, когда функция — это одна формула; тело-блок — когда нужно несколько шагов.' },
  { q: 'Можно ли не писать : Int в fun square(x: Int) = x * x?', options: ['Нет, это ошибка компиляции', 'Да — при теле-выражении Kotlin выводит тип возврата сам по значению справа от =', 'Можно, но тогда функция всегда будет возвращать Unit', 'Можно только для функций без параметров'], correct: 1, why: 'Вывод типов работает и для тела-выражения: Kotlin смотрит на x * x, видит Int и присваивает такой же тип возврата функции.' },
  { q: 'Что вернёт fun greet(name: String) { println("Привет, $name!") }, если тип возврата не указан?', options: ['String, потому что внутри печатается строка', 'Unit — функция ничего не возвращает, только выполняет действие', 'Int, потому что это тип по умолчанию', 'Ошибку компиляции — тип возврата обязателен'], correct: 1, why: 'Если после скобок нет : Тип, Kotlin подставляет Unit — специальный тип «ничего не возвращаю». Это не ошибка, а нормальный случай для функций-действий.' },
]} />
</Block>

<SectionAnchor chapterId="functions-lambdas" sectionId="expression-body" />

## Параметры по умолчанию: необязательные аргументы

Параметру можно задать значение по умолчанию (default argument) прямо в объявлении. Тогда при вызове его можно не передавать — Kotlin подставит значение сам.

<Block kind="breakdown" title="Разбор по символам: параметр по умолчанию" chapterId="functions-lambdas" blockId="breakdown-default-argument">
<SyntaxBreakdown parts={[
  { text: 'fun ', label: 'ключевое слово', note: 'объявление функции' },
  { text: 'greet', label: 'имя функции', note: 'по нему будем вызывать функцию' },
  { text: '(', label: 'открывающая скобка параметров', note: 'начало списка' },
  { text: 'name: String', label: 'обязательный параметр', note: 'без значения по умолчанию — передавать его при вызове придётся всегда' },
  { text: ', ', label: 'запятая-разделитель', note: 'отделяет один параметр от другого; пробел после неё — для читаемости, как и у аргументов' },
  { text: 'greeting: String', label: 'параметр с типом', note: 'ещё один параметр типа String' },
  { text: ' = ', label: 'значение по умолчанию', note: 'этот = живёт внутри скобок параметров — третье значение того же символа в главе. Он говорит: «если аргумент не передали, возьми то, что справа»' },
  { text: '"Привет"', label: 'значение по умолчанию', note: 'будет использовано для greeting, если вызывающий код его не укажет' },
  { text: ')', label: 'закрывающая скобка параметров', note: 'список параметров закончился' },
  { text: ' {', label: 'начало тела', note: 'тело-блок функции' },
]} />
</Block>

<KotlinPlay code={`fun main() {
    greet("Олег")
    greet("Олег", "Здравствуй")
}

fun greet(name: String, greeting: String = "Привет") {
    println("$greeting, $name!")
}`} />

### Разбор по строкам

- **Строка 2: `greet("Олег")`** — передан только `name`; `greeting` не указан, поэтому берётся значение по умолчанию `"Привет"`.
- **Строка 3: `greet("Олег", "Здравствуй")`** — передано два аргумента, второй перекрывает значение по умолчанию.
- **Строка 6** — само объявление: `greeting: String = "Привет"` делает второй параметр необязательным.

**Что выведет:**
```text
Привет, Олег!
Здравствуй, Олег!
```
Первый вызов взял значение по умолчанию, второй — то, что передали явно.

Обязательные параметры (без `=`) по соглашению ставят первыми, необязательные (со значением по умолчанию) — после них: это стиль, а не запрет компилятора. У параметра, который не в конце списка, тоже может быть значение по умолчанию — но тогда, чтобы пропустить его и задать следующий, понадобятся именованные аргументы, о которых дальше.

<SectionAnchor chapterId="functions-lambdas" sectionId="default-arguments" />

## Именованные аргументы: вызов по имени, а не по порядку

Аргументы можно передавать не только по порядку, но и по имени параметра — тогда порядок вызова не важен, а из кода сразу видно, что чему соответствует.

<Block kind="breakdown" title="Разбор по символам: именованные аргументы" chapterId="functions-lambdas" blockId="breakdown-named-arguments">
<SyntaxBreakdown parts={[
  { text: 'describe', label: 'имя функции', note: 'вызываем функцию describe' },
  { text: '(', label: 'открывающая скобка', note: 'начало списка аргументов вызова' },
  { text: 'name = "ПГК"', label: 'именованный аргумент', note: 'name = — это имя параметра из объявления функции, а не переменная: слева стоит имя параметра, справа — значение для него' },
  { text: ', ', label: 'запятая', note: 'разделяет аргументы' },
  { text: 'score = 100', label: 'именованный аргумент', note: 'значение 100 попадёт в параметр score, где бы он ни стоял в объявлении' },
  { text: ', ', label: 'запятая', note: 'ещё один разделитель' },
  { text: 'isChampion = true', label: 'именованный аргумент', note: 'этот параметр в объявлении имеет значение по умолчанию, но здесь мы его переопределяем явно' },
  { text: ')', label: 'закрывающая скобка', note: 'конец списка аргументов' },
]} />
</Block>

<KotlinPlay code={`fun main() {
    describe(name = "ПГК", score = 100, isChampion = true)
    describe(score = 42, name = "Соня")
}

fun describe(name: String, score: Int, isChampion: Boolean = false) {
    println("$name: $score очков, чемпион: $isChampion")
}`} />

### Разбор по строкам

- **Строка 2** — все три аргумента именованные, порядок совпадает с объявлением.
- **Строка 3** — аргументы переставлены местами (`score` раньше `name`) и один пропущен: `isChampion` не указан, значит берётся значение по умолчанию `false`. Именованные аргументы можно расставлять в любом порядке — Kotlin смотрит на имя слева от `=`, а не на позицию.
- **Строка 7** — обычное объявление функции с тремя параметрами, третий — со значением по умолчанию.

**Что выведет:**
```text
ПГК: 100 очков, чемпион: true
Соня: 42 очков, чемпион: false
```

<Hint id="functions-lambdas-h3" type="tip">
Заметь: `name = "ПГК"` при вызове и `greeting: String = "Привет"` при объявлении выглядят похоже, но это разные вещи. В объявлении `имя: Тип = значение` задаёт значение по умолчанию. При вызове `имя = значение` — это именованный аргумент, у него нет двоеточия и типа, потому что тип параметр уже получил при объявлении.
</Hint>

Проверь себя: здесь встречаются и значение по умолчанию, и именованные аргументы из этого раздела.

<Block kind="trainer" title="Предскажи вывод: default и именованные аргументы" chapterId="functions-lambdas" blockId="trainer-predict-default-named">
<PredictOutput kotlin normalizeWhitespace chapterId="functions-lambdas" trainerId="trainer-predict-default-named" expected={`Привет, Соня!
Здравствуй, Олег!`} code={`fun main() {
    greet("Соня")
    greet(greeting = "Здравствуй", name = "Олег")
}

fun greet(name: String, greeting: String = "Привет") {
    println("$greeting, $name!")
}`} />
</Block>

<Block kind="quiz" title="Параметры по умолчанию и именованные аргументы" chapterId="functions-lambdas" blockId="quiz-default-named">
<SelfCheck chapterId="functions-lambdas" quizId="q2" questions={[
  { q: 'Что делает greeting: String = "Привет" в объявлении параметра?', options: ['Присваивает переменной greeting значение "Привет" один раз', 'Делает параметр необязательным: если его не передать при вызове, используется "Привет"', 'Меняет тип параметра на String', 'Ничего — это просто комментарий'], correct: 1, why: 'Значение по умолчанию (default argument) используется, только если вызывающий код не передал свой аргумент для этого параметра.' },
  { q: 'Что выведет greet("Олег"), если объявление — fun greet(name: String, greeting: String = "Привет")?', options: ['Олег, Привет!', 'Привет, Олег!', 'Ошибку компиляции: не хватает аргумента', 'null, Олег!'], correct: 1, why: 'greeting не передан, поэтому берётся значение по умолчанию "Привет"; строка собирается как "$greeting, $name!".' },
  { q: 'Зачем нужны именованные аргументы, если можно передавать значения просто по порядку?', options: ['Они работают быстрее позиционных', 'Делают вызов понятным без взгляда в объявление функции и позволяют менять порядок или пропускать параметры со значением по умолчанию', 'Без них нельзя вызвать функцию с более чем одним параметром', 'Именованные аргументы обязательны для всех функций Kotlin'], correct: 1, why: 'describe(score = 42, name = "Соня") сразу понятно без документации, а порядок аргументов можно менять местами.' },
  { q: 'Что выведет describe(score = 42, name = "Соня") для fun describe(name: String, score: Int, isChampion: Boolean = false)?', options: ['Соня: 42 очков, чемпион: false', '42: Соня очков, чемпион: false', 'Ошибку: isChampion не передан', 'Соня: 42 очков, чемпион: true'], correct: 0, why: 'Именованные аргументы расставляют значения по именам параметров независимо от порядка вызова; isChampion не передан — берётся значение по умолчанию false.' },
]} />
</Block>

<SectionAnchor chapterId="functions-lambdas" sectionId="named-arguments" />

## Лямбды: с `it` и с именованными параметрами

В главе 1 мы уже писали `numbers.map { it * 2 }` — лямбду (lambda) с неявным параметром `it`. Освежим и пойдём глубже: что делать, если параметров несколько или `it` неудобно читать.

<KotlinPlay code={`fun main() {
    val distances = listOf(3, 5, 4, 6, 2)
    val long = distances.filter { it > 4 }
    println(long)
}`} />

### Разбор по строкам

- **Строка 3** — новый метод `filter`: пропускает каждый элемент через лямбду и оставляет в результате только те, для которых лямбда вернула `true`. `it` здесь — очередной элемент списка, `it > 4` — сравнение «больше 4».
- **Что выведет:** `[5, 6]` — только элементы длиннее 4 км остались в списке; `filter`, в отличие от `map`, не меняет сами значения, а решает, какие из них оставить.

Когда у лямбды не один параметр, а несколько, `it` уже не подходит — нужно назвать каждый явно и разделить их запятой перед стрелкой `->`.

<KotlinPlay code={`fun main() {
    val add: (Int, Int) -> Int = { a, b -> a + b }
    println(add(3, 4))
}`} />

### Разбор по строкам

- **Строка 2: `val add: (Int, Int) -> Int = { a, b -> a + b }`** — слева от `=` объявлена переменная `add` с типом `(Int, Int) -> Int`: это тип функции (function type), который читается «принимает два Int, возвращает Int». Справа — сама лямбда: `a, b` — имена двух параметров, `->` отделяет их от тела, `a + b` — тело.
- **Строка 3: `add(3, 4)`** — лямбда, лежащая в переменной, вызывается точно так же, как обычная функция: именем и скобками с аргументами.

**Что выведет:** `7` — потому что `3 + 4 = 7`.

<SectionAnchor chapterId="functions-lambdas" sectionId="lambdas" />

## Функции высшего порядка: `map`, `filter` и свои

Функция высшего порядка (higher-order function) — это функция, которая принимает другую функцию как параметр или возвращает функцию. `map` и `filter` из главы 1 — как раз такие: они принимают лямбду и решают, что с ней делать для каждого элемента.

<KotlinPlay code={`fun main() {
    val distances = listOf(3, 5, 4, 6, 2)
    val longInMeters = distances.filter { it > 4 }.map { it * 1000 }
    println(longInMeters)
}`} />

**Что выведет:** `[5000, 6000]` — сначала `filter` оставил тренировки длиннее 4 км (`[5, 6]`), затем `map` перевёл их в метры. Каждый метод в цепочке — сам по себе функция высшего порядка: и `filter`, и `map` принимают лямбду параметром.

Посмотри в замедленной съёмке, как выполнение прыгает из `main` в функцию и обратно, а `map` прогоняет лямбду по каждому элементу:

<Block kind="trainer" title="Трассировка вызова: функция + map с лямбдой" chapterId="functions-lambdas" blockId="trainer-call-tracer">
<CallTracer chapterId="functions-lambdas" trainerId="trainer-call-tracer" />
</Block>

Теперь напишем свою функцию высшего порядка — такую же, как `filter`/`map` внутри устроены сами.

<Block kind="breakdown" title="Разбор по символам: функция calculate" chapterId="functions-lambdas" blockId="breakdown-calculate-signature">
<SyntaxBreakdown parts={[
  { text: 'fun ', label: 'ключевое слово', note: 'объявляем функцию' },
  { text: 'calculate', label: 'имя функции', note: 'считает результат операции над двумя числами' },
  { text: '(a: Int, b: Int, ', label: 'обычные параметры', note: 'два целых числа — с ними будет работать переданная операция' },
  { text: 'operation: (Int, Int) -> Int', label: 'параметр-функция', note: 'параметр operation имеет функциональный тип: сам является функцией, которая принимает два Int и возвращает Int. Именно это делает calculate функцией высшего порядка' },
  { text: ')', label: 'закрывающая скобка', note: 'конец списка параметров' },
  { text: ': Int', label: 'тип возврата', note: 'сама calculate тоже возвращает Int' },
  { text: ' {', label: 'начало тела', note: 'тело-блок' },
]} />
</Block>

<KotlinPlay code={`fun main() {
    println(calculate(6, 3, { a, b -> a + b }))
    println(calculate(6, 3) { a, b -> a - b })
}

fun calculate(a: Int, b: Int, operation: (Int, Int) -> Int): Int {
    return operation(a, b)
}`} />

### Разбор по строкам

- **Строка 5: сигнатура `calculate`** — разобрана выше по символам.
- **Строка 6: `return operation(a, b)`** — внутри `calculate` мы не знаем заранее, что за операция придёт: сложение, вычитание, что угодно. `operation(a, b)` вызывает то, что передали, как обычную функцию.
- **Строка 2: `calculate(6, 3, { a, b -> a + b })`** — лямбда передана третьим аргументом обычным способом, в скобках через запятую.
- **Строка 3: `calculate(6, 3) { a, b -> a - b }`** — то же самое, но лямбда вынесена за скобки вызова. Такая запись называется трейлинг-лямбдой (trailing lambda): если лямбда — последний параметр функции, её можно писать не внутри `( )`, а сразу после них.

<Block kind="breakdown" title="Разбор по символам: трейлинг-лямбда" chapterId="functions-lambdas" blockId="breakdown-trailing-lambda">
<SyntaxBreakdown parts={[
  { text: 'calculate', label: 'имя функции', note: 'та же функция, что и выше' },
  { text: '(6, 3)', label: 'обычные аргументы', note: 'a и b передаются как всегда, в скобках через запятую' },
  { text: ' ', label: 'пробел', note: 'отделяет скобки вызова от лямбды — они больше не одно целое' },
  { text: '{ a, b -> a - b }', label: 'трейлинг-лямбда', note: 'третий аргумент — лямбда для operation, — вынесенный за круглые скобки. Это ровно то же значение, что и { a, b -> a - b }, переданное третьим параметром внутри скобок' },
]} />
</Block>

<Figure scheme="trailing-lambda-shift" caption="Трейлинг-лямбда — та же самая лямбда, только вынесенная за круглые скобки вызова: содержимое не меняется, меняется только пунктуация." />

**Что выведет:**
```text
9
3
```
Первый вызов сложил (6 + 3 = 9), второй вычел (6 − 3 = 3) — потому что мы передали разные лямбды одной и той же функции `calculate`.

<Figure scheme="higher-order-plug" caption="Три разных лямбды в одном и том же параметре operation дают три разных результата: сама calculate не знает, что делает op." />

А если той же `calculate` с теми же `6` и `3` передать третью лямбду — с умножением?

<Block kind="trainer" title="Предскажи вывод: calculate с новой лямбдой" chapterId="functions-lambdas" blockId="trainer-predict-calculate">
<PredictOutput kotlin chapterId="functions-lambdas" trainerId="trainer-predict-calculate" expected="18" code={`fun main() {
    println(calculate(6, 3) { a, b -> a * b })
}

fun calculate(a: Int, b: Int, operation: (Int, Int) -> Int): Int {
    return operation(a, b)
}`} />
</Block>

Теперь собери сигнатуры сам — от простой функции до функции высшего порядка:

<Block kind="trainer" title="Собери сигнатуру из кусочков" chapterId="functions-lambdas" blockId="trainer-signature-builder">
<SignatureBuilder chapterId="functions-lambdas" trainerId="trainer-signature-builder" tasks={[
  {
    brief: 'Функция greet: принимает имя-строку, возвращает строку.',
    pieces: ['fun ', 'greet', '(', 'name: ', 'String', ')', ': ', 'String'],
  },
  {
    brief: 'Функция greet: принимает имя-строку и приветствие-строку со значением по умолчанию "Привет", возвращает строку.',
    pieces: ['fun greet', '(', 'name: String', ', ', 'greeting: String', ' = ', '"Привет"', ')', ': String'],
  },
  {
    brief: 'Функция высшего порядка calculate: принимает два Int и операцию-лямбду (два Int → Int), возвращает Int.',
    pieces: ['fun calculate', '(', 'a: Int', ', ', 'b: Int', ', ', 'operation: ', '(Int, Int)', ' -> ', 'Int', ')', ': Int'],
  },
]} />
</Block>

<Block kind="quiz" title="Лямбды и функции высшего порядка" chapterId="functions-lambdas" blockId="quiz-lambdas-hof">
<SelfCheck chapterId="functions-lambdas" quizId="q3" questions={[
  { q: 'Что такое функция высшего порядка (higher-order function)?', options: ['Функция, которая вызывает сама себя', 'Функция, которая принимает другую функцию как параметр или возвращает функцию', 'Любая функция с более чем одним параметром', 'Функция без тела'], correct: 1, why: 'map, filter и наша calculate — функции высшего порядка именно потому, что принимают лямбду (функцию) как один из параметров.' },
  { q: 'Что означает тип (Int, Int) -> Int?', options: ['Список из трёх целых чисел', 'Функция, принимающая два Int и возвращающая Int', 'Массив типа Int размером 2', 'Функция без параметров, возвращающая Int дважды'], correct: 1, why: 'Слева от стрелки в скобках — типы параметров, справа от стрелки — тип возврата. Это тип самой функции как значения.' },
  { q: 'Чем calculate(6, 3) { a, b -> a - b } отличается от calculate(6, 3, { a, b -> a - b })?', options: ['Это две разные функции', 'Ничем по результату — это два способа записать один и тот же вызов; лямбду-последний-параметр можно вынести за скобки', 'Первый вариант работает быстрее', 'Второй вариант — ошибка компиляции'], correct: 1, why: 'Это трейлинг-лямбда (trailing lambda): синтаксический вариант записи, а не другая функция.' },
  { q: 'Что делает numbers.filter { it > 4 }?', options: ['Заменяет каждый элемент на it > 4', 'Оставляет в новом списке только те элементы, для которых лямбда вернула true', 'Считает количество элементов больше 4', 'Сортирует список по возрастанию'], correct: 1, why: 'filter — функция высшего порядка: пропускает каждый элемент через лямбду-предикат и оставляет только те, где результат true.' },
]} />
</Block>

<SectionAnchor chapterId="functions-lambdas" sectionId="higher-order" />

## Типичные ошибки

Все четыре ошибки ниже — реальные сообщения из встроенной песочницы Kotlin Playground (той же, что работает в кнопках Run выше в этой главе). Тексты проверены вживую в этой сессии.

### 1. Значение из Unit-функции

<KotlinPlay code={`fun main() {
    println(square(4))
}

fun square(x: Int) {
    return x * x
}`} />

```text
Return type mismatch: expected 'Unit', actual 'Int'.
```

**Перевод:** «Несовпадение типа возврата: ожидался Unit, а по факту Int». У `square` не указан тип возврата — значит, Kotlin считает его `Unit` («ничего»). А `return x * x` пытается вернуть `Int`. Обещание (неявное `Unit`) не совпало со значением.

**Как починить:** указать настоящий тип возврата — `fun square(x: Int): Int { return x * x }`, — либо, если функция правда ничего не должна возвращать, убрать `return x * x` и оставить только действие.

### 2. Забытый `return` в теле-блоке

<KotlinPlay code={`fun main() {
    println(square(4))
}

fun square(x: Int): Int {
    x * x
}`} />

```text
Missing return statement.
```

**Перевод:** «Отсутствует оператор return». Функция обещала вернуть `Int` (`: Int` в сигнатуре), но тело-блок сам по себе ничего не возвращает — `x * x` здесь просто вычисляется и тут же забывается. В отличие от тела-выражения, тело-блок никогда не возвращает своё последнее выражение автоматически.

**Как починить:** добавить `return` явно — `return x * x`, — либо переписать функцию как тело-выражение: `fun square(x: Int): Int = x * x`.

### 3. Не передан обязательный аргумент

<KotlinPlay code={`fun greet(name: String, greeting: String) {
    println("$greeting, $name!")
}

fun main() {
    greet("Олег")
}`} />

```text
No value passed for parameter 'greeting'.
```

**Перевод:** «Не передано значение для параметра greeting». У `greeting` нет значения по умолчанию, поэтому он обязателен, а вызов `greet("Олег")` передаёт только один аргумент.

**Как починить:** передать оба аргумента — `greet("Олег", "Привет")`, — либо дать `greeting` значение по умолчанию в объявлении: `greeting: String = "Привет"`.

### 4. Лямбда `filter` вернула не `Boolean`

<KotlinPlay code={`fun main() {
    val numbers = listOf(1, 2, 3)
    val big = numbers.filter { it }
    println(big)
}`} />

```text
Return type mismatch: expected 'Boolean', actual 'Int'.
```

**Перевод:** «Несовпадение типа возврата: ожидался Boolean, а по факту Int». Лямбда, переданная в `filter`, обязана отвечать на вопрос «оставить этот элемент?» — то есть возвращать `Boolean` (`true`/`false`). Здесь лямбда `{ it }` просто отдаёт сам элемент, а он `Int`, а не `Boolean`.

**Как починить:** написать настоящее условие, дающее `Boolean`, например `filter { it > 0 }`.

<Block kind="quiz" title="Типичные ошибки" chapterId="functions-lambdas" blockId="quiz-typical-errors">
<SelfCheck chapterId="functions-lambdas" quizId="q4" questions={[
  { q: 'Почему fun square(x: Int) { return x * x } не компилируется?', options: ['Забыта точка с запятой', 'Тип возврата не указан → неявно Unit, а return пытается вернуть Int', 'Параметр x не используется', 'В Kotlin нельзя использовать return внутри функций'], correct: 1, why: 'Без явного : Int тип возврата — Unit. Возврат значения через return x * x конфликтует с этим неявным Unit.' },
  { q: 'Почему fun square(x: Int): Int { x * x } выдаёт "Missing return statement."?', options: ['x * x — недопустимое выражение', 'Тело-блок не возвращает последнее выражение автоматически — нужен явный return', 'Тип Int нельзя использовать как тип возврата', 'Забыта точка с запятой после x * x'], correct: 1, why: 'В отличие от тела-выражения, блок в фигурных скобках требует явного return для каждого пути возврата значения.' },
  { q: 'Какую ошибку покажет компилятор для greet("Олег"), если оба параметра greet обязательны?', options: ["Missing return statement.", "No value passed for parameter 'greeting'.", "Return type mismatch: expected 'Unit', actual 'Int'.", "Unresolved reference 'greeting'."], correct: 1, why: 'Второй параметр без значения по умолчанию обязателен — вызов с одним аргументом не может его заполнить.' },
  { q: 'Почему numbers.filter { it } не компилируется для List<Int>?', options: ['it нельзя использовать в filter', 'Лямбда filter должна возвращать Boolean, а it здесь имеет тип Int', 'filter работает только со списками строк', 'Нужно писать it() вместо it'], correct: 1, why: 'filter решает, оставить элемент или нет, — то есть его лямбда обязана возвращать true/false (Boolean), а не сам элемент.' },
]} />
</Block>

<SectionAnchor chapterId="functions-lambdas" sectionId="typical-errors" />

## Символы главы

Шпаргалка по всем новым знакам и конструкциям этой главы.

<Block kind="cheatsheet" title="Символы главы" chapterId="functions-lambdas" blockId="cheatsheet-symbols" favPayload={{
  kind: 'table',
  head: ['Символ / конструкция', 'Как называется', 'Что делает', 'Пример'],
  rows: [
    ['fun', 'ключевое слово', 'объявляет функцию', 'fun square(x: Int): Int'],
    ['( ) после имени функции', 'список параметров', 'объявляет входные данные функции', 'square(x: Int)'],
    [': после скобок функции', 'аннотация типа возврата', 'указывает, что функция возвращает', '): Int {'],
    ['return', 'ключевое слово', 'возвращает значение из тела-блока и завершает функцию', 'return x * x'],
    ['Unit', 'тип «ничего»', 'возвращаемый тип функций, которые не возвращают значение; пишется неявно, если : Тип пропущен', 'fun greet(name: String) { ... }'],
    ['= после сигнатуры функции', 'тело-выражение', 'заменяет { return ... } на одну строку; тип возврата можно вывести', 'fun square(x: Int) = x * x'],
    ['= значение внутри скобок параметра', 'параметр по умолчанию', 'делает аргумент необязательным при вызове', 'greeting: String = "Привет"'],
    ['имя = значение при вызове', 'именованный аргумент', 'явно указывает, какому параметру передаётся значение, независимо от порядка', 'describe(score = 42, name = "Соня")'],
    ['{ a, b -> ... }', 'лямбда с именованными параметрами', 'стрелка отделяет параметры лямбды от тела', '{ a, b -> a + b }'],
    ['it', 'неявный параметр лямбды', 'имя единственного параметра, если его не назвали явно', 'filter { it > 4 }'],
    ['(Тип, Тип) -> Тип', 'тип функции', 'описывает сигнатуру лямбды/функции как значения', '(Int, Int) -> Int'],
    ['fn(a, b) { ... }', 'трейлинг-лямбда', 'лямбда-последний-параметр выносится за круглые скобки вызова', 'calculate(6, 3) { a, b -> a - b }'],
  ],
}}>

| Символ / конструкция | Как называется | Что делает | Пример |
|---|---|---|---|
| `fun` | ключевое слово | объявляет функцию | `fun square(x: Int): Int` |
| `( )` после имени функции | список параметров | объявляет входные данные функции | `square(x: Int)` |
| `:` после скобок функции | аннотация типа возврата | указывает, что функция возвращает | `): Int {` |
| `return` | ключевое слово | возвращает значение из тела-блока и завершает функцию | `return x * x` |
| `Unit` | тип «ничего» | возвращаемый тип функций, которые не возвращают значение; пишется неявно, если `: Тип` пропущен | `fun greet(name: String) { ... }` |
| `=` после сигнатуры функции | тело-выражение | заменяет `{ return ... }` на одну строку; тип возврата можно вывести | `fun square(x: Int) = x * x` |
| `= значение` внутри скобок параметра | параметр по умолчанию | делает аргумент необязательным при вызове | `greeting: String = "Привет"` |
| `имя = значение` при вызове | именованный аргумент | явно указывает, какому параметру передаётся значение, независимо от порядка | `describe(score = 42, name = "Соня")` |
| `{ a, b -> ... }` | лямбда с именованными параметрами | стрелка отделяет параметры лямбды от тела | `{ a, b -> a + b }` |
| `it` | неявный параметр лямбды | имя единственного параметра, если его не назвали явно | `filter { it > 4 }` |
| `(Тип, Тип) -> Тип` | тип функции | описывает сигнатуру лямбды/функции как значения | `(Int, Int) -> Int` |
| `fn(a, b) { ... }` | трейлинг-лямбда | лямбда-последний-параметр выносится за круглые скобки вызова | `calculate(6, 3) { a, b -> a - b }` |

</Block>

<details>
<summary>Углубиться</summary>

**Unit — не пустота, а настоящий тип.** В отличие от `void` в некоторых других языках, `Unit` в Kotlin — обычный тип с единственным значением (тоже называется `Unit`). Поэтому функцию, возвращающую `Unit`, формально можно использовать всюду, где ожидается значение, — например, передать её результат в `println`, и это не будет ошибкой. На практике так почти никогда не делают: `Unit` пишут (точнее, не пишут явно) для функций-действий, у которых важен побочный эффект, а не результат.

**Замыкание (closure).** Лямбда умеет обращаться не только к своим параметрам, но и к переменным, объявленным снаружи, — и даже изменять `var`-переменные, если они `var`. Например: `var sum = 0; numbers.forEach { sum += it }` — лямбда внутри `forEach` видит и меняет внешнюю `sum`. Это называется замыканием: лямбда «замыкает» вокруг себя часть окружающего кода. Работает это не только для лямбд, но и для локальных функций.

Дальше по теме:
- [Functions](https://kotlinlang.org/docs/functions.html) — официальная документация: параметры, тело-выражение, Unit, области видимости функций (member/local/extension)
- [Lambdas](https://kotlinlang.org/docs/lambdas.html) — официальная документация: функциональные типы, трейлинг-лямбды и раздел Closures подробнее

</details>

<SectionAnchor chapterId="functions-lambdas" sectionId="cheatsheet-symbols" />

## Слова главы

<Block kind="vocab" title="Словарь главы" chapterId="functions-lambdas" blockId="vocab-glossary">
<Flashcards chapterId="functions-lambdas" cards={[
  { term: 'parameter', translation: 'параметр', note: 'имя во входных данных функции, объявленное в её сигнатуре' },
  { term: 'argument', translation: 'аргумент', note: 'конкретное значение, переданное параметру при вызове функции' },
  { term: 'return type', translation: 'тип возврата', note: 'тип значения, которое функция отдаёт наружу через return' },
  { term: 'expression body', translation: 'тело-выражение', note: 'короткая форма функции: = выражение вместо { return ... }' },
  { term: 'block body', translation: 'тело-блок', note: 'тело функции в фигурных скобках, требующее явного return для значения' },
  { term: 'default argument', translation: 'аргумент по умолчанию', note: 'значение параметра, которое используется, если его не передали при вызове' },
  { term: 'named argument', translation: 'именованный аргумент', note: 'аргумент, переданный по имени параметра, а не по порядку' },
  { term: 'higher-order function', translation: 'функция высшего порядка', note: 'функция, которая принимает другую функцию параметром или возвращает функцию' },
  { term: 'closure', translation: 'замыкание', note: 'способность лямбды видеть и использовать переменные из окружающего кода' },
  { term: 'trailing lambda', translation: 'трейлинг-лямбда', note: 'синтаксис, когда лямбда-параметр выносится за круглые скобки вызова' },
]} />
</Block>

<SectionAnchor chapterId="functions-lambdas" sectionId="vocab" />

## Потренируйся печатать

Сначала однострочная функция — тело-выражение:

<Block kind="trainer" title="Сигнатуры и однострочные функции" chapterId="functions-lambdas" blockId="trainer-functions">
<CodeTyping pools={[{ label: 'Функции', snippets: [
  'fun square(x: Int): Int = x * x',
  'fun square(x: Int) = x * x',
  'fun square(x: Int): Int { return x * x }',
  'fun calculate(a: Int, b: Int, operation: (Int, Int) -> Int): Int',
  'fun totalLong(distances: List<Int>, threshold: Int = 4): Int',
  'fun countBy(distances: List<Int>, predicate: (Int) -> Boolean): Int',
] }]} keyboard chapterId="functions-lambdas" trainerId="trainer-functions" targetCpm={110} targetAccuracy={90} />
</Block>

Теперь лямбда с типом функции и двумя именованными параметрами:

<Block kind="trainer" title="Лямбды и цепочки вызовов" chapterId="functions-lambdas" blockId="trainer-lambdas">
<CodeTyping pools={[{ label: 'Лямбды', snippets: [
  'val add: (Int, Int) -> Int = { a, b -> a + b }',
  'distances.filter { it > 4 }.map { it * 1000 }',
  'calculate(6, 3) { a, b -> a - b }',
  'numbers.filter { it > 0 }',
  'distances.filter { it > threshold }.map { it * 1000 }.sum()',
] }]} chapterId="functions-lambdas" trainerId="trainer-lambdas" targetCpm={120} targetAccuracy={90} />
</Block>

<SectionAnchor chapterId="functions-lambdas" sectionId="practice-typing" />

## Куда дальше: проверенные ресурсы

<Block kind="cheatsheet" title="Проверенные ресурсы" chapterId="functions-lambdas" blockId="cheatsheet-resources">

**Русские материалы с нуля**

- [kotlinlang.ru — Функции](https://kotlinlang.ru/docs/functions.html) — официальная документация в переводе: параметры по умолчанию, именованные аргументы, тело-выражение разобраны с примерами.
- [kotlinlang.ru — Лямбда-выражения и функции высшего порядка](https://kotlinlang.ru/docs/lambdas.html) — тот же официальный перевод про лямбды, `it`, функциональные типы и трейлинг-лямбды.
- [Хабр: Kotlin для новичков — всё о функциях за 15 минут](https://habr.com/ru/companies/otus/articles/1008354/) — продолжение статьи про переменные из главы 1, тот же цикл OTUS: декомпозиция, параметры по умолчанию, именованные аргументы.
- [Урок 10: функции в Kotlin](https://ievetrov.ru/kotlin-s-nulya-uroki/urok-10-funkczii-v-kotlin/) — продолжение курса с урока 1: `fun`, параметры, возвращаемый тип на практических примерах.
- [Hyperskill: Kotlin Functions](https://hyperskill.org/university/kotlin/kotlin-functions) — та же площадка, что и в главе 1, теперь тема — функции, именованные аргументы и значения по умолчанию.

**Официальный тур Kotlin (на английском)**

- [Kotlin tour: Functions](https://kotlinlang.org/docs/kotlin-tour-functions.html) — официальный тур: сигнатура, именованные аргументы, значения по умолчанию, однострочные функции и лямбды со встроенными упражнениями и готовыми решениями.

**Потренироваться**

<ToolCard
  name="play.kotlinlang.org"
  url="https://play.kotlinlang.org"
  desc="Та же песочница, что работает в кнопках Run этой главы, но во весь экран."
  embeddable
/>

- [Kotlin Koans: Named arguments / Default arguments / Lambdas](https://play.kotlinlang.org/koans/overview) — в разделе Introduction есть три задачи именно на темы этой главы, с автопроверкой.

- [Видео и материалы сообщества — ниже](#community-sources) — ролики по теме и ссылки от студентов и преподавателей смотри в самом низу страницы.

</Block>

<SectionAnchor chapterId="functions-lambdas" sectionId="resources" />

## Челлендж ⭐

Та же команда из главы 1 пробежала за неделю пять тренировок: `listOf(3, 5, 4, 6, 2)` километров.

**Ступень 1.** Напиши функцию `totalLong(distances: List<Int>, threshold: Int = 4): Int`, которая суммирует в **метрах** только те тренировки, что длиннее `threshold` километров. Используй цепочку `.filter { }.map { }.sum()` из этой главы и объяви функцию как тело-выражение (`=`, без фигурных скобок и `return`). Значение по умолчанию для `threshold` — `4`.

**Ступень 2 ⭐.** Напиши функцию высшего порядка `countBy(distances: List<Int>, predicate: (Int) -> Boolean): Int`, которая возвращает количество тренировок, удовлетворяющих условию `predicate`. Вызови её с трейлинг-лямбдой, чтобы посчитать, сколько тренировок длиннее 4 км.

<KotlinPlay code={`fun main() {
    val distancesKm = listOf(3, 5, 4, 6, 2)
    // ступень 1: замени 0 на вызов своей функции totalLong
    val totalLongMeters = 0
    println("Метров в длинных тренировках: $totalLongMeters")
    // ступень 2 (со звёздочкой): напиши countBy и вызови её с трейлинг-лямбдой
}

// ступень 1: допиши функцию здесь
// fun totalLong(distances: List<Int>, threshold: Int = 4): Int = ...

// ступень 2 (со звёздочкой): допиши функцию здесь
// fun countBy(distances: List<Int>, predicate: (Int) -> Boolean): Int = ...`} />

<details>
<summary>Решение (сначала попробуй сам)</summary>

Ступень 1:

```kotlin
fun totalLong(distances: List<Int>, threshold: Int = 4): Int =
    distances.filter { it > threshold }.map { it * 1000 }.sum()
```

Ступень 2 ⭐:

```kotlin
fun countBy(distances: List<Int>, predicate: (Int) -> Boolean): Int =
    distances.filter(predicate).size

fun main() {
    val distancesKm = listOf(3, 5, 4, 6, 2)
    val totalLongMeters = totalLong(distancesKm)
    println("Метров в длинных тренировках: $totalLongMeters")
    val longCount = countBy(distancesKm) { it > 4 }
    println("Длинных тренировок: $longCount")
}
```

Полный вывод:
```text
Метров в длинных тренировках: 11000
Длинных тренировок: 2
```
5 и 6 км — единственные тренировки длиннее 4 км: `(5 + 6) * 1000 = 11000` метров, и их ровно две.

</details>

<SectionAnchor chapterId="functions-lambdas" sectionId="challenge" />

<Block kind="quiz" title="Экзамен главы" chapterId="functions-lambdas" blockId="exam">
<ChapterExam chapterId="functions-lambdas" questions={[
  { q: 'Что выведет код: greet(name = "Марина", greeting = "Добрый день"), если fun greet(name: String, greeting: String = "Привет") печатает "$greeting, $name!"?', options: ['Марина, Добрый день!', 'Привет, Марина!', 'Добрый день, Марина!', 'Ошибка: перепутан порядок именованных аргументов'], correct: 2, why: 'Именованные аргументы расставляются по имени параметра независимо от порядка вызова; greeting передан явно как "Добрый день", поэтому значение по умолчанию не используется.' },
  { q: 'Что выведет println(distances.filter { it > 4 }.map { it * 1000 }), если distances = listOf(2, 7, 4, 9, 1)?', options: ['[7000, 9000]', '[2000, 7000, 4000, 9000, 1000]', '[7, 9]', 'Ошибка компиляции: filter и map нельзя объединять в одну цепочку'], correct: 0, why: 'filter сначала оставляет элементы больше 4 — [7, 9], затем map переводит каждый в метры, умножая на 1000.' },
  { q: 'fun double(x: Int): Int { x * 2 } — что скажет компилятор и почему?', options: ['Скомпилируется и вернёт x * 2 — тело-блок возвращает последнее выражение само', "Missing return statement — тело-блок не возвращает значение автоматически, нужен явный return", "Return type mismatch: expected 'Unit', actual 'Int' — надо убрать : Int из сигнатуры", "Unresolved reference 'x' — параметр не объявлен"], correct: 1, why: 'В отличие от тела-выражения, тело-блок в фигурных скобках никогда не возвращает последнее выражение автоматически — нужен явный return x * 2.' },
  { q: 'fun describe(name: String, score: Int, isChampion: Boolean = false) вызвана как describe(name = "Игорь") — что произойдёт?', options: ['Скомпилируется: score получит значение 0 по умолчанию', "No value passed for parameter 'isChampion' — он обязателен всегда, даже со значением по умолчанию", "No value passed for parameter 'score' — у score нет значения по умолчанию, а передан только name", 'Missing return statement — describe должна что-то возвращать'], correct: 2, why: 'score — обязательный параметр без значения по умолчанию; именованный вызов не отменяет обязательности: name передан, а score — нет.' },
  { q: 'Почему в fun square(x: Int) = x * x можно не писать : Int для типа возврата?', options: ['Потому что все функции в Kotlin возвращают Int по умолчанию', 'Потому что Kotlin выводит тип возврата по типу выражения справа от = — тот же вывод типов, что и у переменных', 'Потому что тип возврата вообще не обязателен ни для одной функции', 'Потому что x уже объявлен как Int, и функция автоматически получает то же имя типа'], correct: 1, why: 'Тело-выражение — это одно выражение справа от =; компилятор смотрит на его тип и присваивает такой же тип возврата функции, как и при выводе типов у переменных.' },
  { q: 'Почему filter считается функцией высшего порядка (higher-order function)?', options: ['Потому что вызывается через точку после списка', 'Потому что работает только со списками чисел', 'Потому что принимает лямбду (функцию) как параметр — это и есть определение функции высшего порядка', 'Потому что она всегда возвращает Boolean'], correct: 2, why: 'Функция высшего порядка — та, что принимает функцию параметром или возвращает функцию. filter принимает лямбду-предикат, поэтому это функция высшего порядка.' },
  { q: 'Чем параметр (parameter) отличается от аргумента (argument)?', options: ['Это два разных слова для одного и того же понятия', 'Параметр — имя в объявлении функции, аргумент — конкретное значение, переданное при вызове', 'Параметр — только у функций высшего порядка, аргумент — у обычных функций', 'Аргумент задаёт тип, параметр — имя переменной'], correct: 1, why: 'Параметр (x в fun square(x: Int)) — это место в сигнатуре; аргумент (4 в square(4)) — то, что туда передают при вызове.' },
]} />
</Block>

## Что должен уметь

- Объявлять функцию с `fun`, параметрами и типом возврата, объясняя, что делает каждый символ сигнатуры.
- Отличать тело-блок (`{ return ... }`) от тела-выражения (`= ...`) и знать, когда тип возврата можно не писать.
- Объяснять, что такое `Unit`, и когда тип возврата опускают.
- Задавать параметру значение по умолчанию и предсказывать, что произойдёт при вызове без этого аргумента.
- Вызывать функцию с именованными аргументами — в произвольном порядке и пропуская параметры со значением по умолчанию.
- Писать лямбду и с неявным параметром `it`, и с явно названными параметрами через `->`.
- Объяснять, что такое функция высшего порядка, и приводить пример на `filter`/`map`.
- Писать свою функцию, принимающую лямбду как параметр через функциональный тип `(Тип, Тип) -> Тип`, и вызывать её через трейлинг-лямбду.
- Узнавать по сообщению компилятора четыре типичные ошибки функций и лямбд — значение из Unit-функции, забытый `return`, непереданный обязательный аргумент, неверный тип результата лямбды — и чинить каждую.

<SectionAnchor chapterId="functions-lambdas" sectionId="what-you-should-know" />
