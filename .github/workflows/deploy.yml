name: deploy
on: { push: { branches: [main] } }
permissions: { contents: read, pages: write, id-token: write }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm, cache-dependency-path: package-lock.json }
      - run: npm ci && npm test && npm run test:ui && npm run build
      - uses: actions/upload-pages-artifact@v3
        with: { path: build }
      - name: upload build for edu.alspio.com
        uses: actions/upload-artifact@v4
        with: { name: site, path: build, retention-days: 1 }
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: { name: github-pages }
    steps: [{ id: d, uses: actions/deploy-pages@v4 }]
  # Второй адрес: тот же собранный сайт на своём сервере под edu.alspio.com.
  # Зеркало к GitHub Pages — если один адрес недоступен, работает другой.
  # Секреты EDU_HOST и EDU_SSH_KEY заданы в настройках репозитория.
  edu-mirror:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with: { name: site, path: build }
      - name: rsync build to server and refresh
        run: |
          install -m 600 /dev/stdin "$HOME/edu_key" <<< "${{ secrets.EDU_SSH_KEY }}"
          SSH="ssh -o StrictHostKeyChecking=accept-new -o BatchMode=yes -i $HOME/edu_key"
          $SSH root@${{ secrets.EDU_HOST }} 'rm -rf /tmp/edu-alspio-incoming && mkdir -p /tmp/edu-alspio-incoming'
          rsync -az --delete -e "$SSH" build/ root@${{ secrets.EDU_HOST }}:/tmp/edu-alspio-incoming/
          $SSH root@${{ secrets.EDU_HOST }} '/opt/edu-alspio/deploy.sh'
